# Roadmap

Tracking what's done, what's planned, and what's still undecided for this fork.

## Beta target

Aiming for a beta/0.1 release as early as next weekend (no hard deadline — that's an earliest-case, not a commitment). The items marked **[Beta blocker]** in the backlog below are what's actually standing between "works great on this one machine" and something a stranger could reasonably install; everything else in the backlog (compositor, MPRIS, calendar flyout, etc.) is normal post-beta polish, not something a 0.1 needs.

## Done / working

- Dynamic tiling (dwindle + master) — inherited from upstream Hypr, unchanged
- Multi-monitor support — verified end-to-end (bar on every screen, correct per-monitor workarea reservation)
- Workspaces — global pool model; switching pulls a workspace to the focused monitor
- Built-in status bar removed in favor of an external EWMH shell (Quickshell)
- Quickshell rofi-style app launcher (usage-sorted results, icons)
- Quickshell bar (workspaces, clock, system tray, CPU/GPU temp, network status, volume)
- Audio (pipewire / pipewire-pulse / wireplumber) autostart on session launch
- Window rules extended with `title:` matching (upstream Hypr only had `class:` / `role:`)
- Fullscreen window rule for Steam games — confirmed working live on two different games. The mechanism turned out to already be fully implemented upstream (`windowrule=fullscreen,class:...`), just undocumented. Steam sets `WM_CLASS` to `steam_app_<appid>` for any game it launches (native or Proton/Wine), so `windowrule=fullscreen,class:^steam_app_.*$` covers a user's whole Steam library with one rule — activated as the default in both the example config and the user's own.
- Launcher positioning — added a new `center` window rule (reuses the existing dialog/modal auto-centering math, now available to any window via config) and applied it to the launcher; verified it lands pixel-exact in the center of the screen
- Animations fix — the animation speed formula (`1/DELTA`) used an integer-truncated millisecond delta; any two ticks landing under 1ms apart truncated to exactly `0`, causing a division-by-zero (infinite speed) that silently stalled that frame. Measured this actually happening (~1 in 221 idle ticks). Switched to a fractional-millisecond delta; confirmed empirically over hundreds of ticks that the stall case no longer occurs.
- Raised the default animation speed to near-instant (`speed`/`window_resize_speed` both 5→20, now equal) per feedback, updated everywhere a default lives (in-code fallback, autogenerated first-run config, example config) so this is the out-of-the-box experience, not just a personal tweak.
- Launcher auto-focus — floating windows (launcher, dialogs, etc.) never actually got X11 input focus from the WM on creation, only tiled windows did; you had to click in manually. Fixed and verified live (docks/panels correctly excluded, so the bar doesn't steal focus).
- Fixed a near-freeze on launching a game: `_NET_WM_STATE_FOCUSED` was appended to a focused window's state property on every dirty-refresh tick with no dedup, growing unbounded for any window that stays busy/dirty while focused (exactly what a game does). Same bug family as the earlier `_NET_WORKAREA`/RandR loops. Verified fixed under a forced stress test (40 rapid dirty cycles → still exactly one entry).
- Fixed focus-stealing (confirmed resolved live, not just in sandbox testing): launching a game and clicking away to another window would immediately snap focus back to the game. Took five attempts to actually nail: a 1s cooldown broke because the real reclaim happened 7+ seconds later; tracking "the specific window the user moved away from" broke because `eventEnter` fires on every window the pointer passes through en route, not just where it settles; the "does current focus belong to the user" redesign still failed live because it explicitly trusted EWMH's own source==2 ("explicit user action") indicator - confirmed via diagnostic logging that Wine/Proton sends exactly that value for its own internal reclaims, making the trust carve-out a complete no-op against the real culprit; one more asymmetry bug (a user hover into an already-focused-for-other-reasons window wasn't upgrading its status) surfaced during re-verification. Final version trusts no EWMH source value at all - only real clicks/hovers count as user-initiated. The live session's own log now shows real reclaim attempts from the game being rejected during actual gameplay, and the user confirmed focus behaves correctly.
- Bug fixes along the way: missing `xcb-util` build dependency, dock/workarea reservation race at map time, an EWMH `_NET_WORKAREA` infinite-loop freeze, a RandR screen-change notification feedback loop, and a broken session launcher that was silently running a stale system-wide binary instead of the real one
- Fixed popup/dropdown menus instantly closing on click or hover (reported live via Zen Browser's bookmark folder menus, "like a double click really fast"). `eventEnter()`'s fallback for a never-before-seen window unconditionally tried to manage and focus it, unlike `eventMapWindow()` which already skips override-redirect windows via `shouldBeManaged()` — toolkits create menus/dropdowns override-redirect specifically so the WM leaves them alone, and stealing focus the instant the mouse entered one made the toolkit immediately dismiss it. Reproduced end-to-end in a Xephyr sandbox against a real Zen Browser instance (clicking a bookmark folder, then hovering an entry inside it to open a nested tooltip) before and after the fix to confirm it.
- "Stay awake" toggle — a bar icon (coffee cup) that toggles X11 screensaver/DPMS blanking on/off via `xset`. No external tool (`caffeine`, etc.) was worth pulling in: this WM runs no idle-lock/screensaver daemon at all (no `xautolock`/`xss-lock`, nothing under OpenRC), so there's no DBus screensaver service for such a tool to talk to anyway — `xset` is the whole mechanism regardless. State lives in a shared `StayAwakeState` singleton so all monitors' bars agree, since DPMS/screensaver state is global to the X server, not per-monitor. Hit and fixed one bug along the way: attaching a `QtQuick.Controls` `ToolTip` (as an attached property) to the same `Item` as a click-handling `MouseArea` silently ate the click events — confirmed via a `console.log` in `onClicked` never firing even though the icon's hit-region was pixel-verified correct; removing the `ToolTip` fixed it immediately. Verified live: clicking each monitor's icon toggles real `xset` DPMS/screensaver state and all three bars' icons stay in sync.
- Configurable bar modules + a "hidden tray" overflow flyout. Hand-edited at `~/.config/quickshell/modules.json` (JSON, not TOML — Quickshell has no TOML parser but reads JSON natively via `JsonAdapter`), hot-reloaded live (no `qs` restart). Each of the 7 status modules (kernel/cpu/cpuTemp/gpuTemp/network/volume/stayAwake) gets `enabled` (on/off), `screens` (`"all"` / `"primary"` / an array of exact RandR output names), and `tray` (`false` = shown inline in the bar as today; `true` = still active, but tucked into a "..." flyout instead of taking up bar space). Checked Noctalia Shell and DankMaterialShell (both Quickshell-based) for prior art first: neither has the "active but tray-hidden" third state at all, just enabled/disabled — this is a genuine addition, not a port. Adapted Noctalia's simpler `enabled` + per-monitor-match TOML model over DankMaterialShell's much heavier full-GUI-settings-app approach. The flyout itself (`Overflow.qml`) needed the same `windowrule=float,title:...` / `windowrule=center,title:...` treatment the launcher already needed — Quickshell's `FloatingWindow` doesn't self-position under this WM by default, it gets auto-tiled like any other window unless a window rule says otherwise. Verified live: toggling `tray` in the JSON hot-swaps a module between the bar and the flyout with no restart, the flyout opens correctly sized and floating (not tiled), and clicking a module inside the flyout (tested with stayAwake) drives the same shared state a bar-inline instance would.
- GUI settings window (`Settings.qml`, gear icon in the bar) for the same `modules.json` the point above covers — a scoped-down first slice of the deferred full GUI settings app, not the whole WM config (that stays hand-edit-only in `hypr.conf` for now). Per-module Enabled / Screens (All or Primary — pinning to specific monitors by name is still JSON-only) / In-tray toggles, built from plain `Rectangle`/`MouseArea` rather than `QtQuick.Controls` (given the `ToolTip` bug found while building the stay-awake toggle, stuck to the pattern already proven reliable in this codebase). Writes go through the same `FileView`/`JsonAdapter` `ModulesConfig` already used for reading, via full-object reassignment (`Object.assign`) rather than in-place mutation, since QML only notices property *assignment*, not a nested object's field changing — confirmed in-place mutation would have silently failed to persist or update any binding. Verified live end-to-end: toggling a module's tray switch in the GUI immediately updates the bar, the flyout, and the on-disk JSON.
- Idle-based screen lock. `xautolock` (idle timer, `exec-once` in `hypr.conf`) driving `betterlockscreen` (a wrapper around `i3lock-color`, both already installed) as the locker, plus a manual `SUPER+L` keybind. No systemd/logind on this box, so `xss-lock` — which leans on logind's Lock/Unlock DBus signals — wasn't the right fit; `xautolock` has no such dependency. Locks after 10 minutes idle (matching the default DPMS timeout) with the desktop wallpaper blurred behind the prompt, cached once via `betterlockscreen -u <path> --fx blur`. Hit one gotcha: `betterlockscreen -l` takes exactly one effect name matching a cached variant, not a comma list — `-l dim,blur` silently fell back to the plain, unblurred image since "dim,blur" isn't a real cached name (confirmed by checking `~/.cache/betterlockscreen/current/`, which only ever holds one file per single effect, never a combined one). Verification was necessarily two-tier given a real i3lock unlock needs the actual account password, which nothing here can supply: the visual rendering (wallpaper/blur, clock, greeter text) was confirmed in a Xephyr sandbox, cleaned up by killing the process directly rather than unlocking it; the idle-timing mechanism itself was separately confirmed live by swapping in a throwaway `xautolock` instance with a harmless one-line-file locker in place of the real one, watching it fire after its 1-minute threshold, then restoring the real instance — the actual account password was never needed or used anywhere in this process.
- Coupled the stay-awake toggle to the idle lock. These were initially independent: `xautolock` determines idle time via the X Screen Saver *extension* (confirmed via `ldd` showing it links `libXss.so.1` and calls `XScreenSaverQueryInfo`), which keeps counting regardless of whether `xset`'s own screensaver/DPMS blanking is on or off - so turning on stay-awake to e.g. watch a video without touching the mouse would still get the screen locked out from under it once xautolock's timer elapsed. Fixed by having the stay-awake toggle also send `xautolock -disable`/`-enable` (an IPC message to the already-running instance, no restart needed) alongside its existing `xset` calls. Verified live with the same throwaway-instance technique as above: confirmed a test instance's 1-minute timer did *not* fire while disabled, across a full 4-minute idle-polling window (this session's own active use of the machine kept resetting real idle time to near-zero throughout, which is exactly the scenario this fix targets); `-enable` uses the identical IPC mechanism in reverse and follows directly, though it didn't get its own independent live-fire confirmation in this session for the same reason (never a continuous idle stretch to test against) - worth a quick sanity check next time the machine is genuinely left idle with stay-awake off.
- **Renamed the project: Hypr → ZarisWM** (short form **Zaris** for directory names, binary names, etc.). Name landed after several rounds: an earlier candidate, **Xobra** (X11 + the "Cobra" handle nod), was dropped once the goal of eventual use by others (not just personal use) came into focus — reviewing how established WMs are named (Hyprland, Sway, i3, bspwm, dwm, qtile, awesome) showed none of them tie the name to the maintainer's personal handle, which argued against **croXeyeWM** too (a second candidate, more directly tied to the "crosseyedCOBRA" handle). Zaris was checked clean against GitHub, npm, PyPI, and crates.io (crates.io's API 403's on every query from this environment regardless of name, so that check is inconclusive across the board, not a Zaris-specific gap) — the only exact-name GitHub hits are a handful of inactive, 0-star placeholder repos. Execution happened in two passes: an initial commit covered the CMake project/target name (lowercase `zaris`), EWMH `_NET_WM_NAME`, log messages, config path (`~/.config/zaris/zaris.conf`), log path (`/tmp/zaris/`), example config/desktop files, README, CONTRIBUTING.md, and issue templates; a follow-up swept the repo for what that pass missed — leftover `Hypr` strings in `example/zaris.desktop`'s internal fields (`Name=`/`Exec=`/`TryExec=`/etc.), a VS Code launch path, a config comment, the `pkill`/"exit Hypr" keybind-hint strings, a legacy `WM_NAME` placeholder in `windowManager.cpp`, and the internal `HYPRATOMS`/`HYPRATOM` EWMH-atom-map code symbols (renamed to `ZARISATOMS`/`ZARISATOM`, rebuilt clean to confirm). Also renamed everything outside version control: the local repo directory, the GitHub repo itself, `~/.config`, both the per-user and system-wide installed binaries, and the LightDM session entry — old `Hypr`-named copies of all of these were confirmed removed after the switch. `Hyprland` references (the separate, actively maintained Wayland compositor by the same original author) were deliberately left alone throughout, as was the README's fork-attribution mention of the original Hypr project — both are accurate references to real external projects, not this project's own branding.
- **Notification daemon.** `screenshot.sh`'s `notify-send` call (and anything else on the system that uses it) was a no-op with nothing listening on `org.freedesktop.Notifications`. Rather than build one in QML, wired up `dunst` (already installed, and `~/.config/dunst/dunstrc` already existed pre-themed to match this shell's palette exactly — a carryover from the old qtile setup) via `exec-once=dunst` in zaris.conf. Verified live end-to-end with `notify-send` and a screenshot: themed popup renders correctly top-right, doesn't get tiled/mismanaged by the WM (dunst's popups are override-redirect, which `shouldBeManaged()` already skips — same mechanism as the earlier Zen Browser dropdown-menu fix).
- **Volume OSD.** Transient popup on volume up/down/mute, driven by new `OSD.qml`/`OSDState.qml` plus `~/.config/zaris/osd-volume.sh` (adjusts the default sink via `wpctl`, then reports the result to the OSD over Quickshell's IPC: `qs ipc call osd volume <percent> <muted>`) bound to the `XF86Audio*` keys in zaris.conf. Auto-hides ~1.5s after the last call. Verified live: raise/lower/mute all show the correctly themed popup with the right percentage/muted state, and it disappears on schedule. Brightness was scoped out for now — this box is a desktop with no `/sys/class/backlight` device and no `ddcutil` installed, so there's nothing for `XF86MonBrightness*` to actually drive; `OSDState`/`OSD.qml` already support a `"brightness"` kind for whenever that's relevant on different hardware.
- **Bar visual fix: flyout anchoring.** The "hidden tray" overflow flyout (`Overflow.qml`) used to open screen-centered. Investigated having Quickshell position it directly (compute the chevron's global coordinates in QML, set them on the window) — turned out to be impossible: Quickshell's `FloatingWindow`/`WindowInterface` exposes `width`/`height` but no `x`/`y` at all (confirmed via its qmltypes; this is a real cross-platform constraint, not a missed API, since Wayland compositors don't let clients self-position either), so this had to be done WM-side instead. Added a new `topright <marginX> <marginY>` windowrule (`src/events/events.cpp`, `src/config/ConfigManager.cpp`) that positions a floating window relative to its monitor's top-right corner, computed from the monitor geometry and the window's own already-known size — the same pattern the existing `center` rule already uses, just anchored to a corner instead of the middle. Also moved the "more" chevron to be the last (rightmost) bar icon in `Bar.qml` (it previously sat before the screenshot/power/settings icons), so the top-right anchor actually lands under it. Built and installed the new binary; taking effect requires a WM restart (full logout, since this WM has no soft-restart/live-reload of its own binary — only the config file hot-reloads), deliberately deferred rather than done mid-session.
- **[Beta blocker, done] Shipped the Quickshell shell config in version control**, as a new top-level `shell/` directory (same repo, not a companion repo — chosen because the shell is tightly coupled to Zaris-specific window rules like `topright`, added this session specifically because the shell needed it, so keeping WM and shell changes in the same commit history matters more than clean separation would). `shell/` holds `quickshell/` (all the QML, `qmldir`, `modules.json`), `zaris/` (`zaris.conf` paired with this shell, `start-zaris.sh`, and the `powermenu.sh`/`screenshot.sh`/`osd-volume.sh` scripts), `dunst/`, and `rofi/` (the power menu's theme) — see `shell/README.md` for install steps and the full runtime dependency list. Auditing what a fresh install would actually need surfaced two real bugs along the way, both fixed: `Bar.qml` loaded its logo icon and screenshot script from `~/.config/qtile/...` (the old, unrelated qtile setup this WM replaced — a fresh install without that directory would have a broken logo and a dead screenshot button), now moved into the shell bundle itself (`quickshell/assets/artix.svg`, `zaris/screenshot.sh`); and `zaris.conf` had a stale leftover `exec-once=qs -c` (missing its required config-name argument, logging a `--config: 1 required TEXT missing` error every single boot) left over from before a later line replaced it with the correct `qs -d` invocation — removed. Confirmed both fixes live (rebuilt/reloaded, screenshot and logo click both still work). This is meant as the shipped *default* a future first-run wizard (see Backlog) customizes from, not the wizard itself.
- **Genericized the shipped default config** (first slice of the "hone the default" beta blocker below — the rest, bar polish and the dock, is still open). Found a real robustness bug while at it: `zaris.conf`'s `qs -d` (the bar/launcher) was chained after monitor setup and wallpaper via `sh -c '... && ... && qs -d'`, so if either of the first two ever failed, the whole shell silently never launched — split into independent `exec-once` lines so the bar always comes up regardless. For the live config (this machine), kept the real 3-monitor `xrandr` setup and personal wallpaper, just decoupled from the bar's own launch. For the shipped `shell/` copy specifically: dropped the personal 3-monitor `xrandr` call entirely (redundant anyway — `start-zaris.sh` already applies monitor layout before ZarisWM even connects, which is the correct place for it and was already running first regardless), replaced the hardcoded personal wallpaper path with a solid color matching `Colors.qml`'s own `bg` via `xsetroot` (no assumption of a specific image existing), and cleaned up several comments/docs that referenced "the old qtile setup" or instructed keeping colors "in sync with qtile's config.py" — accurate history for this machine, actively misleading for someone with no qtile config to sync with. Also documented (rather than silently removed) a harmless duplicate keybind (`SUPER+C` and `SUPER+Q` both bound to `killactive`) that had accumulated undocumented.
- **Swapped the screen locker from `betterlockscreen`/`i3lock-color` to plain `i3lock`.** The blurred-wallpaper lock screen (see the idle-lock entry above) depended on `betterlockscreen`, which is AUR-only on Arch and doesn't exist in Debian's repos at all — not viable as a cross-distro default. Checked all three target ecosystems directly (Arch/pacman via `pacman -Si` on this machine, Debian/apt via real apt metadata on a Devuan VM, Fedora/dnf via packages.fedoraproject.org) and found `i3lock` is the only screen locker packaged natively on all three — `slock` is missing on Debian, `i3lock-color`/`betterlockscreen` aren't packaged anywhere except the AUR, `physlock` is AUR-only on Arch. Traded away the blur/theming for that portability: `i3lock` has none built in. `zaris.conf`'s idle-lock `exec-once`, its `SUPER+L` keybind, and `powermenu.sh`'s Lock option all updated to call `i3lock` directly instead. Left a note in `zaris.conf` for anyone who wants the blur back later — it'd need a small vendored script (screenshot + blur, then hand off to `i3lock`), since no distro packages that combination as a single thing. See `DEPENDENCIES.md` for the full cross-referenced locker comparison.
- **Swapped the idle-lock timer from `xautolock` to `xss-lock`.** Same portability motivation as the locker swap above: `xautolock` isn't packaged for Debian under any name, and looks AUR-only on stock Arch too (only official via Artix's own `galaxy` repo); `xss-lock` is confirmed official on Arch, Debian, *and* Fedora. Real mechanism difference: `xss-lock` has no `-time` flag of its own — it fires off the X screensaver extension's own activation event instead, so the idle timeout now lives in the X server's screensaver timer (`xset q`) rather than a dedicated option, and it additionally listens for logind's Lock signal (elogind reimplements this, already a dependency here for the power menu's `loginctl` calls), so a suspend/lid event or manual `loginctl lock-session` locks too — `xautolock` never had that. Verified end-to-end in a Xephyr sandbox that `xss-lock -- i3lock` correctly spawns `i3lock` on a forced screensaver-activation event. One knock-on simplification this enabled: the stay-awake toggle's `xset s off` already fully suppresses screensaver activation (confirmed via `xset q` showing `timeout: 0` afterward), and `xss-lock` can't fire without that event, so the separate `xautolock -enable`/`-disable` IPC call the previous setup needed (see the "Coupled the stay-awake toggle" entry above) is gone entirely — `StayAwake.qml` simplified accordingly. Caveat worth being upfront about: the actual auto-fire-vs-suppressed timing comparison didn't reproduce cleanly under Xephyr (nested X servers are known to be unreliable about real idle-timer counting, separate from the forced-activation test which did work), so that specific piece rests on X11's well-established core-protocol semantics for `timeout: 0` rather than a clean sandboxed reproduction — worth a real live confirmation next chance.
- **Built Quickshell from source on Debian, closing the biggest remaining dependency-list gap.** `quickshell` itself was the one non-negotiable shell dependency with no Debian package at all — everything else has a distro-native answer, but without Quickshell the shell simply doesn't run. Read through Quickshell's own `BUILD.md` for its real dependency list and CMake feature flags, then verified an actual from-scratch build end-to-end on a real Devuan Excalibur VM: apt-only build deps (`qt6-base-dev`, `qt6-declarative-dev` + its private-headers package, `qt6-shadertools-dev`, `libdrm-dev`, `spirv-tools`, `libcli11-dev`, `libpipewire-0.3-dev`, `ninja-build`), a `cmake -GNinja` configure with the Wayland-specific features (`WAYLAND`, `WAYLAND_WLR_LAYERSHELL`, `WAYLAND_SESSION_LOCK`, `WAYLAND_TOPLEVEL_MANAGEMENT`, `SCREENCOPY`) and the unrelated `HYPRLAND`/`I3` IPC integrations all turned off (Zaris is X11-only and isn't Hyprland or i3), plus `CRASH_HANDLER`/`USE_JEMALLOC` off since neither's needed and `cpptrace` isn't packaged for Debian either. Configured clean, built clean (592/592), installed clean, and `qs --version` ran successfully afterward — confirmed Quickshell's own CMake install rules create the `qs` → `quickshell` symlink automatically, matching exactly what `zaris.conf`'s `qs -d` invocation expects. Automated as a new `quickshell` step in `contrib/devuan-bootstrap.sh`, which the `nix` step's messaging previously pointed to instead — Nix is now only needed there for a Nerd Font, not Quickshell itself. See `DEPENDENCIES.md`'s "Building Quickshell from source" section for the full recipe.
- **Built the app dock.** `Dock.qml` + `DockConfig.qml` (config: `enabled`, `mode`, `pinned`, backed by `~/.config/quickshell/dock.json`) + `DockIcons.qml` (the icon row) + `DockLauncherIcon.qml` (a static, always-leftmost icon opening the same launcher the bar's logo does). Pinned apps come from a right-click "Pin to Dock"/"Remove from Dock" themed confirmation dialog (`PinDialog.qml`/`PinDialogState.qml`, styled like OSD/Settings, anchored bottom-center just above the dock via the new `bottomcenter` windowrule below) — reachable from either a Launcher result or an existing dock icon. Running apps are tracked via `wmctrl -lx` polled on a timer (same approach as `Workspaces.qml`, since Quickshell has no generic X11 window-list service) and matched to installed `.desktop` entries primarily via `entry.startupClass` (the freedesktop-standard `StartupWMClass=` field, read by Quickshell's `DesktopEntries`), falling back to exact-token comparison against the entry's id/name if unset — never a loose substring match, which turned out to matter: an early substring-based heuristic let a generic WM_CLASS fragment like `"com"` (from a web-app window whose `--class` embeds a domain) false-positive-match a completely unrelated app's id that merely happened to contain the same fragment, showing that app's icon instead. Two modes, both with a Settings toggle: "reserved" (a `PanelWindow`, permanently reserves space like the bar) and "floating" (a `FloatingWindow` anchored via `bottomcenter`, no reserved space, windows can tile underneath). Deliberately does not try to disambiguate two windows that both genuinely report the same WM_CLASS (confirmed live: two WebApp Hub-created web apps sharing one non-isolated Chromium process/profile can end up with the second window inheriting the first's `--class` at the OS level - fixed on the WebApp Hub side, per-webapp process isolation, not something the dock can distinguish since X11 itself can't tell the windows apart at that point).

  Along the way, found and fixed three real, previously-dormant WM bugs that nothing had ever exercised before a dock could be toggled on/off and resized at runtime: (1) `ConfigManager.cpp`/`events.cpp` needed a new `bottomcenter <marginY>` windowrule (mirroring the existing `topright`) for the floating mode's positioning. (2) Floating-window position/size rules (`topright`/`bottomcenter`/`center`/`move`/`size`) only ever apply the *first* time a given window is mapped (gated by `getFirstOpen()`) - fine for every other floating window in this shell since their target position is a fixed formula that never needs to change after first open, but the pin dialog's rule got changed (center → topright → bottomcenter) after it had already opened once, so it stayed stuck at a stale position until the next full `qs` restart created a genuinely new window. (3) `eventUnmapWindow` unconditionally skipped `closeWindowAllChecks` for any dock-type window, which meant hiding one (the dock's own `enabled` toggle) never released its reserved space - nothing had ever unmapped a dock-type window at runtime before, since the bar has no on/off switch. Also found and fixed: `eventConfigure` only ever pushed a resized floating window's new geometry to the X server (`xcb_configure_window`) for dock-type windows via `recalcAllDocks()` - a *non*-dock floating window (the dock's own "floating" mode) that resizes itself post-map had its internal bookkeeping updated with nothing ever applied on screen, so it never visually changed size, while "reserved" mode (dock-type, already going through `recalcAllDocks()`) resized correctly the whole time.

  Also traced and fixed a real, previously-unconfirmed pre-existing bug while debugging why the dock and secondary-monitor bars intermittently didn't appear at all after a fresh boot/login - documented in the next entry below.
- **[Beta blocker, done] Root-cause the dock/workarea reservation sometimes failing at boot.** Root cause confirmed with real evidence, not just a working theory anymore: `/tmp/zaris/zaris.log`'s `New dock created` entries showed, across multiple fresh logins, that secondary-monitor bars (and the app dock) were never created at all - not mispositioned, not missing their reservation, genuinely absent, while the primary monitor's bar came up fine every time. `qs kill` + relaunch reliably fixed it every single time it was reproduced. This points at Quickshell's own screen enumeration (`Variants { model: Quickshell.screens }` in `Bar.qml`), not the WM's dock-handling: Quickshell reads the screen list once at its own startup, and if RandR hasn't finished settling a multi-monitor reconfiguration at that exact instant, it undercounts and never self-corrects for the rest of that process's life - only a full restart re-queries and gets the real count. `start-zaris.sh`'s own `xrandr` call finishing doesn't guarantee the X server's internal state has fully propagated to other clients by the time `zaris.conf`'s `exec-once=... qs -d` runs moments later.

  Fix: `zaris.conf`'s `qs -d` line now polls `xrandr --query`'s connected-monitor count, waiting for it to read the same value on two consecutive checks (0.3s apart, capped at 6s) before launching Quickshell, rather than assuming RandR is settled the instant the WM hands off to `exec-once`. Falls through and launches anyway after the timeout if the count never stabilizes. Confirmed working on a genuine fresh reboot (not just a `qs`-only restart): `zaris.log`'s `New dock created` entries showed all three monitors' bars plus the app dock created correctly on the very first attempt, no manual intervention needed - the first time all session a cold boot has come up clean on its own.

  Worth noting as a separate, later-discovered failure mode with the same *symptom* (bar/tray content going missing) but a different cause, since fixed properly: the reference machine intentionally mirrors a display sometimes (`xrandr --output HDMI-A-0 --same-as DisplayPort-0`), which - as real RandR mirroring, not a glitch - gives `HDMI-A-0` the exact same geometry as `DisplayPort-0`. `Bar.qml`'s `Variants { model: Quickshell.screens }` had no awareness that two distinct screen entries could share one geometry, so it spawned one `PanelWindow` per screen regardless - a second, fully overlapping bar for the mirrored output, which then visually sat on top of the real primary bar (`panel.isPrimary` correctly stayed false for the new one, so its tray/kernel modules were correctly absent from it - they just weren't visible on the *real* primary bar underneath, which is a real bug, not user error). First diagnosed as a possible reconnect glitch since the symptom happened without an obvious trigger at the time; the user later identified the actual trigger themselves (running their mirroring script), which matched exactly. Fixed properly in `Bar.qml`: the `Variants` model now deduplicates `Quickshell.screens` by geometry (`x,y,width,height`) before creating panels, keeping only the first occurrence of each - collapsing mirrored outputs down to a single bar while leaving true multi-monitor (distinct positions) completely unaffected, and preserving `isPrimary`'s correctness since the survivor for the first geometry is always the literal `Quickshell.screens[0]` object. Verified live with the mirror genuinely active (`HDMI-A-0` actively mirroring `DisplayPort-0` at the time of the fix): `zaris.log` showed exactly one bar created at the shared position instead of two, and the primary bar's tray icon and kernel version both stayed visible with the mirror on.
- **Fixed the dock (and bar) rendering as a rounded rectangle floating inside a plain black square.** Root cause: neither is a real compositor-rendered window - both are dock-type (`getNoInterventions()`), which `windowManager.cpp`'s `applyShapeToWindow` (the WM's real X11 `SHAPE`-extension corner-rounding, already used for every regular window) explicitly skipped entirely. So both faked rounding client-side instead: a decorative QML `Rectangle` with its own `radius` inset by a margin from the actual (square, X11-default-opaque) window. With no compositor running to actually blend alpha (confirmed earlier: none is), that inset margin doesn't render as transparent, it renders as plain opaque black - the "square black box" behind the visible rounded shape.

  Fixed at the root rather than patched around: `applyShapeToWindow` now lets dock-type windows through (`pWindow->getNoInterventions() && !pWindow->getDock()`, rather than blocking all `noInterventions` windows unconditionally), and `border_size` is forced to 0 for docks specifically (they don't want a border, and it would otherwise expand the shape mask's bounding box beyond the dock/bar's own real geometry for no reason) - `rounding` still applies normally, using the same config value every other window already uses. `Bar.qml`'s and `Dock.qml`'s decorative `Rectangle`s dropped their own `radius`/`anchors.margins` entirely and just fill their window edge to edge now, since the real window shape is what's rounded going forward, not something drawn inside it. Real aesthetic tradeoff surfaced and confirmed acceptable: the bar previously looked like a rounded pill floating inset from the screen edges (via that margin); shape-masking has no concept of a margin, so it's now a full-width bar with just its four corners rounded instead - a genuine, deliberate look change, not a regression.

  That first pass wasn't actually enough - confirmed still fully square after a real restart, which surfaced two more layers of the same underlying gap, both real bugs rather than cosmetic tweaks: (1) `refreshDirtyWindows()` has its own, *earlier* exclusion that `continue`s past any dock-type window before ever reaching the `applyShapeToWindow` call - the first fix change the function's own internal check, but the function was never even being invoked for docks in the first place. Confirmed via `zaris.log`: `New dock created` fired for every dock window, `Applying shape to` never did for any of them. Fixed by explicitly calling `applyShapeToWindow` for dock-type windows right before that early exclusion's `continue`, without letting them fall into the rest of that block's tiling/animation-oriented logic (none of which applies to them). (2) Even after that, shape application ran (confirmed logged) but had zero visible effect, because its geometry came from `getRealSize()` - a value only ever kept current by the tiling/animation system, which docks skip entirely, so it just sits at its unset default (0,0) forever for them. The shape mask was being computed for a zero-sized window. Fixed by using `EffectiveSize` instead for dock-type windows specifically, which *is* kept correct for them (set at creation in `remapFloatingWindow`, updated on every resize in `eventConfigure`). Confirmed working after this: `zaris.log` shows `Applying shape to <id>` immediately after each `New dock created` for all four dock windows, and a pixel-level screenshot of the bar's corner confirmed a real rounded cutout (the desktop background showing through), not a square one - both the bar and the dock render correctly rounded now.
- **Installed the Nerd Font directly from upstream, closing the last dependency-list gap.** Neither Debian nor Fedora officially packages a *patched* Nerd Font (only Arch does, `ttf-jetbrains-mono-nerd`), and the per-distro workarounds were inconsistent — a COPR on Fedora, Nix on Debian, two different answers to the same problem. Instead, pull the font directly from the upstream Nerd Fonts project's own GitHub release archives, which installs identically on any distro with no package manager involved: extract `JetBrainsMono.tar.xz` into `~/.local/share/fonts/`, run `fc-cache -f`. Verified end-to-end on the Devuan VM — `fc-match 'JetBrainsMono Nerd Font:style=SemiBold'` resolved correctly afterward, the exact family+style string `shell/dunst/dunstrc` uses. Automated as a new `nerdfont` step in `contrib/devuan-bootstrap.sh`; the `nix` step's messaging updated since Nix is no longer needed for anything this shell requires. See `DEPENDENCIES.md`'s "Installing the Nerd Font from upstream" section.
- **Fixed the WM's own debug logging being completely silent in normal use.** Discovered while chasing a live bug report (windows tiling underneath the bar again, workarea reservation lost) that turned out to be unconfirmable because the logs meant to diagnose it didn't exist: `clearLogs()` (`Util.cpp`) only ever `unlink()`s `/tmp/zaris/{zaris,zarisd}.log`, never creates the `/tmp/zaris/` directory itself - on a real machine (`/tmp` is tmpfs, wiped every boot), that means `Debug::log`'s `ofstream::open()` for either file fails silently on the very first run after any reboot, and stays broken for the rest of that session. Separately, stdout is fully-buffered (not line-buffered) whenever it isn't attached to a terminal - true for how this WM actually runs, launched via a session script - so even the `printf` half of every log call was sitting unflushed in memory rather than reaching `~/.xsession-errors`, for a long-lived process that hadn't logged enough yet to fill the buffer. Fixed both: `clearLogs()` now calls `std::filesystem::create_directories("/tmp/zaris")` first, and `main()` calls `setvbuf(stdout, nullptr, _IOLBF, 0)` before any logging happens. Verified end-to-end: ran the newly built binary directly after deleting `/tmp/zaris` to simulate a fresh boot, confirmed `zaris.log` now gets created and populated, and confirmed stdout showed up promptly rather than being trapped in a buffer. (That test run briefly became a second, real WM instance for ~2 seconds since XCB doesn't refuse the connection just because another client already holds `SubstructureRedirect` - it was killed via `timeout` before doing anything, and the live session was confirmed undisturbed afterward, but worth knowing that quirk exists if testing this again.) The actual dock/workarea bug that prompted this was root-caused and fixed later the same session (see the "Root-cause the dock/workarea reservation" entry above) - restarting Quickshell fixed it live in the moment, but the underlying boot-time cause wasn't confirmed until this logging fix made the evidence readable.
- **Bluetooth applet.** Full toggle/pair/connect/forget UI, not just a power switch — a new `bluetooth` bar module (`BluetoothIndicator.qml`, following the same generic-module pattern as `nightLight`/`stayAwake`: wired into `ModulesConfig.qml`'s `moduleIds`, so it gets a `modules.json` entry and a `Settings.qml` GUI row for free) whose click opens `BluetoothPanel.qml`, a new flyout (`FloatingWindow`, `windowrule=float`+`center,title:^Bluetooth$` in `zaris.conf`, same mechanism as the settings/overflow windows) listing every device BlueZ knows about with Pair/Connect/Disconnect/Forget actions plus an adapter power toggle and a scan-for-devices toggle. Talks to BlueZ directly over D-Bus via Quickshell's own native `Quickshell.Bluetooth` module (`BluetoothAdapter`/`BluetoothDevice`/`Bluetooth` singleton) — no shelling out to `bluetoothctl` anywhere, unlike the wmctrl-based approach used for the dock/workspaces. Named the bar icon component `BluetoothIndicator` rather than the shorter `Bluetooth` specifically to avoid colliding with `Quickshell.Bluetooth`'s own `Bluetooth` singleton, which both this file and the panel need to import directly. Pairing confirmation (PIN/passkey/"just works") is handled internally by Quickshell's bundled agent — there's no separate agent API exposed to QML for legacy PIN-entry devices, so those aren't handled, but that's the rare case; modern audio/input devices use "just works" SSP and are unaffected. Found while scoping this that `bluez` (the daemon) was already installed on the reference machine but never actually running — Artix, unlike Debian/Fedora, has no systemd unit to auto-enable it, and needs the separate `bluez-openrc` package plus `rc-update add bluetoothd default && rc-service bluetoothd start`; documented in `DEPENDENCIES.md` (confirmed `bluez` itself packaged on all three distros via sources.debian.org and packages.fedoraproject.org). Verified in a Xephyr sandbox that the QML loads cleanly and falls back to a clear "No Bluetooth adapter found" message when `bluetoothd` isn't reachable over D-Bus. Confirmed live on the reference machine after installing `bluez-openrc` and starting the service: Quickshell only connects to `org.bluez` once at its own startup (no retry if the daemon appears later), so a `qs kill`+relaunch was needed after `rc-service bluetoothd start` for the panel to pick it up — after that, the bar icon correctly switched from muted to powered-on blue, and the panel's power toggle, scan-for-devices toggle, and live device list all worked scanning for real nearby devices. Pair/connect/disconnect/forget on an actual device is untested and likely to stay that way on this particular reference machine - the buttons exist and call straight through to `BluetoothDevice`'s own `pair()`/`connect()`/`disconnect()`/`forget()` methods (the same Quickshell-provided API other Quickshell-based shells use for the same purpose), but there's no Bluetooth device available to test against day-to-day on this machine. Not treated as a blocker; worth a real test opportunistically if a device is ever on hand.
- **Night light bar toggle.** A new `nightLight` bar module (`NightLight.qml`/`NightLightState.qml`), following the exact same shared-singleton-toggle pattern as `StayAwake.qml` — a moon icon that flips a global on/off state, since the underlying mechanism (X11 RandR gamma ramps) is global to the X server, not per-monitor. Manual/instant rather than automatic time-of-day scheduling for this first pass: clicking on applies a fixed warm color temperature (4500K, `redshift`'s own day/night default) via `redshift -O 4500` (one-shot, adjusts the gamma ramps and exits — no daemon to manage), clicking off resets via `redshift -x`. Picked `redshift` over `gammastep` since this is X11-only (gammastep's main advantage is Wayland support, not needed here) and confirmed officially packaged on all three target distros (checked live via sources.debian.org and packages.fedoraproject.org, not just assumed) — added to `DEPENDENCIES.md`. Wired into the existing generic bar-module infrastructure rather than a one-off: added to `ModulesConfig.qml`'s `moduleIds` list and default JSON shape, so it automatically gets a `~/.config/quickshell/modules.json` entry (enabled/screens/tray, same as every other module) and a GUI toggle row in `Settings.qml` for free, plus a place in `Overflow.qml`'s hidden-tray flyout — no bespoke plumbing needed beyond the module itself. Confirmed working live after installing `redshift` on the reference machine: toggling applies/resets the warm color temperature correctly.
- **Refuse to start a second instance when a WM is already running.** `windowManager.cpp`'s `setupManager()` selects `SubstructureRedirect` on the root window — the one X11 hook that lets exactly one client act as the window manager — via `xcb_change_window_attributes_checked`, but discarded the result without ever checking it. X11 itself refuses this specific request with `BadAccess` if another client already holds it, but XCB doesn't refuse the *connection* for that, so a second `zaris` instance previously ran silently as a second, real, competing WM (confirmed happening live, briefly, while testing an earlier logging fix). Fixed by capturing the request's cookie and calling `xcb_request_check` on it — the exact same checked-request idiom already used two functions earlier in the same file (`setupColormapAndStuff`) — logging a `CRITICAL` message ("is another window manager already running?") and calling `exit(1)` if it comes back with an error, rather than continuing. Verified in a Xephyr sandbox: a second instance launched against a display already running one now logs the error (X error code 10, `BadAccess`) and exits with status 1 immediately, instead of running at all.
- **Confirmed focus-follows-mouse (hover-to-focus) already exists and is already the default.** Surfaced while listing tiling-WM features potentially missing from this project — wrongly flagged it as absent without checking first. It's actually a pre-existing `focus_when_hover` config value (`ConfigManager.cpp`, default `1`) already wired into `events.cpp`'s `eventEnter`: moving the pointer into any window (tiling or floating) focuses it immediately, no click needed, and this is already the value in the C++ default, the shipped example config, and the reference machine's live config. Setting it to `0` instead disables hover-focus specifically for *tiling* windows (per its own config comment) - floating windows always get it either way. No code changed; this entry exists purely so the feature doesn't get wrongly re-flagged as missing again.
- **Labeled the hidden-tray flyout's modules.** `Overflow.qml`'s rows used to be icon+value only (fine in the compact bar, where space is tight, but a flyout list has room to say what each row actually is). Each module is now wrapped in a label + component `Row` inside `Overflow.qml` itself - not inside the module files, and not in `Bar.qml`'s own compact usage, so the bar's tight layout is completely unaffected. Wrapping rather than modifying the components also meant zero risk to the modules that already have real click behavior (Volume opens `pavucontrol`/right-click mutes, Stay Awake/Night Light toggle, Bluetooth opens its panel) - the label is just a sibling `Text` to their left, never something layered on top that could shadow their existing `MouseArea`. Confirmed live (with real mirroring active on the reference machine, coincidentally a great stress test - see the mirrored-screen entry above) that the flyout still renders correctly and existing interactive modules are unaffected by the wrapping.
- **Restructured the settings window into a sidebar-nav shell.** First step of the deferred "GUI settings app, full WM config" backlog item - `Settings.qml` was a single long scrolling list (bar module table, then a Dock section below it); the user shared a screenshot of Noctalia's own settings panel (categorized sidebar + content pane) as the concrete target to work toward, and this is that shell built out for real. Two categories exist so far - "Bar" (the same per-module Enabled/Screens/In-tray table as before) and "Dock" (the same Enabled/Mode toggle) - deliberately not padded out with placeholder categories for WM features that have no GUI yet (keybinds, window rules, gaps/borders); those get their own category exactly when they get real settings to hold, not before. Still writes through the exact same `ModulesConfig`/`DockConfig` `FileView`s as before - purely a structural reorganization, no behavior change to how any individual setting works. Fixed window size (680x460) with an internal `Flickable` per category rather than the window resizing itself per category (matching Noctalia's own fixed-size panel) - content taller than the window scrolls rather than growing the window. Verified in a Xephyr sandbox: both categories render and navigate correctly, all existing toggles (module enabled/screens/tray, dock enabled/mode) still work, and conditional visibility (the Dock "Mode" row correctly hiding when Dock is disabled) still behaves the same as before the restructure.
- **OSD icon/text padding fix.** The volume/brightness OSD's icon glyph and its percentage/label text were a single concatenated string (`" " + "65%"`) inside one `Text`, which left almost no visible gap between them. Split into a `Row` of two separate `Text` elements (icon, then label) at 8px spacing, the same icon+label pattern already used throughout `Bar.qml`'s status modules, rather than relying on a literal space character to do layout work. Verified in a Xephyr sandbox across all three OSD states (volume, muted, brightness) via IPC-triggered screenshots, then deployed live and confirmed by the user — spacing now reads clearly; a small remaining aesthetic issue was noted but functionality/readability is what mattered here and that's resolved.
- **Fixed the bar/dock never coming back after a fullscreen game exits.** Root-caused a real race condition in `processDockHiding()` (`windowManager.cpp`): when a workspace's fullscreen state engages, docks on that monitor are hidden via `xcb_unmap_window`, with the resulting `UnmapNotify` suppressed via `Events::ignoredEvents` - a sequence-number match list capped at 20 entries (`recieveEvent()`, `windowManager.cpp`). Under the burst of window/property events a game exiting fullscreen genuinely generates, that entry can get evicted before the real event arrives, so it falls through to `eventUnmapWindow()` and gets treated as a genuine close: `closeWindowAllChecks()` permanently removes the dock from tracking via `removeWindowFromVectorSafe()` - it's simply gone from `windows` by the time fullscreen later clears and `processDockHiding()` wants to re-map it. A `qs kill` + relaunch "fixes" it only because that creates a brand-new window with a fresh `MapRequest`, unrelated to the stale tracking entry - never the actual bug.

  Fixed in `Events::eventUnmapWindow` (`events.cpp`): before falling through to `closeWindowAllChecks`, check `PCLOSEDWINDOW->getDock() && PCLOSEDWINDOW->getDockHidden()` and skip entirely if true. `getDockHidden()` is set `true` by exactly one code path in the whole codebase (confirmed via grep) - `processDockHiding()`'s own hide branch - so it's a reliable, race-free signal that this specific unmap is an intentional, temporary hide, independent of whether the racy sequence-number list caught it. The dock's config-disable toggle (Quickshell's own `visible: false`) is unaffected - `getDockHidden()` stays false for that path, so it still goes through the normal close-and-`recalcAllDocks()` handling it always has.

  **A second, completely unrelated, real crash bug found and fixed while building a reproduction for the above**: `EWMH::updateClientList()` (`ewmh.cpp`) built a `std::vector<xcb_window_t>` of currently-tiled (non-floating) windows, then unconditionally dereferenced `&windowsList[0]` to get a raw pointer for `xcb_change_property` - undefined behavior whenever that vector is empty (zero tiled windows currently open, e.g. right after login before any tiled window exists, or any time the last tiled window closes while only floating windows remain). This codebase's normal build is Release, so this had been running as silent UB in practice, not a visible crash - only surfaced because reproducing the dock bug required a Debug build (`_GLIBCXX_ASSERTIONS` catches the same out-of-bounds access cleanly as a `SIGABRT`, confirmed via a full `gdb` backtrace pointing straight at `std::vector::operator[]` called from here via `eventMapWindow`). This function runs on nearly every window map/unmap/destroy event, so it was a real, frequently-reachable path, not a theoretical one. Fixed by using `nullptr`/length-0 when the vector is empty - exactly the correct EWMH semantics for "no tiled windows" anyway, not just a defensive guard.

  Both bugs verified with a purpose-built minimal test harness (`fstest.c`, a ~150-line xcb client compiled against the same `xcb`/`xcb-icccm`/`xcb-ewmh` libraries the WM itself links) run against the real `zaris` binary as the actual window manager in a nested Xephyr sandbox - not just the Quickshell shell config this project's Xephyr testing has otherwise always meant. Built a separate Debug configuration (`build-debug/`, `-DCMAKE_BUILD_TYPE=Debug`) so `ISDEBUG` routes to `~/.config/zaris/zarisd.conf` instead of the real live `zaris.conf` - a pre-existing (harmless, autogenerated-placeholder) file from long before this project's `Hypr`->`Zaris` rename, temporarily given one extra `windowrule=fullscreen,class:^steam_app_.*$` line for the test and restored byte-for-byte afterward, so none of this touched the real live config the actual running desktop uses.

  The EWMH crash was reproduced and fixed first (blocking any further testing entirely - it crashed on the very first dock window's own creation, before a fullscreen game was even involved). With that fixed, the dock-hide bug needed genuinely deterministic proof rather than hoping to catch the race by luck: the test harness's "dock" client re-maps and immediately re-unmaps its own window right after observing the WM's own hide-unmap, producing a second real `UnmapNotify` the WM's `ignoredEvents` has *zero* record of - guaranteed (not probabilistic) to exercise exactly the "suppression didn't catch it" path regardless of real-world timing. Ran this as a matched pair: against the unfixed code, the dock stayed `UNMAPPED` permanently even after the fullscreen game fully closed (the bug, reproduced on demand); against the fixed code, the WM's log showed the new guard firing by name ("is our own fullscreen dock-hide, not a real close - skipping closeWindowAllChecks") and the dock ended up back in a real, queried `VIEWABLE` state via a small `check` mode added to the same harness (`xcb_get_window_attributes`'s `map_state`, not just inference from log lines).

  Both fixes are staged in the installed binary (`~/.local/bin/zaris`, safely swapped via `cp --remove-destination` while the real live WM process kept running unaffected off its already-open, now-unlinked inode) but **not yet active** - this WM has no live-reload of its own binary, only config hot-reload (same limitation the earlier `topright` windowrule fix hit), so picking this up needs a full logout/login, deliberately not done automatically mid-session.
- **[Beta blocker, done] Generate a complete dependency list.** `DEPENDENCIES.md` now cross-references exact package names for both the WM's own build deps and `shell/`'s runtime deps across all three of Arch/pacman, Debian/apt, and Fedora/dnf side by side, linked from both `README.md` and `shell/README.md`. Two genuine gaps surfaced along the way, not naming differences — the idle-lock timer (`xautolock`, not packaged for Debian under any name) and the screen locker (`betterlockscreen`/`i3lock-color`, AUR-only on Arch and unavailable on Debian) — and **both are now resolved** (see the two Done entries above): `xautolock` → `xss-lock`, `betterlockscreen`/`i3lock-color` → plain `i3lock`, both confirmed officially packaged on all three. Two more gaps surfaced and got resolved the same way: `quickshell` itself wasn't packaged for Debian at all (fixed by building from source with apt-only deps, verified end-to-end on the Devuan VM), and neither Debian nor Fedora officially packages a patched Nerd Font (fixed by pulling it directly from the upstream Nerd Fonts project's GitHub releases instead of reaching for inconsistent per-distro third-party sources — see the two newest Done entries above). At this point `DEPENDENCIES.md` has zero genuinely non-uniform gaps left open — everything the shell needs works identically on Arch, Debian, and Fedora.

  Zaris's own WM build now has the from-scratch verification treatment on **all three** of Arch, Debian, and Fedora: cloned fresh, `cmake` configured clean against exactly the documented package list for each (every `pkg_check_modules` module resolved, confirmed via `pkg-config --exists` before even trying), built clean, and produced a working, correctly-linked binary. Debian's pass ran on a real Devuan Excalibur VM. One interesting non-issue surfaced there: the built binary's `ldd` output doesn't list `libxcb-ewmh.so` at all on Debian, unlike Arch's build — traced to Debian's toolchain linking with `--as-needed` by default, which drops the `DT_NEEDED` entry for `xcb-ewmh` since the codebase only uses its header (`#include <xcb/xcb_ewmh.h>`) for type/constant definitions and never actually calls any of its functions (confirmed via `grep`: the only `xcb_ewmh_*` match in `src/` is inside a comment). Harmless, not a missing dependency, just a tidier binary on Debian.

  Fedora's pass ran on a real Fedora Linux 44 VM, provisioned headlessly via `virt-install --unattended` (netinst boot ISO alone wasn't enough for a fully unattended install — needed `--location <network install tree URL>` plus explicit `--extra-args "console=ttyS0,115200n8 inst.text inst.notmux"` to get Anaconda's kickstart install to actually run over a serial-only console rather than defaulting to a VGA console with no display attached; the `jeos` unattended profile installs headless and shuts down cleanly after kickstart completes rather than rebooting, which is expected behavior, not a failure, and just needed a manual `virsh start` afterward). `dnf install` of the exact documented package list, `cmake` configure, and the full build all came back completely clean — every `pkg_check_modules` module resolved via `pkg-config --exists` first, then the real build succeeded outright. One interesting non-issue in the opposite direction from Debian's: Fedora's `ldd` output *does* include `libxcb-ewmh.so.2` (unlike Debian, which drops it via `--as-needed`) — just a toolchain default difference between the two, not a sign anything's wrong on either side. No package-name gaps found; `DEPENDENCIES.md`'s Fedora column needed no corrections. This closes the last piece of this item — all three distros now have a real, verified, from-scratch build, and there are no known gaps left anywhere in `DEPENDENCIES.md`. The eventual per-distro install script follow-on (explicitly not itself required for beta) can now be picked up whenever, with no verification work still blocking it.
- **Fixed the bar's reserved screen space not being reclaimed when its position changes at runtime** (reported live: moving the bar from top to bottom, in either the "statusbar" or "taskbar" layout mode, left a permanent gap at the old top edge that tiled windows never grew back into). Root-caused in `Events::eventConfigure` (`events.cpp`) — a dock window's `ConfigureRequest` handler (fired when Quickshell's `Bar.qml` toggles its `anchors.top`/`anchors.bottom` and the window moves) only ever updated `DefaultPosition`/`DefaultSize` (plus `EffectivePosition`/`EffectiveSize`), never the plain `Position`/`Size` members. `recalcAllDocks()` (`windowManager.cpp`) — the function that resets and recomputes every monitor's reserved top/bottom/left/right edge — bases its "which edge is this dock on" decision on `getPosition()`/`getSize()`, not the Default ones, so after the very first map those two members went stale forever: the window itself correctly moved on screen (since that part uses `DefaultPosition`/`DefaultSize`), but `recalcAllDocks()` kept reserving space at the bar's *original* edge, matching the reported bug exactly.

  Fixed by also syncing `setPosition(getDefaultPosition())`/`setSize(getDefaultSize())` inside `eventConfigure`, mirroring the same sync that already happens once at window-creation time (`Events::remapFloatingWindow`). Verified against the real `zaris` binary (Debug build, isolated `$HOME`/`zarisd.conf`, same discipline as the earlier fullscreen-dock-bug pass) in a Xephyr sandbox: with a tiled terminal window open, toggling `bar.json`'s `position` between `"top"` and `"bottom"` (live, the same file Settings' bar-position control writes) correctly reclaimed the old edge and reserved the new one every time, confirmed via screenshots before/after each toggle; re-verified once more with `layoutMode` also set to `"taskbar"`, matching the user's report that the bug wasn't specific to either layout mode. Staged in `~/.local/bin/zaris` (safely swapped via `cp --remove-destination` while the real live WM process kept running unaffected) but **not yet active** — same no-live-reload limitation as every other WM-binary fix here, needs a full logout/login to take effect.

- **Decoupled the shell/WM interface (first step toward splitting `shell/` into its own repo).** User request: swap the bundled compositor for picom (done above) and stop assuming the shell can only ever live in this same repo as the WM, so other bars/shells could target Zaris and this shell could in principle run under another WM later — but only decouple for now, the actual repo split is deferred. Added `shell/INTERFACE.md`, compiling what was previously scattered across `zaris.conf`'s own comments and individual QML file headers into one contract: which parts already work against any EWMH-compliant WM as-is (workspace/taskbar EWMH properties, the anchored `PopupWindow`s, the `qs ipc call` surface), the Zaris-specific parts a different WM would need to replicate (the full per-title float/position window-rule table, and the "float every shell-created popup above tiling" always-on-top requirement), and the one deep coupling point not addressed this pass (`ThemeConfig.qml` directly regex-rewrites `col.active_border`/`col.inactive_border` lines inside `zaris.conf`'s own text, rather than going through EWMH/IPC — flagged as real follow-up work for whenever the repo split actually happens, not solved here). `shell/README.md` now links to it.

- **[Beta blocker, done] Verify on a non-OpenRC (systemd) system.** The reference machine switched from Artix/OpenRC to Arch/systemd this session, giving a real (not VM/sandbox) end-to-end test: build, install, full shell session (picom, dunst, rofi, xss-lock, the Quickshell bar/dock) all confirmed working live. The "these exec-once lines are harmless no-ops under systemd" assumption this item's own earlier writeup made turned out to be **wrong**, not just unverified - a real bug, caught live: `zaris.conf`'s `exec-once=pipewire`/`pipewire-pulse`/`wireplumber` directly launching those binaries races systemd's own `pipewire.socket`/`pipewire-pulse.socket` activation for the same runtime sockets (`/run/user/1000/pipewire-0`, `/run/user/1000/pulse/native`), and repeating that race across a handful of logins (this session restarted the WM several times testing other changes) was enough to trip systemd's start-limit and leave both sockets permanently in a failed, refusing-activation state - `pactl info` returned "Connection refused," audio devices still enumerated (ALSA-level, unaffected) but nothing routed, confirmed and reproduced live rather than guessed. Fixed by gating each line behind `command -v systemctl`: skip entirely on a systemd system (trust the already-enabled socket-activated units), launch directly same as before when there's no systemd at all (OpenRC/runit). Verified the fix: `systemctl --user reset-failed`, cleared the stale processes/socket files, let systemd's own units bind cleanly, confirmed `pactl info` connects and real audio plays (`pw-play` against a real sound file, audible). `powermenu.sh`'s `loginctl` and `xss-lock`'s logind suspend/lid hooks needed no changes - both already talk to the real systemd-logind here (not elogind, Arch is systemd-native), same interface either way, exactly as this item's original writeup expected.

## Noctalia design-system port — running list

Investigated whether Noctalia (a Quickshell-based shell, screenshotted by the user as a design target for the settings app) could be adopted wholesale to save time. Current Noctalia (`noctalia-dev/noctalia`, main branch) turned out to be a full C++/Qt rewrite (215 `.cpp` files vs. 26 `.qml`), tightly coupled to their own custom Wayland compositor ("Umbriel") - not portable to an X11 WM's plain-QML-on-Quickshell shell at all. `noctalia-dev/noctalia-qs` (their toolkit repo) turned out to just be an archived fork of Quickshell itself with one Wayland-only compositor-protocol patch - irrelevant to X11, no value.

However, the **pre-rewrite `v4.7.7` tag** of `noctalia-dev/noctalia` is a genuine MIT-licensed, plain-QML Quickshell shell - same architecture as Zaris. Of its 418 QML files, only ~43 (~10%) import Wayland/compositor-IPC-specific Quickshell modules (`Quickshell.Wayland`, `Hyprland`/`Sway`/`I3` IPC) and can't be ported; the rest (widgets, styling, generic services) has no such coupling. Per explicit user direction, the approach is to pull as much of that ~90% as reasonably adaptable now, rather than pre-filtering for "do we need this yet" - reassessing what to keep/drop happens after, not before, porting. See `README.md`'s "Third-party code" section for the MIT attribution this carries.

**The actual end goal, stated explicitly by the user**: get Zaris's shell UI as close as reasonably possible to Noctalia v4.7.7 - functionality and a pleasing aesthetic, not a pixel-exact 1:1 replica - then build new Zaris-specific features on top of that foundation. Part of the motivation is onboarding: someone coming from Noctalia (or a similar shell) should find Zaris's workflow and layout intuitively familiar without it needing to be indistinguishable. Not port-for-its-own-sake. This is a three-phase effort, and the phases are sequenced deliberately rather than interleaved: **(1)** finish pulling the portable "ingredient set" first - the design tokens, color-role system, generic widget library, and portable services, all tracked below - **(2)** only after that set is more complete, do one cohesive pass rebuilding Zaris's *existing* widgets/screens (bar, dock, launcher, the individual status modules) to actually use those ingredients consistently, since Noctalia's own bar/dock/panel *composition* files are exactly the Wayland-coupled ones that can't be ported - the ingredients transfer, the assembly is still Zaris's own to (re)build, and porting the ingredients alone doesn't retroactively reskin anything already built with the older hand-rolled `Rectangle`/`Text` pattern - **(3)** new custom features on top of that unified foundation. **Phase 1 is now substantially complete** (27 of ~50 `Widgets/`, the Material color-role system, and every genuinely portable Service either shipped or explicitly resolved with a documented reason - see the running list below for the full detail): confirmed with the user directly rather than assumed, since the remaining unported Phase-1 items are overwhelmingly blocked *by* Phase 2 itself (a settings-GUI rebuild would need `NSectionEditor`/`NColorPickerDialog`/`NKeybindRecorder`; `NFilePicker`'s icon-mapping work has no forcing consumer until something actually needs a file browser) rather than being independently pullable ingredients - so **Phase 2 starts now**, and whatever Phase-1 ingredients turn out to be genuinely needed will surface naturally while rebuilding real screens, rather than being guessed at in the abstract. Only 3 toggles (`Settings.qml` x2, `BluetoothPanel.qml`) use the new `ToggleSwitch` component so far - everything else in the shell still uses the pre-port hand-rolled `Rectangle`/`Text` pattern; that's what Phase 2 replaces, screen by screen.

  Phase 3's plan, once reached: first cross-reference what Zaris has by then against **current Noctalia (v5+, the C++/Umbriel-compositor rewrite)'s actual feature set** specifically - not to port any of its code (still fundamentally incompatible, C++/Wayland-coupled, per the investigation above), but to identify *features* it gained in the rewrite that v4.7.7 never had and Zaris therefore hasn't inherited either, and see which of those are worth building fresh, X11/XLibre-native equivalents for. Only after that cross-reference move on to genuinely new, Zaris-original features with no Noctalia counterpart at all.

Status of what's been pulled so far (updated as the port progresses):

- **[Done]** `Style.qml` - design-token scale (font sizes/weights, radii, margins, border widths, opacity levels, animation durations), adapted from their `Commons/Style.qml`, stripped of dynamic per-user scale multipliers and bar-density/position sizing (Zaris's bar is fixed single-density/position, that axis doesn't apply).
- **[Done]** `ToggleSwitch.qml` - animated toggle switch (smooth color/position transition, pointing-hand cursor), adapted from `Widgets/NToggle.qml`, stripped of its bundled label/description/icon prefix and "changed from default" indicator tooltip (both I18n-driven, no Zaris equivalent). Wired into `Settings.qml` (Bar/Dock category toggles) and `BluetoothPanel.qml` (power toggle), replacing the static hand-copied `Rectangle` blocks that existed in both places.
- **[Done]** Full Material Design 3 color-role system, adapted from `Commons/Color.qml` (507 lines) - `Colors.qml` extended with 16 named roles (`mPrimary`/`mSecondary`/`mTertiary`/`mError`/`mSurface`/`mSurfaceVariant`/`mOutline`/`mHover` plus their `mOn*` pairs) mapped onto Zaris's existing 8-color palette (blue→primary, teal→secondary, coral→tertiary, bg→surface, pill→surfaceVariant, textMuted→outline, pillActive→hover), plus `resolveColorKey`/`resolveOnColorKey`/`resolveColorKeyOptional`/`colorKeyModel` helpers. One genuinely new color added (`red`, backing `mError` - nothing existing was a real alarm/danger tone). The existing 8 flat names are untouched, nothing that already used them needed to change. Deliberately does NOT include their dynamic wallpaper-based color generation algorithm - that's a separate, genuinely large image-processing feature, still an open follow-up decision, not silently dropped or half-built.
- **[Done, 19 of ~50]** A first, fully-verified batch of the `Widgets/` library: `NText`, `NIcon`, `NBox`, `NDivider`, `NHeader`, `NLinearGauge`, `NSettingsIndicator`, `NLabel`, `NButton`, `NIconButton`, `NCheckbox`, `NBusyIndicator`, `NIconButtonHot`, `NTabBar`, `NTabButton`, `NTabView`, `NCollapsible`, `NRadioButton`, `NScrollText`. Each stripped of `import qs.*` module-namespace imports (Zaris's shell dir is one flat namespace), `Settings.data.*`/`I18n.tr()`/`Logger.*`/`TooltipService.*` calls, and rewired to `Colors.qml`/`Style.qml`. `NIcon` takes a raw glyph codepoint directly rather than a semantic icon name, matching every other icon usage already in this codebase (see the Icons.qml flag below). Verified in a Xephyr sandbox, all 19 instantiated together with zero QML errors/warnings - both by the background pass that built them and independently re-verified afterward. One of the two real gaps this introduced - stripping `TooltipService` meant `NButton`/`NIconButton`/`NTabButton`/`NSettingsIndicator` lost their hover-tooltip text entirely - is now fixed (see the dedicated `TooltipService`/`Tooltip` entry below). Two widgets *were* judgment-skipped: `NImageRounded` (needs a pre-compiled `.qsb` shader binary + `QtQuick.Effects` pipeline, real Qt-version/RHI-backend risk, and no current Zaris use case for it - no avatars/album art anywhere yet) and `NPluginSettingsPopup` (literally Noctalia's own plugin system, no Zaris equivalent, same exclusion reasoning as `Services/Noctalia/*` below).
- **[Done, 25 of ~50]** A second batch of the `Widgets/` library, picked for being the generic form/layout controls any future settings-GUI work (Phase 2) will need most: `NTextInput` (labeled text field with a clear button and focus-ring border), `NComboBox` (labeled dropdown), `NSlider` (custom-drawn pill-track slider with a circular knob cutout, built on `QtQuick.Shapes` - confirmed installed on this system before porting), `NSpinBox` (numeric stepper with press-and-hold repeat/ramp-up and a directly-editable value field), `NContextMenu` (popup-based right-click-style menu - a real, currently-missing capability; Dock.qml's own "Pin to Dock" flow hand-rolls something similar today and could migrate to this later, not done as part of this port), and `NScrollView` (`ScrollView` with custom-styled scrollbars, edge-fade gradient masks, and smooth animated mouse-wheel scrolling - a real upgrade over the plain default-styled `Flickable` `Settings.qml`'s content pane currently uses, not yet migrated to it). Same stripping pattern as the first batch, plus two new patterns specific to this batch: `NComboBox`'s and `NContextMenu`'s popup list content was rebuilt on a plain `ListView` rather than introducing a dependency on the still-unported `NListView.qml`, since neither needs its scroll-gradient-mask styling; and `NScrollView`'s three `Settings.data.*` per-user toggles (always-visible scrollbar, smooth-scroll enabled, animations disabled) were each fixed to their more polished default rather than left configurable, the same "no per-user settings system exists here" simplification `Style.qml`/`NText.qml` already established.

  Verified together in a throwaway Xephyr smoke-test window (deleted before committing, same discipline as every other unverified-by-click-sim feature in this arc): all five input-style widgets rendered correctly with real interactive state (a slider at 40%, a combo box correctly resolving `currentKey: "b"` to its display name "Option B", a spin box showing "3 px"), and a `Component.onCompleted` debug trigger confirmed `NContextMenu.openAt()` actually opens and renders its popup list correctly (no click-simulation tool available). The remaining widgets (`NFilePicker`, `NKeybindRecorder`, `NColorPickerDialog`, `NBattery`, `NClock`, `NGraph`, `AudioSpectrum`, etc.) are a scope stop for this pass, not a judgment skip - still genuinely portable (none showed any Wayland/compositor coupling on inspection), just lower-value for what Zaris needs right now (specialized widgets like battery/clock/graph displays, or ones needing a GUI feature - keybind recording, color pickers - that doesn't exist yet) and left for a future pass.
- **[Done, 27 of ~50]** A third widget batch: `NListView` and `NGridView` - the same custom-styled-scrollbar/edge-fade-gradient-mask/smooth-wheel-scroll wrapper pattern as `NScrollView` above, but wrapping a `ListView`/`GridView` directly and forwarding most of their own properties/methods via aliases, so either is a drop-in replacement wherever a plain `ListView`/`GridView` would otherwise go. Picked next specifically because `NComboBox` and `NContextMenu` (previous batch) had both stood in with a plain `ListView` for their popup content rather than wait on this - now that both real widgets exist, migrating those two call sites to get the scrollbar/gradient polish is a real, low-risk follow-up (not done as part of this port), and `WallpaperPickerPanel.qml`'s existing plain `GridView` thumbnail grid is a similar future migration candidate. Same three per-user settings toggles fixed to their more polished default as `NScrollView`, for the same reason.

  Also newly unblocks a genuinely large, high-value widget for a future pass: `NFilePicker.qml` (818 lines - a full grid/list file browser with thumbnails, search, and multi-selection, built on the standard `Qt.labs.folderlistmodel` module with zero Wayland coupling) depends directly on both `NGridView` and `NListView` for its two view modes - inspected but not ported in this pass, since it also needs its ~15 semantic Noctalia icon names (`filepicker-folder`, `filepicker-photo`, etc.) translated to Zaris's raw-glyph convention throughout, and has no current consumer forcing it yet (`WallpaperPickerPanel.qml`'s directory field is a plain text input today, works fine as-is) - a real candidate for the next widget pass, not a skip.

  Verified together in a throwaway Xephyr smoke-test window (deleted before committing): both instantiated side-by-side with 30-item models, confirmed real scrolling overflow renders correctly (a partially-visible last row/item, not a truncated or broken layout) for both the list and the 2-column grid, with the pill-styled grid delegates and their numbered labels all rendering correctly.

  **Remaining `Widgets/` inventory** (~4,300 lines across 19 files, checked one more time for Wayland/compositor coupling before writing this off - none found in any of them): every one of these is confirmed genuinely portable but is being left unported deliberately, not for lack of a look, because none has an actual consumer in Zaris yet and most depend on a Phase-2/3 feature that doesn't exist either:
  - Color-theming building blocks - `NColorPickerDialog` (775 lines, the big one), `NColorPicker` (76), `NColorChoice` (89), `NColorSlider` (238) - all exist to build a per-user color-theming settings screen, which needs the same not-yet-made dynamic-color/light-palette decisions as `DarkModeService` above.
  - Settings-panel construction kit - `NSectionEditor` (958 lines, the single largest remaining file), `NReorderCheckboxes` (310), `NSearchableComboBox` (436, an `NComboBox` with a filter box added), `NTagFilter` (48), `NSlideSwapView` (135), `NDateTimeTokens` (393, a date/time format-string builder UI) - generic scaffolding for a much larger settings GUI than `Settings.qml` currently is; genuinely useful once Phase 2 gets to rebuilding it, premature before then.
  - Needs a GUI feature that doesn't exist - `NKeybindRecorder` (249, records a keypress combo live - no keybind-editing screen exists; `zaris.conf` keybinds are hand-edit-only) and `NIconPicker` (170, an icon-selection grid - blocked on the still-undecided `Icons.qml`/semantic-icon-name question flagged earlier in this list).
  - Decorative alternates to something already covered - `NBattery` (205, a circular/graphical battery gauge - `BatteryIndicator.qml` already covers this with a glyph+percentage) and `NClock` (analog/digital/binary clock widget - the bar already has its own text clock; also pulls in a `ColorsConvert.js` contrast-ratio helper not yet ported).
  - Needs data Zaris doesn't collect - `NGraph` (234, a line/area history graph) and `NCircleStat` (164, a circular ring gauge) both expect a time-series data source, which only `SystemStatService.qml` (already flagged likely-redundant with `CpuLoad.qml`/`HwmonSensor.qml`) would have fed them.
  - Small `NTextInput` companions with no call site yet - `NInputAction` (52) and `NTextInputButton` (57).
  - `AudioSpectrum` (a `Widgets/` subfolder, 49 lines) - a Pipewire audio-visualizer bar, no current consumer.
  - `NDropShadow` (32 lines) - same real technical risk `NImageRounded` was judgment-skipped for back in the first widget batch (a `QtQuick.Effects`/`MultiEffect` GPU shader pipeline, real Qt-version/RHI-backend risk) plus no current consumer (nothing in this shell uses drop shadows yet) - skipped for the same reason, not re-litigated.
  - `NPopupContextMenu` (372 lines) - `NContextMenu`'s own doc comment names this as the better fit "for bar widgets and top-level window contexts" (vs. `NContextMenu`'s "inside panels and dialogs") - likely relevant once `Dock.qml`'s hand-rolled "Pin to Dock" right-click menu or a bar-icon right-click gets a real context-menu treatment, but no consumer forces it yet either.

  None of this is a closed door - each is a quick pull from `Widgets/` whenever the feature that actually needs it gets built (a settings-GUI rebuild, a keybind editor, a color-theming screen). Recorded here so that work doesn't start from "is this portable?" again.
- **[Done]** `Services/Media/MediaService.qml` → fulfills the **MPRIS "now playing" widget** backlog item. Ported close to verbatim (not the "build something simpler" recommendation first floated - on actually reading all 356 lines, the coupling to strip turned out to be just two `Settings.data.audio.*` references, replaced with local properties, plus a few debug-only `Logger.d(...)` calls removed; everything else is generic, already-working MPRIS control logic built directly on `Quickshell.Services.Mpris` - confirmed every property/method it uses, e.g. `trackTitle`/`playbackState`/`canGoNext`/`play()`, exists on the real `MprisPlayer` type via its qmltypes before porting). The player-switching heuristic (pairing a generic browser-tab MPRIS identity with its real track metadata by matching titles, so e.g. a YouTube tab playing in Firefox still shows the actual song info) is genuine, working, non-trivial logic worth keeping intact, not overhead to trim. New `MediaWidget.qml` bar module built on top (play/pause icon showing the next action, not current state, plus a truncated "artist - title", left-click play/pause, right-click next track) - hides itself entirely when nothing's playing, following `KernelVersion.qml`'s vanish-rather-than-show-empty convention, which had to be re-derived at both the `Bar.qml` and `Overflow.qml` instantiation sites specifically (setting `visible` externally replaces a component's own internal default binding rather than combining with it - true for every module in this codebase already, but only actually matters here since "nothing is playing" is the common case, unlike e.g. kernel version being empty). Wired into the standard module machinery (`ModulesConfig.moduleIds`, `Bar.qml`, `Overflow.qml`'s labeled tray row, `Settings.qml`'s category table) exactly like every other module. Verified live against a real active MPRIS player (a Chromium web app instance) - the bar correctly showed "⏸ Purrple Cat - Meteorites" with the pause icon correctly indicating active playback, real track metadata pulled from the actual D-Bus session, not a synthetic test.
- **[Done]** `TooltipService.qml` + `Tooltip.qml` - restores the hover-tooltip gap from the widget-library port above. Trimmed from their originals (`Services/UI/TooltipService.qml` + `Modules/Tooltip/Tooltip.qml`, 133 + 701 lines) by dropping grid-mode content (a multi-column table layout inside the tooltip bubble - nothing ported into Zaris needs anything but plain text) and the per-instance font-family override (Zaris's `NText` has no such override either, by the same simplification already made there); kept the genuine "auto" positioning logic intact (tries bottom, then top, then right, then left, whichever fits; falls back to whichever has the most room; clamps to whichever monitor the target is actually on) - real, non-trivial, working logic worth preserving, not overhead to trim, same reasoning as `MediaService`'s player-matching heuristic.

  Architecturally notable: built on Quickshell's own `PopupWindow` type, not a `FloatingWindow` - a genuinely different, self-positioning mechanism (`anchor.item`/`anchor.rect` position it directly relative to an arbitrary target `Item`) that needs none of Zaris's own WM-side windowrule system at all. This is the first thing in the shell built this way. Worth remembering for future popups anchored to a specific existing element rather than an arbitrary screen position - e.g. the still-unbuilt calendar flyout (anchored under the clock) might be a better fit for `PopupWindow` than a new WM-side `topcenter` rule, since the clock is a known QML `Item` the flyout could anchor to directly. Not yet evaluated for anything beyond tooltips.

  With both files in place, tooltip support (a `tooltipText` property plus the hover-triggered `TooltipService.show()`/`hide()` calls) was restored in all four widgets that lost it: `NButton`, `NIconButton`, `NTabButton` (including its 500ms hover-delay timer), `NSettingsIndicator`. None of these four are wired into any real shell UI yet (matches phase 2 not having started), so verification used a throwaway smoke-test window in a Xephyr sandbox: confirmed all four render correctly (including `NTabBar`/`NTabButton`'s segmented styling), and since no click-simulation tool exists in this environment to test real hover, force-triggered `TooltipService.show()` directly to confirm the popup actually renders, positions itself below its target, and styles correctly using the Material color roles - genuine end-to-end confirmation of the mechanism, not just "loads without error."
- **[Done]** Clipboard manager, fulfilling the **clipboard manager** backlog item - but NOT a port of `Services/Keyboard/ClipboardService.qml`, despite fulfilling the same item. That file turned out to be entirely built on Wayland-only external tools (`cliphist`, `wl-copy`/`wl-paste`, `wtype`) - the same category of problem as `NightLightService`'s `wlsunset` dependency, just wider (5 Wayland-specific binaries, not one), and again invisible to a `Quickshell.Wayland`-import grep since the coupling is entirely in shell commands, not QML imports. With no single obvious drop-in replacement the way `redshift` was for `wlsunset`, this was treated as a real architecture decision and put to the user directly rather than guessed at: watch X11's real clipboard-change event via `clipnotify` (blocks until the CLIPBOARD selection's owner changes, relaunched after every capture) instead of polling, read new content via `xclip -o`, and keep history in a Zaris-managed JSON file rather than depending on a cliphist-equivalent history daemon that doesn't exist as a standard X11 tool.

  `ClipboardHistoryService.qml` (the new singleton) keeps Noctalia's genuinely portable content-type detection (color/link/file/code pattern-matching, pure JS with zero platform coupling) but everything else is new. Two real implementation gotchas surfaced and fixed before this ever ran: (1) assumed a `FileView.setText()`/`writeToFile()` API for writing arbitrary text to a temp file - doesn't exist, `FileView` is read-plus-JSON-adapter-only. Fixed by passing clipboard content through a shell-escaped `printf` argument instead (`content.replace(/'/g, "'\\''")`, the exact same escaping Noctalia's own `pasteText()` used for its equivalent problem) - verified directly in a real shell before trusting it in QML. (2) A `Process` whose command doesn't exist on `PATH` never actually fires `exited` at all (confirmed live: running with `clipnotify` genuinely uninstalled produced no retry storm, but also silently never flipped an `exited`-based availability flag, since that handler simply never ran - a real, non-obvious Quickshell `Process` behavior worth remembering for any future "is this external tool available" check). Fixed by checking `command -v clipnotify` upfront instead, mirroring the exact same dependency-check pattern Noctalia's own original file already used for `cliphist`.

  New `ClipboardHistoryPanel.qml` picker UI - not ported from anywhere (Noctalia's own picker wasn't examined), built instead by following `Launcher.qml`'s own established search+list pattern for consistency with the rest of this shell. Left-click copies an entry and closes the panel, right-click deletes just that entry, a "Clear" button wipes everything, and an honest warning banner shows when `clipnotify` isn't installed rather than silently doing nothing. Wired into the standard module machinery (`ModulesConfig.moduleIds`, `Bar.qml`, `Overflow.qml`'s labeled tray row, `Settings.qml`'s category table) plus a new `center`/`float` windowrule for its "Clipboard History" title, exactly like every other panel.

  `clipnotify` is confirmed officially packaged on Arch and Fedora but **not Debian** (checked directly against packages.debian.org/sources.debian.org - a real gap, not a naming difference, same category as the xautolock/betterlockscreen/quickshell/Nerd Font gaps already resolved elsewhere). Since it's a tiny, dependency-light C program (X11 + Xfixes only, nothing else), building it from source on Debian is the likely fix, mirroring the Quickshell-from-source precedent - not done yet, needs its own pass. Verified end-to-end in a Xephyr sandbox with `clipnotify` genuinely not installed (true on the reference machine too): the "not installed" warning correctly renders, and seeded test entries (one per content type) confirmed the list, glyph-per-type detection, and preview formatting all render correctly; the actual copy-back mechanism was independently confirmed by directly reading the (sandboxed) X11 clipboard after a forced `copyToClipboard()` call - the real content came back correctly. What's *not* yet verified live: the actual `clipnotify`-triggered capture path, since the tool isn't installed on the reference machine either - needs a real install + live test opportunistically, same pattern as Bluetooth/redshift needing user-side installation first.
- **[Done]** Wallpaper picker/rotation, fulfilling the **wallpaper picker/rotation** backlog item - but NOT a port of `Services/UI/WallpaperService.qml`, despite fulfilling the same item. That file (1683 lines) turned out to be deeply entangled with Noctalia's own dynamic per-wallpaper color-scheme extraction and light/dark "appearance slot" system - the same dynamic wallpaper-based color generation already deferred as its own open decision (see the Material color-role entry above). Porting it properly would mean building that system first, which is well beyond what the backlog item actually asks for. Scoped down instead to a genuinely new, much smaller implementation: a directory scan (non-recursive, `find -maxdepth 1` over common image extensions) plus `xwallpaper --zoom <path>` to apply, matching the single static `xwallpaper` call already in `zaris.conf`'s wallpaper `exec-once` line rather than anything fancier.

  `WallpaperService.qml` (new singleton) stores its config (directory, current wallpaper, rotation enabled/interval) in a `FileView`-backed `wallpaper.json`, same pattern as every other config-backed service in this shell. One implementation risk caught and fixed before ever running it: the directory scan's `find` command was initially built as a `sh -c` string with the path embedded via `JSON.stringify()` - recognized as not properly shell-safe (JSON's escaping doesn't fully align with shell double-quote safety) and switched to a plain argv array (`["find", root.directory, "-maxdepth", "1", ...]`) instead, which needs no escaping at all since no shell is involved; verified directly via both Bash and Python `subprocess.run()` before trusting it in QML. Rotation is a simple `Timer` that picks a random image (excluding the current one when more than one exists) on the configured interval.

  New `WallpaperPickerPanel.qml` UI - a directory field + Rescan button, a rotation `ToggleSwitch` with its interval shown, and a `GridView` of 120x80 thumbnails (`Image` with `PreserveAspectCrop` fill and `sourceSize` set for decode performance); clicking a thumbnail sets it as the wallpaper and closes the panel, and the currently-active wallpaper gets a highlighted border. Wired into the standard module machinery (`ModulesConfig.moduleIds`, `Bar.qml`, `Overflow.qml`'s labeled tray row, `Settings.qml`'s category table) plus a new `center`/`float` windowrule for its "Wallpaper Picker" title, exactly like every other panel. Verified live in a Xephyr sandbox: the bar icon renders, `qs ipc call wallpaper toggle` opens the panel showing real thumbnails scanned from `~/Pictures` (six real files, including `ArtixLinux.png` and `cosmic.jpg`), the `GridView`'s "fill remaining space below the toggle row" layout renders correctly with no overlap, and the current wallpaper (`cosmic.jpg`, matching `zaris.conf`'s exec-once line) correctly shows its highlighted border. The `xwallpaper --zoom <path>` invocation itself wasn't re-tested standalone since it's the exact same command already running successfully in production via `zaris.conf`'s own exec-once line - independent verification already exists. Deployed to the live `~/.config/quickshell/` config and live `~/.config/zaris/zaris.conf`, diffed against the shipped defaults first per usual (both live copies' pre-existing personal divergences - the custom monitor+wallpaper exec-once line and xautolock+betterlockscreen instead of xss-lock+i3lock - confirmed untouched by the diff).
- **[Done]** Calendar flyout on clock click, fulfilling the **calendar flyout on clock click** backlog item - but NOT a port of `Services/Location/CalendarService.qml` + `Calendar/*` + `Modules/Cards/{CalendarHeaderCard,CalendarMonthCard}.qml`, despite fulfilling the same item. `CalendarService.qml` is built entirely around real calendar-event backends (`Calendar/Khal.qml` and `Calendar/EvolutionDataServer.qml` - shelling out to `khal` or talking to GNOME's Evolution Data Server over D-Bus) - genuinely portable to X11 in principle (neither is Wayland-specific), but adopting either is a real, undecided architecture choice (which backend(s) to support, if any, and what happens with neither installed) in the same category as the clipboard-mechanism decision, not something to guess at silently. `CalendarHeaderCard.qml` was separately ruled out on inspection - it's entangled with Noctalia's weather/location system and bundles its own analog/digital clock widget, neither of which exists in Zaris (the bar already has its own clock).

  What's left, and what actually got ported, is `CalendarMonthCard.qml`'s month-grid generation and navigation logic - pure JS date math (previous/next-month padding days, ISO-style day-of-week offsets, which cell is "today") with zero external coupling, ported close to verbatim into a new `CalendarFlyout.qml`. Deliberately scoped down to a plain date-browsing calendar with no event integration (no per-day dots, no hover tooltips listing events, no click-to-open-`gnome-calendar`) since there's no event data source to show yet - the same "build what the backlog item actually asks for, not the full original feature set" judgment call as the Wallpaper picker above. Event-backend integration (`khal`/EDS) is left as a real follow-up decision, not silently dropped.

  Architecturally, this is the second thing in the shell built on `PopupWindow` (after `Tooltip.qml`) rather than needing a new WM-side `topcenter` C++ rule - anchored directly to the bar's own clock `Text` item via `anchor.item`. Since `Bar.qml` has one `PanelWindow` per monitor (via `Variants`) and `PopupWindow`s can't just be nested inside another window's visual tree the way `Dock.qml` nests two window variants inside a shared `Item` wrapper, this uses a single shared flyout instance instead, retargeted to whichever monitor's clock was actually clicked - a new `CalendarFlyoutState.qml` singleton holds `visible`/`targetItem`, the same "one instance, retarget on interaction" approach `Tooltip`/`TooltipService` already use for hover targets, just persistent instead of transient. Clicking a clock either opens the flyout anchored to that clock (retargeting away from any other monitor's clock first) or toggles it closed if that same clock's flyout is already open.

  One real QML gotcha hit and fixed before this ever ran live: the "Today" link at the bottom was originally wrapped in a `Row` with `anchors.horizontalCenter` on its child - `Row` positions children itself and explicitly forbids anchoring them (a real runtime warning, `Row will not function`, not a silent no-op), fixed by swapping that single-child wrapper to a plain `Item`. Verified end-to-end in a Xephyr sandbox via a throwaway `Component.onCompleted` debug trigger (no click-simulation tool available, same testing approach as every other feature in this arc): the flyout renders correctly positioned and centered below the clock, the current month grid and today's highlighted date were both correct for the real system date, and a second throwaway debug `Timer` confirmed `goToNextMonth()` correctly advances the grid (verified October 2026's leading/trailing padding days and day-of-week alignment) before both debug additions were removed. Deployed to the live `~/.config/quickshell/` config; no `zaris.conf` windowrule needed at all, since `PopupWindow` doesn't go through Zaris's WM-side windowrule system.
- **[Done]** Scheduled day/night service, extending (not replacing) the existing manual `NightLight.qml` on/off toggle, per the backlog's own "a possible upgrade of the existing manual toggle into a real scheduled day/night service (extend, don't break, the current toggle)" note. NOT a port of `Services/Location/NightLightService.qml`, despite the same purpose - that file is built entirely around spawning and supervising a long-running `wlsunset` daemon process, including real crash-restart bookkeeping - `wlsunset` is Wayland-only, the same category of gap as `ClipboardService`'s `cliphist`/`wl-copy`/`wtype` dependency (invisible to a `Quickshell.Wayland`-import grep since the coupling is purely in shell commands). `redshift` (already used by the existing manual toggle) has no equivalent to `wlsunset`'s manual sunrise/sunset-time flags for a long-running daemon - its own continuous mode is built around real solar-position calculation from a lat/long (`-l LAT:LON` or `-l geoclue2`), which would mean building a `LocationService` first, the same dependency `DarkModeService` was already deferred over.

  So rather than a continuous daemon, only the genuinely portable piece of Noctalia's file was ported - the manual-schedule time math (`timeToMinutes`, `isCurrentlyNight`, `msUntilNextBoundary`, kept close to verbatim, pure JS with zero external coupling) - into a new `NightLightService.qml` singleton that drives the existing one-shot `redshift -O <temp>` / `redshift -x` calls from a `Timer` that re-fires exactly at the next sunset/sunrise boundary, rather than keeping any process running continuously in between. `NightLight.qml`'s hardcoded `nightTemp: 4500` was moved into this new singleton's config (both the manual click and the schedule now read the same `NightLightService.nightTemp`) so the two can never drift apart.

  Scheduling is opt-in and hand-edit-only for now (`scheduleEnabled`/`sunset`/`sunrise`/`nightTemp` in a new `nightlight.json`, defaulting to `scheduleEnabled: false` so this is a complete no-op until a user opts in) - no `Settings.qml` GUI yet, the same precedent as `modules.json`'s per-module `"screens"` pinning already being hand-edit-only. Verified in a Xephyr sandbox via a throwaway debug `Text` binding (no click-simulation tool available): with the schedule enabled and `sunset`/`sunrise` set to bracket the real test time, the computed night/day state matched by hand-calculation, and - importantly - editing the config file *while the shell kept running* correctly flipped the live state within ~2 seconds with no restart needed, confirming `FileView`'s `onFileChanged`/`onLoaded` hooks correctly cooperate to reapply the schedule on a hot-edit, not just at startup. One easy mistake caught mid-testing: an early debug run appeared to show a stuck `active=false` after a config edit - not a real bug, just a stale Xephyr/`qs` test process the harness's `pkill` had silently failed to actually kill (a recurring quirk in this sandbox unrelated to the feature itself), confirmed once a genuinely fresh process was launched. The test `nightlight.json` (temporarily written with `scheduleEnabled: true` to drive the above verification) was deleted before deploying, same "don't leave test config in the real config directory" discipline as the Clipboard manager's seeded-entries mistake. Deployed to the live `~/.config/quickshell/` config; confirmed the live reload was a complete no-op (no `nightlight.json` gets written, no `redshift` call happens) since `scheduleEnabled` still defaults to `false`.
- **[Done]** `HostService.qml` - host/user identity info (distro pretty-name + logo, resolved display name via GECOS real name falling back to `$USER`, and hostname), adapted from `Services/System/HostService.qml`. No current use site yet (no About panel or user-facing header exists to show any of it) - ported as a foundation piece the same way `MediaService`/`WallpaperService` were, on the strength of it being genuinely useful, zero Wayland/compositor coupling, and not redundant with anything Zaris's own bar modules already read (those cover CPU/kernel/network, not host/user identity). Dropped the Noctalia-specific `NOCTALIA_REALNAME` env-var override (no Zaris equivalent) and `Logger.*` calls. Verified live in a Xephyr sandbox via a throwaway debug `Text` binding showing all of its properties at once - resolved real values from this actual machine: `osPretty="Artix Linux"`, `osLogo="file:///usr/share/pixmaps/artixlinux-logo.svg"` (confirming the pixmaps-fallback probe path actually works, not just the icon-theme lookup), `username="mike"`, `hostName="artix"`, `displayName="mike"` (correctly fell back from an empty GECOS `realName` to `$USER`) - genuine functional verification against real system data, not just "loads without error."
  - `Services/Location/DarkModeService.qml` → re-inspected while working through this list: its actual scheduling logic (manual sunset/sunrise or weather-API-derived, pick the current light/dark state, schedule the next flip) is the same shape as `NightLightService.qml` above and just as portable in isolation. What blocks this one is a different kind of gap than "install a missing package" (the category `BrightnessService` below was built ahead of) - it toggles `Settings.data.colorSchemes.darkMode`, a flag with nothing to actually change yet, since Zaris's `Colors.qml` is still a single fixed dark palette with no parallel light variant defined. Porting the schedule logic now would just flip a boolean nothing reads. This needs an actual light-palette design decision made first (a real Phase-2/3 color-system undertaking, same category as the still-deferred dynamic wallpaper-based color generation), not a dependency to install - still deferred.
- **[Done]** `BatteryService.qml` + `BatteryIndicator.qml`, fulfilling the **battery bar module** half of the `Services/Hardware/{BatteryService,BrightnessService}.qml` backlog item. Built on Quickshell's own `Quickshell.Services.UPower` module - a real D-Bus/UPower integration with zero Wayland or compositor coupling (works identically on X11), so unlike most of this port arc this was a straight architectural port, not a "build something simpler" rescope. Requires the `upower` daemon running - already installed and active on the reference machine (confirmed via `upower -e` and `busctl list` showing a live `org.freedesktop.UPower` service), **but flagging for other installs**: `upower` itself is the one real dependency here, and needs to be running (most desktop environments/DEs pull it in already, but a minimal X11-only install might not) - added to `DEPENDENCIES.md`.

  Scoped down from the original in one way: dropped Bluetooth-peripheral-battery merging (Zaris's own Bluetooth module is built directly on `Quickshell.Bluetooth` rather than Noctalia's wrapper shape, and whether its device type exposes comparable battery properties hasn't been checked - a real follow-up, not a silent drop). Broadened in one respect: `primaryDevice` falls back to *any* present non-`LinePower` UPower device, not just ones flagged `isLaptopBattery` - this machine has no laptop battery, but does have a real device UPower already tracks, and restricting to laptop-only would have left this permanently untestable and hidden here.

  That real device led to a genuine, non-obvious fix: a wireless mouse's battery, reported by the kernel's HID++ driver (`nativePath: hidpp_battery_0`, confirmed via `upower -e`), turned out to have **`type: Mouse` (enum value 5), not `type: Battery`** - an initial filter requiring `type === UPowerDeviceType.Battery` silently excluded it entirely (debug-verified: `all=1` devices visible but `oth=0` matched). Fixed by matching Noctalia's own actual `deviceModel` filter (anything except `LinePower`, not specifically `Battery`) instead of the narrower assumption. `BatteryIndicator.qml` (new bar module, real Nerd Font battery-tier glyphs rather than Noctalia's semantic icon-name strings, hides entirely when no battery device is present) verified end-to-end live in Xephyr against this real device - resolved `present=true pct=28 name="Logitech PRO X" time="Idle"`, with the bar rendering the correct quarter-charge glyph next to "28%" - then confirmed showing correctly on the actual live desktop bar too after deploying. Wired into the standard module machinery (`ModulesConfig.moduleIds`, `Bar.qml`, `Overflow.qml`'s labeled tray row, `Settings.qml`'s category table).

  The original file's low/critical-battery notifications (`ToastService.showNotice(...)`) were kept, functionally, but rebuilt on `notify-send` rather than porting `ToastService.qml` - see that decision written up in its own entry below. One real hot-reload bug caught and fixed before it ever worked: the new `_hasNotified`/`checkBatteryNotification()` logic was originally wired to re-check only on `battery.json`'s individual `warningThresholdChanged`/`criticalThresholdChanged` signals, which never actually fired the check in practice (unconfirmed why - possibly a `JsonAdapter` reload-identity subtlety) - a hand-edited threshold change silently did nothing until the next real percentage/state change. Fixed by switching to the same proven-reliable `FileView.onLoaded` hook `NightLightService.qml` already used for its own hot-reload. Verified genuinely end-to-end on the **real live desktop** (not a Xephyr sandbox, since this needed the actual `dunst` daemon and the actual 28%-charged mouse): temporarily lowered `battery.json`'s `warningThreshold` to 90 while the live shell kept running, and a real desktop notification correctly appeared reading "Logitech PRO X low / 28% remaining" - genuine proof the whole chain (hot-reload -> threshold check -> `notify-send` -> `dunst` render) works, not just that it should in theory.
- **[Done, dependencies noted]** `BrightnessService.qml` (554 lines - three-way DDC/internal-backlight/Apple-Display branching, built on `ddcutil`/`brightnessctl`/`asdbctl`, zero Wayland coupling) - ported in full despite **neither `ddcutil` nor `brightnessctl` being installed on the reference machine, and it having no `/sys/class/backlight` device either** (per explicit user direction to keep going through the ingredient list and note whatever needs installing later, rather than waiting for the hardware/tools to be available first). Stripped of `Settings.data.brightness.*` (now a hand-edit-only `brightness.json`: `enableDdcSupport`, `enforceMinimum`, `brightnessStep`, `backlightDeviceMappings` - same precedent as `nightlight.json`/`battery.json`) and `Logger.*` calls.

  Wired into the existing (previously unused) OSD brightness hook: `OSD.qml` already had `qs ipc call osd brightness <percent>` support from when the OSD popup itself was first built, anticipating this. Added a second `IpcHandler` (`target: "brightness"`) with `increase()`/`decrease()` functions that call into `BrightnessService` and then show the OSD with the primary monitor's resulting level - and added commented-out example keybinds to `zaris.conf` (`XF86MonBrightnessUp`/`Down` -> `qs ipc call brightness increase`/`decrease`) for whoever has the hardware to uncomment, mirroring the existing informative comment already there about why brightness keybinds aren't on by default. One real subtlety caught while wiring this: `increase()`/`decrease()` only *queue* the new brightness value (applied after a short debounce timer), they don't update the monitor's `brightness` property synchronously - reading it immediately for the OSD would show the stale pre-change level, fixed by reading `queuedBrightness` first when present.

  **Verified everything that this hardware allows**: loads with zero errors, and every degradation path was confirmed live via a throwaway debug binding rather than assumed - `ddcutil detect` and the `/sys/class/backlight` scan both correctly report nothing found (`ddc=0`, `bl=0`), Apple Display detection correctly reports absent (`ap=false`), and `brightnessControlAvailable` correctly computes `false` for the one (non-functional) monitor - all without any error or crash, confirming the graceful-degradation design genuinely works rather than just looking like it should on paper. Went further than a load-check: called the new `brightness increase`/`decrease` IPC targets directly and confirmed the *entire* chain up to the final hardware command works correctly - the debounce timer fires, `showPrimaryBrightness()` reads the queued value, and the real OSD popup renders with the correct icon and percentage ("⚙ 5%" after one increase from a resting 0%). The only genuinely unverifiable piece is the final `ddcutil`/`brightnessctl`/`asdbctl` command itself, since none of those three tools exist here.

  **Update - `ddcutil`/`brightnessctl` installed, real DDC control tested, real bug found and fixed.** Once the user installed both, `ddcutil detect` found three genuine DDC/CI-capable monitors (an Acer XZ272 and two MSI displays) - but the actual `brightness decrease` IPC call was still a silent no-op against real hardware. Root-caused live: `Monitor.isDdc`/`busNum` matched `ddcutil`'s own reported DRM connector name (`DP-1`, `DP-2`, `DP-3`) directly against `modelData.name`, Quickshell's own X11/XRandR output name (`DisplayPort-0`, `DisplayPort-1`, `DisplayPort-2`) - confirmed via `xrandr --query` side-by-side with `ddcutil detect`'s output. These are two genuinely different naming schemes for the same physical ports; the original Noctalia code's plain equality check assumed they'd match, which holds on the Wayland/DRM-native compositors it was written for (where the compositor's own screen name typically *is* the DRM connector name) but never holds on X11.

  Fixed with a new `connectorMatches(xrandrName, drmConnector)` helper on `BrightnessService`, using a name+offset heuristic the user chose explicitly over two alternatives (a more robust but unimplemented EDID-based match, or leaving it broken and documented) after being asked directly: strip `"DisplayPort"` to `"DP"`, then try a `+1` index offset (XRandR's `DisplayPort-0`/`HDMI-A-0` are 0-indexed where DRM's `DP-1`/`HDMI-A-1` are 1-indexed, confirmed consistent across all four connectors on this machine). Documented in the function's own comment as a heuristic, not a guaranteed-universal rule - if DDC control doesn't activate on different hardware, this offset assumption is the first thing to re-check.

  Verified genuinely end-to-end against real hardware after the fix: a debug binding confirmed all three monitors now correctly resolve `isDdc: true` with their correct `busNum`s (matching `ddcutil detect`'s own bus assignments) and correct real current-brightness readouts (100%, 90%, 100% - accurately read from each monitor's actual firmware state via `initBrightness()`, not synthetic data). Sent a real `brightness decrease` IPC call and confirmed via independent `ddcutil getvcp 10` queries that actual monitor brightness actually changed on real hardware. All three monitors were restored to their original pre-test brightness values (`ddcutil setvcp 10 100`/etc.) immediately after, same "leave live state exactly as found" discipline as every other real-hardware test in this file.
- **[Explicitly skipped, architectural mismatch]** `Services/UI/PanelService.qml` (421 lines) - a central registry/coordinator for Noctalia's own multi-panel system: one-panel-open-at-a-time enforcement, a "SmartPanel" abstraction, an overlay-launcher mode, tray/context-menu popup-window registration per screen, and closing panels on `CompositorService.overviewActive` - a genuinely compositor-specific concept (Hyprland/niri's "overview" workspace-grid mode) with no X11/EWMH equivalent. None of this maps onto how Zaris actually manages panels: each one (`BluetoothPanel`, `ClipboardHistoryPanel`, `WallpaperPickerPanel`, `Settings`, `Overflow`, ...) is its own independent `FloatingWindow` positioned by a WM-side windowrule, paired with its own small `*PanelState.qml` singleton holding just a `visible` flag - a simpler pattern that's been proven out consistently across seven panels now. Porting `PanelService.qml` would mean replacing that whole proven pattern with Noctalia's much heavier centralized one for no functional gain - this is an architectural mismatch to route around, not a portability or missing-dependency gap.
- **[Explicitly skipped, redundant]** `Services/UI/ToastService.qml` - unlike everything above, this isn't a Wayland/dependency/consumer gap, it's genuinely duplicate infrastructure. `ToastService.qml` itself is a tiny (32-line) signal dispatcher, but its actual visual popup lives in `Modules/Toast/{Toast,ToastOverlay,ToastScreen}.qml` (683 lines) - and two of those three files are themselves built on `Quickshell.Wayland` layer-shell positioning, a real coupling gap on top of everything else. More fundamentally: Zaris already runs `dunst` as a real notification daemon and already calls `notify-send` for exactly this kind of transient message (`screenshot.sh`'s "screenshot taken" notice, and now `BatteryService.qml`'s low/critical-battery notices too - see above) - building a parallel in-shell toast-rendering system on top of an already-working, already-themed (`dunst`'s config matches `Colors.qml`) system notification daemon would just be duplicate infrastructure, not a missing capability. Any future service that wants to notify the user should call `notify-send` directly, the same way `BatteryService.qml` now does, rather than waiting on this.

  **Dependencies needed to actually control real hardware brightness** (added to `DEPENDENCIES.md`'s new "Optional dependencies" section, and to `zaris.conf`'s commented-out keybind block): `ddcutil` for external DDC/CI-capable monitors, `brightnessctl` for an internal laptop panel - install whichever matches your hardware, then uncomment the keybinds in `zaris.conf`.
  - `Services/System/{FontService,SystemStatService}.qml` → general utility services, ported only where not redundant with what Zaris's own bar modules already read directly (`/proc`, hwmon). `SystemStatService.qml` (1506 lines - full CPU/RAM/disk/network stats with history for graphing) is almost certainly a skip on inspection - Zaris's own `CpuLoad.qml`/`HwmonSensor.qml` already read `/proc`/hwmon directly for the same information, just without the graphing history; `FontService.qml` (wraps `fc-list`) has no current use site (no font-picker UI exists)
  - `Services/System/SoundService.qml` (205 lines, generic sound-effect player via a dynamically-created `QtMultimedia` `MediaPlayer`) - inspected and confirmed fully portable (zero Wayland coupling, gracefully handles `QtMultimedia` being unavailable) and `QtMultimedia` itself is confirmed installed on this system (`qt6-multimedia 6.11.2`), but deliberately not ported: it has no current consumer at all (no notification-sound feature exists anywhere in Zaris, no `Assets/Sounds/` directory to resolve paths against), so porting it now would just be orphaned infrastructure with nothing to verify against - same reasoning `NImageRounded` was skipped for, minus the technical risk. Revisit once a feature (an OSD chime, a notification sound) actually needs it.
  
  Explicitly still excluded, confirmed with the user (a "this isn't a Zaris concept" exclusion, not a technical one): `Services/Noctalia/*` (their own plugin marketplace, telemetry, auto-updater, supporter perks) and `Services/UI/{BarWidgetRegistry,ControlCenterWidgetRegistry,DesktopWidgetRegistry,LauncherProviderRegistry}.qml` (their drag-and-drop bar-customization/plugin-registration system - a whole separate plugin-architecture undertaking, flagged as a possible future item rather than attempted here).
- **[Flagged, not started]** Whether to adopt `Commons/Icons.qml`/`IconsTabler.qml` (their icon-name-to-glyph mapping, likely backed by the bundled Tabler icon set) instead of Zaris's current raw Nerd Font codepoints-in-QML approach - a shell-wide icon-system change, deliberately left as a coordinator/user decision rather than something a background pass should decide alone.
- **[Explicitly out of scope]** Their settings-migration system (`Commons/Migrations/`, 20+ version-specific files - irrelevant, Zaris has no legacy users to migrate) and their I18n system (`Commons/I18n.qml` - Zaris is English-only, translation strings get flattened to plain literals wherever ported code used them).
- **[Explicitly out of scope, technically impossible]** Anything genuinely Wayland-protocol- or compositor-IPC-specific (`Quickshell.Wayland`, `WlrLayershell`, `Quickshell.Hyprland`, `Quickshell.I3`) - Zaris is X11-only, these have no analog to port to, confirmed acceptable to drop.

## Phase 2 — rebuilding existing screens

Confirmed with the user directly: Phase 1 is substantially complete, so this phase has started. Each entry below is an existing Zaris screen rebuilt to actually use the Phase-1 ingredient set (widgets, color roles, design tokens) in place of the original hand-rolled `Rectangle`/`Text` pattern - not new functionality, a consistency/polish pass over something that already worked.

- **[Done]** `Settings.qml` - the first screen rebuilt. Three concrete swaps: the plain `Flickable` is now `NScrollView` (real scrollbar styling, edge-fade gradient masks, smooth wheel-scroll - the exact upgrade flagged as pending back when `NScrollView` was first ported); every raw `Text` element is now `NText` (consistent typography off `Style.qml`'s tokens instead of ad-hoc `font.pixelSize` numbers scattered through the file); and the two "pill pair" selectors (module screens: All/Primary, dock mode: Reserved/Floating), each previously a hand-rolled pair of `Rectangle`+`Text`+`MouseArea`, are now `NTabBar`+`NTabButton` - a genuine behavioral upgrade too (real hover states, tooltip support, shared segmented-control styling), not just a re-skin. The sidebar's vertical category list was deliberately left as its own pattern rather than forced into `NTabBar` (a horizontal segmented control, wrong shape for a vertical nav list).

  One real regression caught and fixed before deploying: the full-file rewrite initially dropped the sidebar's two category icons (a hamburger/bars glyph for "Bar", a grid glyph for "Dock") - they existed as real Nerd Font codepoints in the live file already, invisible in this environment's own tooling the same way ported glyphs have been throughout this whole arc (see the Nerd-Font-glyph-insertion note earlier in this file), and got silently replaced with empty strings when the file was retyped from scratch rather than edited in place. Caught via `diff` against the live deployed copy before shipping (the exact discipline this project already follows for every deploy) - re-inserted both via the same explicit-codepoint-escape method used throughout (`0xf0c9` bars, `0xf009` grid), verified via a Python re-read of the actual bytes, then re-confirmed visually in a fresh Xephyr render.
- **[Done]** `PinDialog.qml` - the two hand-rolled "button" `Rectangle`+`Text`+`MouseArea` groups (Pin/Remove-from-Dock, Cancel) are now `NButton` - real hover states and shared styling with every other ported button in the shell, not just a re-skin. Left the outer dialog chrome (bordered `Rectangle`) and the `IconImage` app-icon row as-is - no ported widget maps onto either more usefully than what's already there. Verified in a Xephyr sandbox via a throwaway `Component.onCompleted` debug trigger (`PinDialogState.open("test", "Test App", "")`, since there's no click-sim tool to right-click a dock icon normally): both buttons render correctly with real `NButton` styling (filled pill for the primary action, outlined for Cancel).
- **[Done]** `BluetoothPanel.qml` - the richest Phase 2 target so far: the plain `Flickable` is now `NScrollView`, every raw `Text` is now `NText`, and every hand-rolled "button" `Rectangle`+`Text`+`MouseArea` (Scan, Pair, Cancel, Connect, Disconnect, Forget - six of them, once the per-device action row is counted) is now `NButton`, sized down via a smaller `fontSize` to keep the same compact device-row density the originals had rather than adopting `NButton`'s own larger default padding.

  Verified in a Xephyr sandbox via a throwaway `Component.onCompleted` debug trigger against the **real Bluetooth adapter** (there's no synthetic/mock adapter to test against - this talks to actual BlueZ over D-Bus): the visible portion (title, power toggle) rendered correctly with live adapter state. Hit a real puzzle along the way - the window rendered visibly truncated, showing only the first two rows even once a debug binding confirmed `content.implicitHeight` was correctly computing a much larger value (180px, reflecting the full expanded Scan/Devices content). Root-caused by temporarily swapping in the still-live *original* Flickable-based file in the same sandbox for comparison: it truncated identically, proving this is a pre-existing Xephyr-without-a-window-manager limitation (confirmed separately that `wmctrl` can't even query a client list in this sandbox - no WM running to negotiate window geometry at all), not a regression from the `NScrollView` swap. Worth remembering for any future panel whose height is computed from `content.implicitHeight` rather than fixed - this sandbox can validate the *logic* (the binding computes the right value) but not the *visual result* for a panel taller than its initial small state, the same "loads without error, judge the logic not the pixel-perfect render" caveat already documented for `FloatingWindow` positioning.

  One real side effect from testing against real hardware, cleaned up immediately after: a debug trigger toggled the real Bluetooth radio on (via `panel.adapter.enabled = true`) to get past the "no adapter enabled" empty state and confirm the Devices section's logic - confirmed via `busctl get-property org.bluez /org/bluez/hci0 org.bluez.Adapter1 Powered` that it was left in the changed (`true`) state after testing, and explicitly restored to its pre-test value (`false`) via `busctl set-property` before finishing, mirroring the same "diff and restore any live state a test touched" discipline already used for `dock.json`/`battery.json`.
- **[Done]** `ClipboardHistoryPanel.qml` - the "Clear" button is now `NButton`, the plain `ListView` is now `NListView` (real scrollbar styling + edge-fade gradient masks), and every raw `Text` is now `NText`. The search field's bordered-`Rectangle`+`TextInput` was deliberately left as its own custom pattern rather than switched to `NTextInput` - its Up/Down/Return/Escape key handling (driving result-list navigation) needs `Keys.onXxx` attached handlers declared directly inside the `TextField` itself, which isn't something reachable through `NTextInput`'s own `inputItem` alias from outside the component; fighting the ported widget's shape for a feature it doesn't expose wasn't worth it here. Verified in a Xephyr sandbox via a throwaway debug trigger: the search field, `NButton`-based Clear action, the `clipnotify`-not-installed warning, and `NListView`'s empty-state overlay ("No clipboard history yet", correctly centered within the wrapper) all render correctly - real seeded-entry rendering wasn't re-verified here since it was already confirmed working end-to-end when this panel was first built, and `NText`/`NListView` are drop-in stylistic swaps with no change to the underlying data flow.
- **[Done]** `WallpaperPickerPanel.qml` - "Rescan" is now `NButton`, every raw `Text` is now `NText`, and the thumbnail `GridView` is now `NGridView` - the exact migration flagged as a future candidate back when `NGridView` was first ported, now done. The directory field keeps its own bordered-`Rectangle`+`TextInput`, same `Keys.onReturnPressed`-isn't-reachable-through-`NTextInput` reasoning as `ClipboardHistoryPanel.qml`'s search field. Verified in a Xephyr sandbox via a throwaway debug trigger against the real wallpaper directory: all six real thumbnails from `~/Pictures` rendered correctly through `NGridView`, with the current wallpaper's highlighted border still correctly identifying the active image - confirming the `NGridView` swap changed nothing about the actual thumbnail-rendering or highlight logic, purely a scrollbar/gradient-styling upgrade.
- **[Done]** `Launcher.qml` - the plain `ListView` is now `NListView` and the delegate's raw `Text` is now `NText`, same pattern as every other search+list picker in this shell now. The search field keeps its own bordered-`Rectangle`+`TextInput`, same `Keys.onXxx`-isn't-reachable-through-`NTextInput` reasoning as the others. Verified in a Xephyr sandbox via a throwaway debug trigger against the real installed-app list (`DesktopEntries.applications`, no synthetic data involved): real apps (Alacritty, Brave, Betterbird, etc.) rendered correctly with their real icons via `IconImage`, and the first result's highlighted-current-item styling rendered correctly through `NListView`'s delegate wrapper.
- **[Done]** `Overflow.qml` - a smaller, purely mechanical pass: all 13 row-label `Text` elements (Kernel, CPU, CPU Temp, ..., Battery) are now `NText`, consistent typography off `Style.qml`'s tokens instead of a hardcoded `font.pixelSize: 12` repeated 13 times. The individual module components each row wraps (`KernelVersion`, `CpuLoad`, etc.) are untouched - each is its own file with its own styling, out of scope for this pass. Verified in a Xephyr sandbox via a throwaway debug trigger (`OverflowState.visible = true`) against real tray-enabled modules from the live `modules.json` - loaded with zero errors; the visual render itself was inconclusive due to the flyout overlapping the bar in this no-WM sandbox (the same known positioning limitation documented elsewhere in this file), but given this is a purely mechanical text-styling swap using the exact same `NText` component already proven correct across five other screens this session, a clean load with no errors was judged sufficient here rather than fighting the sandbox for a pixel-perfect screenshot.
- **[Done]** `OSD.qml` - the icon glyph is now `NIcon` and the percentage/muted label is now `NText`, matching the icon-vs-text convention every ported widget already uses internally (e.g. `NButton`'s own icon+text pair). Verified in a Xephyr sandbox via the panel's own real IPC surface (`qs ipc call osd volume 65 false` - no synthetic debug trigger needed, this file already exposes exactly the right entry point): the volume icon, "65%" text, and teal progress bar all rendered correctly. A smaller Xephyr window first produced the same inconclusive no-WM overlap seen with `Overflow.qml`; widening it resolved that and gave a clean, unambiguous render.
- **[Done]** A batch pass over every remaining small bar-icon module - `BatteryIndicator`, `BluetoothIndicator`, `ClipboardIndicator`, `CpuLoad`, `HwmonSensor`, `MediaWidget`, `NetworkStatus`, `NightLight`, `StayAwake`, `VolumeControl`, `WallpaperIndicator`, `Workspaces`, `DockIcons`, and `Bar.qml` itself (14 files) - every one of these is a tiny (1-2 element) icon+label pair that had been hand-rolled with a literal `font.pixelSize: 13` (or 14/16 for a few of `Bar.qml`'s own icons) repeated dozens of times across the shell. Confirmed `Style.fontSizeL` is an exact-pixel match for `13` and `Style.fontSizeXL` an exact match for `16` before starting, so every swap (`Text`/`Text`+icon-glyph -> `NText`/`NIcon`, `font.pixelSize: 13/16` -> `pointSize: Style.fontSizeL/XL`) is a genuine zero-visual-change move onto the token scale, not an approximation - the one exception is `DockIcons.qml`'s single-letter fallback avatar and two of `Bar.qml`'s icons (settings gear, overflow chevron), originally `14px` with no exact token match, snapped to `Style.fontSizeL` (13, a 1px difference judged imperceptible for a single glyph).

  Batched into one pass rather than 14 separate cycles since every file follows the identical mechanical pattern (confirmed by inspecting all of them first) - scripted via Python for the 12 fully-uniform files, hand-edited `DockIcons.qml` and `Bar.qml` individually for their few genuine variations (different font weight/size, real embedded glyph bytes needing byte-exact preservation through Python rather than the Edit tool, which can't reliably match invisible PUA glyph characters in `old_string`). Verified all 14 together in one consolidated Xephyr sandbox test (a single wide window showing the whole bar at once) - zero errors, and the full bar rendered with clock, media title, all indicator icons, battery percentage, and volume all displaying correctly at their original sizes. Also confirmed on the **real live desktop** after deploying - a direct screenshot comparison confirmed the bar looks pixel-identical to before the swap, exactly as the exact-token-match design intended.

  **Correction**: a follow-up full-directory sweep (grepping every `.qml` file for `^\s*Text {`, not just the ones assumed to need it) found `KernelVersion.qml` had been missed from the original 14-file list entirely - same exact icon+label pattern, same fix applied (`Text` -> `NText`, `font.pixelSize: 13` -> `pointSize: Style.fontSizeL`), verified and deployed the same way. That same sweep also confirmed every other remaining `Rectangle`+`MouseArea` usage across the whole shell (`DockLauncherIcon.qml`'s icon hover-highlight, `SystemTrayRow.qml`'s native tray-icon click handling, plus every remaining `Rectangle` in files already rebuilt this pass) is legitimate background/highlight chrome or native-icon click handling, not a hand-rolled text-button pattern - genuinely nothing else left in Phase 2's "reskin with ported widgets" scope, confirmed by inspection rather than assumed.

  Verified in a Xephyr sandbox via a throwaway `Component.onCompleted` debug trigger (`SettingsState.visible = true`, since there's no click-sim tool to open it normally): both categories render and navigate correctly, the `NTabBar` segmented controls render with correct active-state highlighting and respond correctly when clicked, and the Dock category's conditional "Mode" row (only visible when Dock is enabled) was confirmed by temporarily forcing `DockConfig.setEnabled(true)` via a delayed debug `Timer` (an immediate call raced the config's own async initial load and silently did nothing - a real timing gotcha worth remembering for any future "flip a Settings state on startup for testing" trick) - correctly showed the Reserved/Floating tabs, and as a side effect confirmed the real `Dock.qml` panel itself activated correctly too. The live `dock.json`'s real value (disabled) was restored immediately after, and diffed to confirm before deploying.

## Phase 3 — new features, cross-referenced against Noctalia v5

With Phase 1 (pull portable ingredients) and Phase 2 (rebuild existing screens with them) both done, this phase looks at Noctalia's *current* (v5) feature set for ideas worth building as Zaris-original features — not ports, since v5 turned out to be a complete architectural break from the Quickshell/Qt-based v4.7.7 this whole arc has been drawing from: it's now a family of three projects (Noctalia shell, an Umbriel Wayland compositor, a Greeter login screen), and the shell itself is "built directly on Wayland and OpenGL ES with no Qt or GTK dependency" per its own docs — v4 is explicitly archived/no-longer-maintained. None of v5's actual code is portable to an X11/Quickshell shell; only the *feature ideas* are relevant here, confirmed with the user before starting.

Researched (via the shell's GitHub README, docs.noctalia.dev, and targeted searches) and presented to the user as four candidates: a Do Not Disturb toggle, a Control Center rebuild (tabbed unified panel replacing the flat `Overflow.qml` list), custom script-backed bar widgets, and desktop widgets (floating, wallpaper-anchored Clock/Weather/Media/SystemStats). The user picked Do Not Disturb first.

- **[Done]** Do Not Disturb toggle. A bar icon (bell-slash, nf-fa-bell_slash `0xf1f6`) that toggles `dunst`'s own pause state via `dunstctl set-paused toggle` — same simple on/off pattern as `StayAwake.qml`/`NightLight.qml`, and not a port of anything: dunst already exposes exactly this mechanism itself (`dunstctl set-paused`/`is-paused`, confirmed via `dunstctl --help`), so this is a thin bar-icon wrapper the same way `NightLight.qml` wraps `redshift`.

  New files: `DndState.qml` (shared singleton — dunst's pause state is global to the daemon, not per-monitor, and `Bar.qml` creates one `Dnd` instance per monitor, so without a shared singleton each bar's icon would show its own independent, quickly-inconsistent view of one shared toggle) and `Dnd.qml` (the bar-icon module itself, following `StayAwake.qml`'s exact `Row` of two `NText` + `MouseArea` structure). One deliberate difference from `StayAwakeState.qml`: dunst's pause state can persist across shell restarts or be changed by something other than this toggle (unlike `xset`'s screensaver state, which always starts from a known default), so `DndState` queries `dunstctl is-paused` once at startup via a `Process`/`StdioCollector` so the icon reflects dunst's real state rather than assuming "not paused" — confirmed `dunstctl is-paused` outputs a plain `true`/`false` line.

  Wired into the standard module set: `qmldir` (both new singletons/components registered), `ModulesConfig.qml` (`dnd` added to `moduleIds` and given a default `{ enabled: true, screens: "all", tray: false }` entry), `Bar.qml` (inline instance next to NightLight, `Colors.red` as the active color — the one base color not yet used as a direct bar accent, fitting for an attention/alert-style toggle), `Overflow.qml` (labeled tray row, same `Colors.red` accent), and `Settings.qml` (`dnd: "Do not disturb"` in `moduleNames`).

  Verified in a Xephyr sandbox: configuration loaded with zero errors, the bell-slash glyph rendered correctly in the bar at its expected position. No click-simulation tool is available in this environment (`xdotool`/`xte`/`ydotool` all absent), so the actual click→toggle path was verified by deploying to the **live** desktop instead (diffed against the live config first — all five changed files showed exactly the intended additive changes, no drift) and confirming the icon renders correctly against real `dunstctl` state (not paused, muted-gray icon, matching `dunstctl is-paused` → `false`). The `MouseArea`'s click handler itself (`DndState.paused = !DndState.paused` + `Quickshell.execDetached(["dunstctl", "set-paused", "toggle"])`) is structurally identical to `StayAwake.qml`'s own already-proven-live click handler, just swapping the command — a real click on the live bar to fully close the loop is still worth doing next time the machine is in front of the user, but wasn't blocking given the pattern match and the confirmed-correct reactive state display.
- **[Done]** Control Center — the flat "hidden tray" flyout (`Overflow.qml`/`OverflowState.qml`, title "More") rebuilt and renamed to `ControlCenter.qml`/`ControlCenterState.qml`, title "Control Center", as a tabbed Home/System panel. Not a port of Noctalia v5's own Control Center (its code is Wayland/OpenGL-native, nothing to port) — the same idea rebuilt from Zaris's existing services. Deliberately skipped two pieces of Noctalia's version with no Zaris equivalent: an avatar/large clock (the bar's own clock is already visible behind this panel wherever it opens) and weather+forecast (no weather service exists here). The `"tray": true` opt-in from `modules.json` still gates every module exactly as it did before — this is a presentation change, not a new config surface.

  **Home tab**: a 2-column quick-toggle grid (Stay Awake, Night Light, Do Not Disturb, Bluetooth — each tile just wraps the existing bar-icon component at natural size inside a bordered pill, reusing 100% of their existing click/command logic rather than re-deriving it), a real volume slider (`NSlider` two-way-bound to `Pipewire.defaultAudioSink.audio.volume`, capped at 1.0 matching `osd-volume.sh`'s own `-l 1.0` convention, plus a mute `NIconButton`), a media card (album art, title/artist, prev/play-pause/next `NIconButton`s driving `MediaService` directly — richer than the bar's own compact `MediaWidget`), and a battery row (reuses `BatteryIndicator` as-is). **System tab**: the original flat info-row list unchanged (Kernel, CPU, CPU Temp, GPU Temp, Network, Clipboard, Wallpaper).

  Full rename touched every reference: `qmldir`, `shell.qml`, `Bar.qml` (chevron's click handler + comment), and comment mentions in `BluetoothPanelState.qml`/`ClipboardHistoryPanelState.qml`/`WallpaperPickerPanelState.qml` that listed `OverflowState` as a sibling singleton example. `zaris.conf`'s windowrule updated too (`title:^More$` → `title:^Control Center$`, both the live config and the shipped default), confirmed via `diff` this is the only title string change needed since the `topright`/`float` rule *types* are config-only matches against existing WM-binary functionality, not something requiring a rebuild.

  **A real bug found and fixed along the way, not a sandbox artifact**: the original content-driven sizing (`implicitHeight: content.implicitHeight + 24`, the same pattern `Overflow.qml` always used) looked truncated in Xephyr as expected for this class of panel — but proving it wasn't *just* the well-documented Xephyr-without-a-window-manager limitation took a debug `Timer` logging real values via `qs log` on the **live desktop with a real WM running**: `root.implicitHeight` correctly read `207` from the very first frame, but `root.height` (the actual OS-level window height) stayed stuck at `51` and never grew to match, even though `root.width` tracked `implicitWidth` closely (338 vs 344). Root cause not fully chased down (plausibly `NTabView`'s `contentItems` only populating via `Component.onCompleted` after the window's first paint, or a WM-side interaction with the `topright` rule's own size-dependent position math) — instead sidestepped the same way `Settings.qml` already does for its own differently-sized Bar/Dock categories: switched to a **fixed** `implicitWidth: 344, implicitHeight: 440` sized generously for the taller Home tab, with the shorter System tab just leaving empty space below. Confirmed fixed via the same debug `Timer`: `root.height` immediately read `434` (matching the new fixed value, modulo a consistent ~6px frame adjustment already seen on width) and held stable.

  Full visual confirmation needed working around two more real-environment obstacles, both resolved without touching any user data: no click-simulation tool exists here (`xdotool`/`xte`/`ydotool` absent, same gap hit building the DND toggle), so the panel was opened via a temporary `Component.onCompleted` debug trigger identical to earlier panels' established pattern; and the panel opened directly underneath this very Claude Code terminal window (topright-anchored on the same monitor the terminal tiles into), which stayed stacked on top since the synthetic trigger doesn't go through the WM's normal click-driven focus-raise path — worked around by temporarily hiding the terminal window via `wmctrl -b add,hidden` (a `_NET_WM_STATE_HIDDEN` toggle, not a move — an earlier attempt to *move* the tiled terminal via `wmctrl -e` was correctly ignored by the WM's tiling layout, as expected) for one screenshot, then immediately restoring it via `wmctrl -b remove,hidden`. That screenshot confirmed the fully fixed panel: `NTabBar` renders both tabs with correct active-state styling, the toggle grid correctly wraps to a second row (Bluetooth alone, since `dnd` isn't currently tray-enabled in the live `modules.json`), and the window's border completes cleanly on all four sides with no truncation. All debug scaffolding (the trigger, both debug `Timer`s) removed and diffed to confirm a clean final file before the last live restart.

- **[Done]** Control Center reorg: moved Clipboard, Wallpaper, Battery, Do Not Disturb, Screenshot, the Power menu, and Settings out of the bar and into the Control Center, against a second Noctalia reference screenshot (its actual Control Center/Control Panel this time, not just the bar) the user supplied along with an explicit list of exactly these seven items. Confirmed against the user's own earlier bar screenshot that Noctalia doesn't duplicate these in both places - they're Control-Center-only there - so this is a real move, not an addition.

  Four of the seven (Clipboard, Wallpaper, Battery, Dnd) needed zero new UI code: they already had `ModulesConfig.qml` entries with the existing `tray` opt-in mechanism (the same one Settings.qml's "In tray" column already exposes), so flipping their *default* from `tray: false` to `tray: true` was the entire fix - `Bar.qml`'s existing `visible: ModulesConfig.showInBar(...)` bindings automatically stopped showing them inline, and `ControlCenter.qml`'s existing `showInTray(...)`-gated tiles/rows already picked them up (Dnd/Bluetooth's toggle-grid tile and Battery's info row were already there from the first pass; Clipboard/Wallpaper needed moving from a plain System-tab label+row into a proper Home-tab presence, not code they didn't already have). Checked the live `modules.json` first to confirm it had no explicit overrides for these four keys that would've masked the new code defaults - it didn't, so the change took effect immediately on this machine too, not just for future installs.

  The other three (Screenshot, Power menu, Settings) had no `ModulesConfig` entries at all - pure hardcoded `NIcon`+`MouseArea` blocks living directly in `Bar.qml`, never module-shaped to begin with (no per-user show/hide need for three fixed, always-relevant actions) - so building them a home meant adding a new **fixed header row** to `ControlCenter.qml` (gear/power/close `NIconButton`s, visible regardless of which tab is active, matching the persistent header row in the user's reference screenshot) and removing the three original blocks from `Bar.qml` outright (also dropped `Bar.qml`'s now-unused `import Quickshell.Io`, nothing else in the file still needed it). Screenshot's existing left-click/right-click distinction (quick vs. full/region capture) carried over via `NIconButton`'s already-separate `onClicked`/`onRightClicked` signals - no logic rewrite, just relocated.

  New Home-tab quick-launch row (Clipboard/Wallpaper/Screenshot, icon-only `NIconButton`s with tooltips rather than the labeled-tile pattern the toggle grid uses - closer to the reference screenshot's icon-only convention for one-shot actions) sits between the existing toggle grid and the volume row. Panel's fixed height (see the sizing-bug entry above) bumped from 440 to 530 to fit the two new rows.

  Explicitly deferred rather than folded into this pass, per the user (mid-turn: "we can add in weather and power profiles... whatever you feel is best") - a judgment call to keep this a scoped relocation rather than open two new service integrations (a weather API/location + `powerprofilesctl`) in the same change: both remain real, wanted features for a future pass, noted in `ControlCenter.qml`'s own header comment so they're not forgotten.

  Verified in a Xephyr sandbox via the same `Component.onCompleted` debug-trigger pattern (no click-sim tool, as established): zero errors, and - notably - the panel rendered *completely*, no truncation at all, on the very first try in this normally-unreliable-for-FloatingWindow-sizing sandbox, confirming the fixed-size fix from the first Control Center pass generalizes correctly to more content, not just the original two tabs. Deployed live and confirmed the bar itself: kernel/media/volume/chevron only, dramatically leaner - most other modules (CPU/temps/network/Bluetooth/NightLight/StayAwake) turned out to already be user-configured `tray: true` from an earlier session, so the bar reads close to the Noctalia reference's own minimal look now almost by coincidence. A full live screenshot of the *panel itself* (as opposed to the bar) wasn't obtained this round - the debug trigger opened it directly under this very Claude Code terminal window, which was occupying the entire monitor at the time (not just partially, unlike during the first Control Center pass, so the earlier `wmctrl --hidden` workaround didn't fully clear it either) - judged acceptable given the file is byte-identical to what the sandbox already confirmed rendering perfectly moments earlier, and real-click confirmation from the user was already the plan regardless.

- **[Done]** Control Center third pass: dropped the Home/System tab split entirely - after seeing an actual screenshot of Noctalia's own Control Center (not just its bar), it has no tabs at all, one continuous column. Merged `System`'s remaining content (Kernel, Network - CPU/CPUTemp/GPUTemp/Battery moved to gauges, see below) up into the single main flow as plain rows, and removed `NTabBar`/`NTabView` from `ControlCenter.qml` along with the now-unused `currentTab` property.

  New `NCircularGauge.qml` (registered in `qmldir`) - a small reusable radial-gauge widget (ring + centered value + icon below), matching the small circular stat readouts clustered next to Noctalia's own media card in the reference screenshot. Built on `Canvas` (`context.arc(cx, cy, r, startAngle, endAngle)`) rather than `QtQuick.Shapes`/`PathArc` (the technique `NSlider.qml` already uses for its own custom-drawn track) - a percentage-arc is far simpler to get right via Canvas than hand-deriving `PathArc` endpoint coordinates from a percentage. Four instances now sit in a 2x2 grid beside the media card: CPU load (`CpuLoad.qml`'s `percent`), CPU temp and GPU temp (`HwmonSensor.qml`'s `tempC`, sensor labels `Tctl`/`edge` same as before), and Battery (`BatteryService.batteryPercentage`, fill color escalating coral→red via the same `isLowBattery`/`isCriticalBattery` checks `BatteryIndicator.qml` already uses). Reused `CpuLoad.qml`/`HwmonSensor.qml` directly as hidden (`visible: false`) property sources rather than re-deriving `/proc/stat`/hwmon parsing - their own `Timer`/`Process` polling keeps running regardless of `visible`, only their own built-in icon+text `Row` rendering is suppressed, since `NCircularGauge` draws its own.

  Panel widened (`contentWidth` 320→380, `implicitWidth` 344→404) and made a bit taller (`implicitHeight` 440→560) to fit the media-card-plus-gauge-cluster row comfortably side by side (media card ~230px, gauge grid alongside it) plus the two rows that moved up from the old System tab.

  One glyph transmission gap caught by the now-standard full-file Python codepoint scan (not the Edit tool this time - a full **Write** tool call, since the whole file was restructured): the volume mute icon's *unmuted* glyph (`` `0xf028` ``) silently didn't transmit even though its sibling *muted* glyph on the same line did - the same known Nerd-Font-glyph-insertion unreliability documented earlier in this file, just the first time it's hit mid-ternary on a single line rather than across two separate lines. Fixed via a targeted Python string replace, then re-verified with the same full-file scan before testing.

  Verified in a Xephyr sandbox via the established `Component.onCompleted` debug trigger: zero errors, and the panel rendered **completely** and legibly on the first try - all four gauges showing real live data (CPU 2%, CPU temp 52°, GPU temp 45°, battery 100%) with correct per-gauge fill colors and icons. Confirmed by inspection (not by seeing it fire, given no click-sim tool) that the "missing" Kernel/Network/Volume/Media rows in that same screenshot are expected, not a regression: the synthetic debug trigger never sets `ControlCenterState.panel` the way a real chevron click always does, and Kernel/Network are `"screens": "primary"`-scoped in the live `modules.json` - `ModulesConfig`'s own `_screenMatches` correctly requires a real non-null panel for a `"primary"` scope, so they're invisible only in this specific synthetic-trigger test path, not in real use. Volume/Media stayed hidden for the ordinary reason (`tray: false` for both in the live config, unchanged this pass). Deployed live; the bar itself re-confirmed unaffected (kernel/media/volume/chevron only, unchanged from the second pass). A full live screenshot of the panel itself again wasn't obtained (same terminal-occupying-the-monitor obstacle as the second pass) - judged acceptable on the same reasoning: byte-identical to the sandbox's already-confirmed-correct render, and real-click confirmation from the user remains the plan.

  User feedback pending on this pass: "some aesthetic issues with icons/padding" was flagged generically after the second pass, without specifics - not addressed yet, since this third pass already changes enough of the panel's layout that some of that feedback may no longer apply as-is. Worth a fresh look once the user has clicked through this version for real.

- **[Done]** Control Center fourth pass: the user sent an actual screenshot of the third pass's real output, which resolved the vague "icons/padding" feedback into two concrete asks - convert the plain-text Network status row into a toggle tile matching Stay Awake/Night Light's style, and add a matching Wifi tile alongside it; separately, the quick-launch row (Clipboard/Wallpaper/Screenshot) was visibly smaller and label-less compared to the toggle tiles right above it in the same screenshot, the clearest concrete instance of the icon/padding inconsistency called out generically after the third pass.

  New `NetworkToggle.qml` and `WifiToggle.qml` (registered in `qmldir`) - both self-contained (no paired State singleton, unlike `StayAwakeState`/`NightLightState`/`DndState`): the Control Center is a single global window, not per-monitor like `Bar.qml`, so there's no cross-instance sync need the singleton pattern elsewhere in this codebase exists to solve. Both toggle via `nmcli` (confirmed actively managing this machine's connections via `nmcli general status`, not just installed - `rfkill` was also available but `nmcli` was already the natural fit given NetworkManager is what's actually running). `WifiToggle.qml` uses `nmcli radio wifi on/off` (global radio switch - this machine has a real, currently-unused `wlan0` adapter, confirmed via `ip -o link show`). `NetworkToggle.qml` uses `nmcli device connect/disconnect <iface>`, finding the right device by type (`ethernet`) rather than a hardcoded name (this machine has both `eth0` and `eth1`; `eth1` is the one actually in use) - deliberately *not* built on `NetworkStatus.qml`'s existing `ip route show default`-based interface detection, since a disconnected interface holds no default route and that technique would lose track of *which* interface to reconnect to the moment it's toggled off. Old `NetworkStatus.qml` status row removed from the main column entirely, superseded by the new tile (the component file itself is untouched and still used - `NetworkStatus.qml` remains the shipped default's inline bar module, unrelated to this Control-Center-specific change).

  Found and fixed a real, independent latent bug while wiring `network`'s tile into the same `showInTray(id, panel)` gate every other tile uses: `showInTray` was reusing `_screenMatches`, the *bar's* per-monitor filter (`"primary"` requires `panel.isPrimary`) - harmless for a passive status *row* nobody clicked, but a real problem for an actionable *toggle*, since it meant the tile would silently vanish from the Control Center entirely if opened from a non-primary monitor's chevron (the Control Center is one single global window with no per-monitor meaning of its own). Fixed by dropping the `_screenMatches` call from `showInTray` entirely - `screens` now only ever gates `showInBar`, documented directly in `ModulesConfig.qml`'s own header comment. `network`'s live-config-and-code-default `tray` flipped to `true` at the same time (matching the pattern established for clipboard/wallpaper/battery/dnd two passes ago); `wifi` is a new module entry, `tray: true` by default (Control-Center-only, same reasoning).

  Quick-launch row rebuilt from three small unlabeled `NIconButton` circles into a second `Grid` of the exact same tile recipe as the toggle grid (`Rectangle` 150x64, `Style.radiusS`, `Colors.pill`, centered icon+label `Column`, click handling moved to a plain `MouseArea` covering the tile since these aren't `NIconButton`s anymore) - full visual consistency with the toggle tiles above them now, directly addressing the flagged inconsistency.

  Two more instances of the established glyph-transmission gap surfaced and were caught the same way as always (a full-file Python codepoint scan before testing, not assumed from the Edit/Write tool's own success): the volume-mute icon's *unmuted* glyph silently failed to transmit in the *previous* pass's `Write` call (only caught now, on this pass's routine scan - not a regression introduced this pass, just not caught until this scan), and all three of the newly-rebuilt quick-launch tiles' icons (clipboard/wallpaper/camera) silently failed to transmit in *this* pass's edits. Both fixed via targeted Python string replacement, re-verified with the same full-file scan before deploying either time.

  Verified in a Xephyr sandbox via the established debug trigger: zero errors, full legible render on the first try, Network and Wifi tiles both showing the correct **real** state and color (blue/active) cross-checked directly against `nmcli device status`/`nmcli radio wifi` (`eth1` connected, wifi radio enabled) rather than just trusting the UI. Deliberately did not click either toggle myself, in the sandbox or live - no click-simulation tool exists here as established throughout this project, but disconnecting networking is also genuinely disruptive (this session's own tool calls, git push, etc. all depend on it), so unlike lower-stakes toggles (DND, Stay Awake) this one's actual click-triggered command execution is left entirely for the user to verify, not something to attempt working around. Deployed live; bar re-confirmed unaffected.

- **[Done]** Control Center fifth pass: made every quick-toggle tile fully clickable across its whole area, not just the small icon+label at its center - a UX request from the user after using the fourth pass's tiles for real. Previously each tile's `Rectangle` had no `MouseArea` of its own; the only clickable region was the wrapped bar-icon component's (`StayAwake`/`NightLight`/`Dnd`/`BluetoothIndicator`/`NetworkToggle`/`WifiToggle`) own small internal `MouseArea`, sized to just its icon+conditional-label content - most of a tile's actual 150x64 area, including its whole visible label, did nothing when clicked.

  Each of the six components gained an exposed `function toggle()` (the exact body their own internal `onClicked` used to run inline) and a new `property bool clickable: true`, which their internal `MouseArea`'s own `enabled` now binds to. `ControlCenter.qml`'s tiles set `clickable: false` on each wrapped instance and add their own tile-covering `MouseArea` that calls `<instance>.toggle()` instead - this was the deliberate design, not an incidental side effect: simply layering a second full-tile `MouseArea` *without* disabling the inner one would have double-handled any click landing on the small inner icon region specifically (toggled, then immediately toggled back by the inner handler firing too), a real bug that would've been easy to ship unnoticed since it only misbehaves for clicks in one specific sub-region of the tile. Each bar-icon usage of these same six components (`Bar.qml`) is completely unaffected - `clickable` defaults to `true` there, and `Bar.qml` never sets it, so the bar's own long-since-proven click behavior is untouched.

  Two more instances of the same glyph-transmission gap were caught doing the routine full-file scan before testing - not new to this pass, both were fine before it and remained fine, the scan is just always run regardless of whether a given edit's own diff plausibly touched a glyph line.

  Verified in a Xephyr sandbox: zero errors, pixel-identical render to the fourth pass (expected - this only changed hit-testing/click routing, not visuals). Deployed live; bar re-confirmed unaffected via the same components' still-working bar-icon usage.

- **[Done]** Control Center sixth pass: hover feedback for all nine quick-toggle/quick-launch tiles - a direct follow-up to the fifth pass's full-tile-clickable change, since a fully clickable tile with zero visual indication of that (no color change, no cursor change beyond the OS default) reads as broken/unresponsive until clicked. Each tile's background now animates between `Colors.pill` (resting) and `Colors.pillActive` (hovered) via a `Behavior on color { ColorAnimation { duration: Style.animationFast } }` - the exact same two-color resting/active convention already used throughout this codebase (`Workspaces.qml`'s pills, `NIconButton`'s own hover state), not a new color introduced for this. Each tile's `MouseArea` gained `hoverEnabled: true` and an `id`, with the `Rectangle`'s `color` binding to `<id>.containsMouse`.

  Verified genuinely differently from every other panel in this whole arc: this is the first Control Center feature confirmed with the pointer actually *moving*, not just a debug trigger opening the panel. No click-simulation tool exists here (established many times over), but hover only needs pointer *position*, not a synthesized click - `xdotool`/`xte`/`ydotool` were all still absent, but `libX11.so.6`/`libXtst.so.6` are both present, so a small ad hoc Python `ctypes` script (`XOpenDisplay`/`XDefaultScreen`/`XTestFakeMotionEvent`/`XFlush`/`XSync`, all with explicit `argtypes`/`restype` declared - omitting them segfaults, since `ctypes` otherwise guesses a plain-`int` ABI for pointer-typed arguments) warped the pointer to real tile coordinates read off an actual screenshot and confirmed, via a second screenshot, that the exact tile under the pointer visibly lightened while every other tile stayed at rest - then confirmed the reverse (moving off it reverts to resting color) by warping to a second tile and checking the first had reset. Real hover-in and hover-out both confirmed, not assumed from the binding logic alone. Deployed live; bar re-confirmed unaffected (this pass touched only `ControlCenter.qml`).

- **[Done]** Control Center seventh pass: a real "now playing" card and a real audio (output+input) section, against a second reference screenshot the user supplied (Noctalia's actual Control Center this time, showing both together, plus the earlier-referenced weather section which the user explicitly said comes after this pass, not part of it).

  **Now playing**: the media card's small 44x44 thumbnail-and-text row became album art as a full-bleed card background (a dark top-to-bottom gradient overlay for legibility, `Colors.pill` fallback when no art), title/artist/album stacked bottom-left over it (artist in `Colors.coral`, matching the reference's warm accent), a real seek `NSlider` wired to `MediaService.seekByRatio()` (existing, previously-unused API - the bar's own mini scrubber, added a few passes back, is read-only by design; this one can actually drag-seek), and bigger playback buttons (28/34/28px vs. the old 26/30/26).

  Deliberately did **not** gate this card behind `ModulesConfig.showInTray("mediaPlayer", ...)` the way every other tile here is gated - a real, evidenced exception, not an oversight: Noctalia's own reference *bar* screenshot (referenced two passes ago) shows a compact "now playing" widget in the bar *at the same time* this same rich card exists in its Control Center, unlike Clipboard/Wallpaper/Settings/etc., which Noctalia keeps Control-Center-only. Zaris's single `tray` flag can't express "both places" - flipping `mediaPlayer` to `tray: true` (matching the pattern used for every other feature enriched this arc) would have deleted the bar's own mini scrubber to gain this card. Left `mediaPlayer`'s tray default alone (still bar-visible via `Bar.qml`'s `MediaWidget`) and simply made the Control Center card visible whenever `MediaService.currentPlayer` exists, independent of the flag - genuinely the same dual-display Noctalia has, achieved the only way Zaris's current architecture allows.

  **Audio**: the old single master-volume row (mute + slider + percentage, no device identification) became two side-by-side columns, Output and Input, each with its own mute button, elided real device name, and volume slider. `Pipewire.defaultAudioSource` (the input/mic device) is newly tracked via `PwObjectTracker` alongside the already-tracked `defaultAudioSink` - confirmed via `PwNode`'s own qmltypes that `name`/`description`/`nickname` all exist; used `description` (reads closest to Noctalia's own verbose device names, e.g. "HyperX Cloud MIX 2 Analog Stereo" - confirmed live against this machine's real devices via `wpctl status` first). Unlike media, `volume`'s `tray` default *was* flipped to `true` (both `ModulesConfig.qml` and the live `modules.json`, which had an explicit override needing a matching update) - Noctalia's bar shows only a bare percentage number for volume, not the rich per-device slider this pass adds, so there's no real bar functionality lost the way there would have been for media; the master-volume-in-bar convenience is judged to belong to the Control Center now that it's this much richer here.

  Panel grew taller to fit the new card and audio section (`implicitHeight` 560→700, unchanged width). Verified in a Xephyr sandbox via the established debug trigger: zero errors, full legible render, both new sections showing correct **real** data - actual device names/volume levels (`wpctl status` cross-checked: output "HyperX Cloud MIX 2 Analog Stereo" ~100%, input "HyperX Cloud MIX 2 Mono" ~88%, both matched what rendered) and a real now-playing track with art, title, artist all correct. Deployed live; bar re-confirmed correct on both counts - volume's percentage now gone from the bar (moved to Control-Center-only, as intended) while the media mini-widget remains exactly as before (deliberately not touched).

- **[Done]** Control Center eighth pass: a profile header (avatar, display name, uptime) to the left of the existing Settings/power/close row, closing out the last piece of the reference screenshot not yet built. `HostService.qml` (a Phase 1 port that had sat with zero real use sites since it was ported - `displayName`/`username` already existed) got its first real consumer; the only new piece added to it was `uptimeText`, reading `/proc/uptime` directly (not parsing `uptime -p`'s locale-dependent prose) and refreshing every minute like every other live-polled stat in this codebase.

  No real `~/.face` exists on this machine, so the avatar needed a real fallback, not just an assumption it'd always have a photo: reused `DockIcons.qml`'s existing fallback-avatar pattern verbatim (a `Colors.pill`-filled circle with a bold first-letter `NText`, that file's own established solution to the identical problem for app icons) rather than inventing a new one. Real-photo support uses `QtQuick.Effects`' `MultiEffect` (`maskEnabled`/`maskSource`) for an actual circular crop, since a plain `Image` can't clip itself to a non-rectangular shape - confirmed available in this Qt 6.11 install before committing to it.

  **A real, non-obvious rendering bug found and fixed**: the mask source and image source `Item`s were set `visible: false` (since they're only meant to feed `MultiEffect`, never render at their own position) - this seemed reasonable but silently produced a fully blank avatar, with zero errors or warnings logged anywhere. Root-caused by testing with a real image (not just reasoning about it): Qt Quick items with `visible: false` aren't necessarily rendered into the scene graph texture `MultiEffect`'s implicit layering grabs from, so there was nothing for it to sample or mask. Fixed by adding `layer.enabled: true` to both the image and the mask `Rectangle` - forcing Qt to render them to an offscreen texture regardless of their own `visible` state, which is what actually let `MultiEffect` source from them correctly.

  Verified with a real temporary test image, not left unverified just because this machine has no real `~/.face` to exercise the photo path organically: generated a synthetic gradient PNG (`magick -size 200x200 gradient:blue-red`), confirmed `~/.face` didn't already exist, copied the test image there, confirmed via screenshot that the fallback letter-badge disappeared and a correctly circular-masked photo rendered in its place (this is what caught the `visible: false` bug above - the first attempt showed neither the fallback nor a photo, just blank space), fixed the bug, reconfirmed the photo path worked, then deleted the test file to restore the machine to its real prior state (no `~/.face`) and reconfirmed the letter-fallback still renders correctly on its own. Both code paths - fallback and real photo - genuinely exercised, not just one.

  Panel grew slightly taller for the new header row (`implicitHeight` 700→720). Deployed live; bar re-confirmed unaffected (this pass touched only `ControlCenter.qml`/`HostService.qml`, neither of which the bar reads from for anything this pass changed).

- **[Done]** Four features from one round of reference screenshots: a real notification history panel, a bar sound icon restored (icon-only, decoupled from the Control Center's audio section), the two new Z-logo assets wired in as default avatar and Control Center launcher, and a custom display name setting.

  **Notification history** - new `NotificationHistoryService.qml`, `NotificationHistoryPanelState.qml`, `NotificationHistoryPanel.qml`, `NotificationIndicator.qml` (bar bell icon, new `notifications` module - `tray: false` by default, unlike every recently-added module, since the user's reference keeps a bell icon permanently in the bar). Reads dunst's own `dunstctl history` (confirmed live: real JSON, plus `history-rm ID`/`history-clear` both work) rather than building a second notification-tracking mechanism - the only real alternative would be Quickshell itself becoming the `org.freedesktop.Notifications` listener, replacing dunst entirely, for something dunst's own tooling already provides. Real wrinkle worked through: dunst's `timestamp` field turned out to be `CLOCK_MONOTONIC` microseconds, not wall-clock epoch time - confirmed live by comparing a just-sent notification's timestamp against `/proc/uptime`'s own monotonic seconds at the same moment (near-identical). Each refresh now reads the real epoch and `/proc/uptime` together in one shell call so every notification's real send time can be reconstructed (`bootEpoch = epoch - monotonicSeconds`, then `notifEpoch = bootEpoch + dunstTimestampMicros/1e6`), needed for the panel's Today/Yesterday/Earlier filter tabs (`NTabBar`/`NTabButton`, matching the reference) to bucket correctly.

  A real, non-obvious layout bug hit twice in this one file (caught by testing, not just reading the code): a `Row` containing a child `Row` positioned via `anchors.right` - Quickshell/Qt Quick logs a clear runtime warning for this ("Row will not function") the *first* time it was hit (the panel's header), but the *second*, structurally-identical instance (each notification card's app-name/timestamp/delete-button row) produced no warning at all and just silently rendered nothing for that whole row - caught only by comparing the actual screenshot against the reference layout, not by trusting a clean log. Both fixed the same way: the outer `Row` became a plain `Item` with an explicit `height`, since only `Row`'s own positioner logic (not anchoring) was ever supposed to be at play for its own two direct children.

  **Bar sound icon** - `VolumeControl.qml` simplified to icon-only (mute state, no percentage - the real volume/device sliders live in `ControlCenter.qml`'s audio section now, matching the reference bar showing a bare speaker icon with no number). Same "both places at once" exception already established for the media card: `Bar.qml`'s usage is no longer gated by `ModulesConfig.showInBar("volume", ...)` at all (that flag now only controls the richer Control Center panel), falling back to the component's own internal `sink && sink.ready` visibility instead.

  **Z-logo assets** - two new images the user supplied (`zaris-logo-square.png`, `zaris-logo-circle.png`, both copied into `shell/quickshell/assets/` and the live `~/.config/quickshell/assets/`, alongside the existing `zaris-logo.png`/`artix.svg` there) are genuinely different marks from the existing full "ZarisWM" wordmark logo, not a resize of it. The circular one is now the Control Center avatar's default (replacing the plain letter-badge fallback from the previous pass entirely) whenever no real `~/.face` exists - `~/.face` still takes priority when present, unchanged. The square one replaced the bar's "..." chevron as the Control Center launcher outright, sized down to 20x20 and no longer hidden when nothing's tray-enabled (stale logic from when Control Center was just the old flat hidden-tray flyout - it always has real content now regardless of any one module's tray setting).

  **Custom display name** - a new `identityFile` (`~/.config/quickshell/identity.json`, same `FileView`+`JsonAdapter` pattern as `modules.json`/`dock.json`) on `HostService.qml`, holding one `customDisplayName` string, checked first in `displayName`'s existing realName→username→"User" fallback chain - deliberately separate from the real Linux account (`$USER`/GECOS), so nothing here ever touches `useradd`/`chfn`. New "Profile" category in `Settings.qml` (first in the sidebar, ahead of Bar/Dock) with one `NTextInput` (label, description naming the real underlying username, placeholder falling back to it too) wired to `HostService.setCustomDisplayName()`.

  Verified in a Xephyr sandbox via the established debug-trigger pattern, each piece separately: the notification panel (real dunst history, all four filter tab counts correct, both the header-row and delegate-row layout bugs caught and fixed here), the Control Center avatar (circular Z logo rendering as the new default), the bar's new sound icon and square-Z launcher (both via a direct screenshot crop of the real icon row), and the Settings Profile tab (new sidebar category, correct placeholder text). Full-file Python codepoint scans run before every deploy, across every touched file, per the established discipline - confirmed each of the ten-plus new glyphs (bell, trash, close, person, plus the already-established ones reused) actually transmitted. Deployed live; bar re-confirmed correct end to end with a live screenshot.

- **[Done]** Custom avatar picker: clicking the profile avatar in the Control Center header now opens a directory-browse thumbnail grid (new `AvatarPickerPanel.qml`/`AvatarPickerPanelState.qml`), letting the user pick any image under a scannable directory (default `~/Pictures`) as their profile picture. Same browse-thumbnail-grid recipe as `WallpaperPickerPanel.qml` (directory `TextInput` + Rescan button, `find`-based `scan()` via a plain argv `Process`, `NGridView` of thumbnails), scoped down since an avatar has no meaningful "currently active path" the way a wallpaper does - picking a thumbnail just copies it over `~/.face` (`cp <path> ~/.face`) and closes the panel. `avatar`'s `Item` in `ControlCenter.qml` gained a `MouseArea` (pointing-hand cursor, opens the picker) plus the same hover-ring treatment already established for the quick-toggle tiles (a thin `Colors.pillActive` circular border shown on hover), confirmed via the same XTest-pointer-warp technique used for the sixth pass's tile hover states - a genuine before/after pixel diff (56 of 10000 sampled pixels changed) confirmed the ring actually toggles, even though it's visually subtle against the default Z-logo art's own similarly-colored ring.

  **A real race-condition bug found and fixed, not a sandbox artifact**: the cache-busting scheme built during the previous pass (`AvatarPickerPanelState.version`, appended as a `?v=N` query string to `ControlCenter.qml`'s `~/.face` `Image` source, forcing Qt's URL-keyed image cache to treat a same-path overwrite as a new resource) was originally bumped - and the panel closed - immediately after starting the `cp` process (`setProc.running = true`), not after it finished. Since `Process.running = true` returns immediately without waiting for the subprocess, the version bump (and the resulting image reload) usually fired *before* `cp` had actually finished writing the new file, so `Image` tried to load a file that didn't exist yet, landed in `Image.Error`, and - critically - never retried on its own once the real file appeared a split-second later, leaving the avatar stuck on the fallback logo permanently after every single pick. Caught by adding a visible on-screen `version`/`status` debug readout (not just trusting the binding logic) and watching `status` read `3` (Error) even after `~/.face` demonstrably existed on disk with the right content. Fixed by moving both the version bump and `AvatarPickerPanelState.visible = false` into `setProc.onExited`, gated on `exitCode === 0` - the same `Process.onExited` pattern already used throughout this codebase (`ClipboardHistoryService.qml`, `BrightnessService.qml`, etc.) - so the reload only ever fires once the copy has actually and successfully landed on disk.

  Verified end-to-end in a Xephyr sandbox: opened the picker via a real XTest click on the avatar (not just a debug trigger), clicked a real photo thumbnail via a second real click, and confirmed - after the race-condition fix - the avatar immediately switched from the Z-logo fallback to a correctly circular-masked real photo, reusing the existing `MultiEffect`/`layer.enabled: true` masking machinery from the profile-header pass unchanged. Full-file Python codepoint scan run on every touched file before deploy (clean - no new glyphs this pass beyond ones already established). Diffed every changed file against the live config before copying (all changes additive and expected, including the two brand-new files with no live counterpart to diff against) and deployed live; confirmed via a clean "Configuration Loaded" log and a live bar screenshot that nothing else regressed.

- **[Done]** Weather - the last piece deferred out of the seventh Control Center pass, now built as a real feature rather than left as a placeholder. Asked the user up front how location should be determined (a real architecture decision, the same category as the still-deferred calendar-backend and dark-mode-recolor choices noted earlier in this file - not something to guess at silently): the answer was auto-detect via IP geolocation with a manual override available in Settings, not one or the other alone.

  New `WeatherService.qml` singleton, keyless throughout: `api.open-meteo.com/v1/forecast` for current conditions + today's high/low (confirmed free and keyless for this volume live), `geocoding-api.open-meteo.com/v1/search` for turning a typed city name into coordinates, and `ipinfo.io/json` for the one-time-per-refresh IP-based lookup used whenever no manual location is set (tried `ipapi.co` first - hit a shared-IP rate limit immediately in this sandbox environment, so switched to `ipinfo.io` instead, confirmed reliably working over plain HTTPS with no key). All three fetched with QML's own built-in `XMLHttpRequest` rather than shelling out to `curl` - confirmed working (full outbound HTTPS access from inside the Quickshell process itself, tested standalone via the `qml` runner before writing any real code) and there's no output here a CLI tool would make meaningfully easier to parse than a plain JSON GET already is. Refreshes every 15 minutes via a repeating `Timer`, `triggeredOnStart: true` so the very first frame doesn't sit on a blank state waiting a full interval.

  Icon glyphs pulled from the Nerd Font "Weather Icons" block (`0xe300`-`0xe3e3`) - a large, dedicated weather icon set already installed on this machine as part of JetBrainsMono Nerd Font, a much better fit than reaching for generic Font Awesome cloud/sun glyphs the way an earlier instinct suggested. Every candidate codepoint was checked glyph-name-by-glyph-name against the actual installed font's `cmap`/`post` tables via `fontTools` *before* picking any of them (several plausible-looking Font Awesome 6 codepoints guessed from memory - `cloud-sun`, `cloud-rain`, `cloud-bolt`, `smog` - turned out to be genuinely missing from this font entirely, confirming the "wrong codepoint renders as a blank tofu box with zero warning" risk this project has hit before is real, not hypothetical, and worth checking against the real font rather than trusting a remembered codepoint table). WMO weather codes (open-meteo's `weather_code`, the same table used industry-wide) collapsed to a day/night icon pair per condition family (clear/cloudy/overcast/fog/drizzle/rain/snow/showers/thunderstorm), picking the night variant whenever open-meteo's own `is_day` flag is `0` rather than guessing from local wall-clock time.

  Control Center gained a new row below the media-card-and-gauges row (icon + temperature/condition + location/high-low, `implicitHeight` 720→770); Settings gained a new "Weather location" field in the existing Profile category, right below Display Name - typing a city and leaving the field calls `WeatherService.setManualLocation()`, clearing it back to empty calls `useAutoLocation()`, both persisted through the same `FileView`+`JsonAdapter` pattern as `identity.json`/`modules.json` (`~/.config/quickshell/weather.json`, one `manualLocationQuery` string, empty meaning auto-detect).

  Verified in a Xephyr sandbox with a real network round-trip, not mocked data: the auto-detect path correctly resolved this machine's real public IP to Fuquay-Varina, NC and rendered real live conditions (85°F, Clear, sun icon, H:87° L:60°) matching what a plain `curl` to the same two APIs returned independently. The manual-override and geocoding path was exercised via a temporary debug `Timer` calling `WeatherService.setManualLocation("Tokyo")` (removed before deploy, same disposable-scaffolding discipline as every `Component.onCompleted` debug trigger elsewhere in this file's history) - correctly geocoded to real coordinates and rendered Tokyo's actual current conditions, and because it happened to be nighttime there at test time, this incidentally exercised and confirmed the night-icon branch of the mapping too (a crescent-moon glyph, not the sun), so both day and night code paths were genuinely verified, not just one. The test also wrote a real `manualLocationQuery: "Tokyo"` into the live `~/.config/quickshell/weather.json` (this config file's path is hardcoded to the real `$HOME`, unaffected by which shell.qml a sandbox `qs` instance is pointed at) - caught and deleted before finishing, restoring the machine to its real prior auto-detect state. Full-file Python codepoint scan run on every touched file before deploy (all 20 new Weather Icons glyphs plus every already-established glyph elsewhere confirmed intact). Deployed live; confirmed via a clean "Configuration Loaded" log and a live bar screenshot that nothing else regressed.

  One deliberate, undiscussed default worth flagging: temperature is shown in Fahrenheit, not Celsius (unlike this codebase's existing hardware-temperature readouts, e.g. `HwmonSensor.qml`, which are all Celsius) - a judgment call based on this machine's US locale (EDT timezone, US-format date string already in the bar) rather than a matched convention, easy to flip to Celsius in `WeatherService.qml`'s `fetchWeather()` (`temperature_unit=fahrenheit` query param) if that's not the right call.

- **[Done]** Power profile switching - the other half of "we can add in weather and power profiles" from the fourth pass, closing out both deferred items now. A new three-way segmented row (Saver/Balanced/Performance, leaf/balance-scale/bolt icons - each checked against the installed font's real glyph table before use, the same discipline the weather pass established) sits below the quick-launch icon row, backed by `power-profiles-daemon`'s own `powerprofilesctl` CLI - the standard freedesktop mechanism for this, same one Noctalia itself almost certainly wraps.

  Built ahead of a genuinely missing dependency, per this project's own established practice (see `DEPENDENCIES.md`, new row added for it): `power-profiles-daemon` isn't installed on this machine (confirmed via `which powerprofilesctl`), but it's cleanly packaged for all three of Arch/Debian/Fedora (Arch even ships a dedicated `-openrc` init-script variant, matching this machine's init system the same way the `xss-lock`/`i3lock` swap did earlier). New `PowerProfileState.qml` singleton relies on the same graceful-when-missing `Process` behavior already documented and relied on for `BrightnessService.qml`'s `ddcutil detect` call: querying `powerprofilesctl get` at startup simply never calls back when the binary doesn't exist, so `currentProfile` stays empty and no tile shows falsely active - not a crash, not an error, just correctly inert until the daemon exists. The three profile names (`power-saver`/`balanced`/`performance`) are hardcoded rather than parsed from `powerprofilesctl list`'s human-oriented text output - a stable, versioned part of the D-Bus API, lower-risk than depending on that output format staying parseable.

  Same optimistic-update convention as every other toggle in this codebase (`StayAwake.qml`/`Dnd.qml`'s own `toggle()` functions): clicking a tile updates `currentProfile` immediately and fires `powerprofilesctl set <name>` via `Quickshell.execDetached` in the background, rather than waiting on a round trip that - on this machine - would never actually complete anyway.

  Verified in a Xephyr sandbox via the established debug trigger: zero errors, all three icons rendered crisp (no tofu boxes - each codepoint checked glyph-name-by-glyph-name against the installed font's `cmap` before use, same lesson the weather pass's missing-FA6-icon near-miss reinforced), and - since the backing daemon genuinely isn't installed here - no tile shows active by default, correctly matching the "inert until installed" design. Clicked "Performance" via a real XTest click and confirmed via a before/after screenshot that the tile immediately highlighted active (brighter fill, coral bolt icon) - the optimistic-update half of the feature is fully proven even though the actual `powerprofilesctl set` call underneath has no daemon to receive it yet on this specific machine. Full-file Python codepoint scan run before deploy (clean). Deployed live; confirmed via a clean "Configuration Loaded" log and a live bar screenshot that nothing else regressed.

- **[Done]** Control Center layout pass: four user-requested changes against the live power-profile/weather build, all in one round rather than separately.

  **Audio moved up**: the Output/Input volume section now sits directly below the profile header, before the quick-toggle grid, rather than after it - a pure relocation (the `Row` block itself untouched) via a small Python script that cut the exact line range and reinserted it, safer than several fragile `Edit` calls across a file this size.

  **Power profile: three tiles → one cycling button**: the three-way segmented row from the previous pass is gone, replaced by a single tile inside the main quick-toggle grid (icon + label reflecting whichever profile is currently active) that steps to the next profile on each click. New `PowerProfileState.cycleProfile()` - `profiles.indexOf(currentProfile)`, `+1` wrapped with `%`, falling back to index `0` when `currentProfile` is still `""` (nothing queried back yet, or the daemon's missing) so the very first click always lands on a real profile rather than no-op'ing.

  **Grid reordering**: the quick-toggle grid's left column (Stay Awake, Do Not Disturb, Network) stays exactly where it was; the right column shifted down one row and Power Profile now fills the vacated top slot, per the user's own description of the target layout. Reordering a `Grid`'s automatic row-major fill to hold one column fixed while shifting only the other isn't expressible by simply inserting the new tile - it needs the *entire* DOM order rebuilt (`stayAwake, PowerProfile, dnd, nightLight, network, bluetooth, <placeholder>, wifi`), including a new invisible placeholder `Item` (`tileWidth`x`tileHeight`, no content) at the one grid cell that's left with no natural left-column partner once the column counts go from 3-and-3 to 3-and-4. Done via the same Python-script block-extraction approach as the audio move - each tile's exact line range was located by bracket-depth-matching from its `Rectangle {` start, then reassembled in the new order - verified correct afterward by re-scanning the file and printing each block's label in its final position before ever opening Xephyr.

  **Width and padding**: `implicitWidth` 404→440, giving the existing `contentWidth`-380 content real breathing room on both sides (padding roughly 2.5x what it was) rather than making the content itself wider - plus a subtle 1px `Colors.pill` border around the whole panel background, a light-touch stand-in for the "background styling" the user pointed at in Noctalia's own reference screenshot from an earlier pass. `implicitHeight` bumped twice in this same pass (830→900) - the tile grid gained a full extra row from the reordering above, and the first height guess genuinely clipped the new bottom-most content (the weather row) in the very first Xephyr screenshot, caught by looking at the actual render rather than assuming the estimate was right.

  Verified in a Xephyr sandbox via the established debug trigger, `qmllint` run on both touched files first (clean, catching any structural mistake before spending a full Xephyr cycle on it) given how much of this pass was mechanical text surgery on a large file: confirmed the exact requested layout rendered (audio above the grid, Stay Awake/Do Not Disturb/Network column undisturbed, Power Profile beside Stay Awake, the old right column visibly shifted down one row with an empty cell where the layout genuinely has no left-column partner for Wifi), and the height-clipping bug caught and fixed mid-pass. Cycling verified with two real XTest clicks on the same tile, screenshotting between each: `""` (unqueried) → "Saver" (leaf icon) → "Balanced" (scale icon), confirming both the wraparound-from-empty fallback and the cycle-forward logic. Full-file Python codepoint scan run on both touched files before deploy (clean). Deployed live; confirmed via a clean "Configuration Loaded" log and a live bar screenshot that nothing else regressed.

- **[Done]** Control Center layout pass 2: three more fixes from the previous pass's own screenshot.

  **Grid uniformity**: the previous pass's placeholder `Item` (needed because Power Profile pushed the right column to 4 rows against the left column's 3) is gone - `Clipboard` moved out of the quick-launch grid and into that exact slot instead, closing the gap. Since Clipboard's removal left the quick-launch grid with only Wallpaper and Screenshot, that grid collapsed from 2 rows to 1 (`Wallpaper | Screenshot`) - both grids are now completely full, no empty cells anywhere, exactly the "shift clipboard and screenshot up one spot" the user asked for. Same Python block-extraction approach as the previous pass (locate each tile's exact line range via bracket-depth matching, reassemble in the new order, verify the result by re-scanning and printing each block's label before opening Xephyr) - `implicitHeight` trimmed back down (900→830) afterward since the quick-launch grid's now-shorter height freed up space the previous pass's estimate hadn't accounted for losing.

  **Bar media widget removed**: now that the Control Center's own "now playing" card exists, showing the same track info in both the bar and the Control Center was redundant - `Bar.qml`'s `MediaWidget` usage is gone outright (not just hidden), ending the "both places at once" exception that card's visibility used to lean on. `mediaPlayer`'s `tray` default flipped `false`→`true` in `ModulesConfig.qml` (matching every other Control-Center-only module now) and the Control Center's own media card gained the `showInTray("mediaPlayer", ...)` gate it never had before - previously vestigial config (the tray flag had zero effect on anything once the bar's own usage ignored it too) is now meaningful again: a user really can hide the media card via Settings' existing tray toggle if they want to.

  **Control Center launcher icon**: sized up (20→26) and switched from `Image.PreserveAspectFit` to `Image.PreserveAspectCrop`. Root cause of the "smushed" look wasn't a stretch/distortion bug - the underlying asset (`zaris-logo-square.png`) is genuinely 347x304, not square, so fitting it into a square box via `PreserveAspectFit` left real letterboxing (empty vertical space top and bottom) rather than filling the box, reading as compressed next to the bar's other true-square icons. `PreserveAspectCrop` fills the box completely by cropping a sliver off the wider left/right edges instead - a cosmetically "more square" result at the cost of a small, imperceptible crop on a mark that's mostly centered content anyway.

  Verified in a Xephyr sandbox via the established debug trigger: confirmed both grids render fully packed with zero empty cells, `qmllint` clean on all three touched files beforehand. The icon and bar-media-removal changes were confirmed on the **real live desktop** directly rather than in Xephyr (a plain `Image`/`Bar.qml` change, lower-risk than a FloatingWindow layout change and faster to just check for real) - a before/after zoomed crop of the bar's far-right corner shows the Control Center launcher rendering as a clean, larger, fully-filled square with no letterboxing, and the bar's info row confirmed to no longer show any track title/artist text at all. Full-file Python codepoint scan run on every touched file before deploy (clean). Deployed live; confirmed via a clean "Configuration Loaded" log and a live bar screenshot.

- **[Done]** Bar typography pass: workspace pills, the clock, and the kernel version module all matched to one consistent size and weight, per direct user feedback that they read as "a bit big" and inconsistent with each other. The clock (`Bar.qml`, `Style.fontSizeL`/`Style.fontWeightBold`) was already the look the user wanted kept - `Workspaces.qml`'s pill label and `KernelVersion.qml`'s icon+text (both previously `Style.fontSizeL` with no explicit weight, defaulting to `NText`'s own `Style.fontWeightMedium`) were the two that needed to change, not the clock. All three now read `Style.fontSizeM` (13→11, "a few px down" as asked) with an explicit `Style.fontWeightBold` added to the two that didn't have it.

  `KernelVersion.qml`'s edit needed Python rather than the `Edit` tool - the established Nerd-Font-glyph-content match gap (the line being edited sits directly next to a `text: ""` Linux-glyph line) - applied with a full before/after codepoint scan to confirm the glyph itself (`0xf17c`) survived the edit untouched.

  Workspace pills need a running WM (`wmctrl -d` populates them) that Xephyr's no-WM sandbox can't provide, so - like the DND toggle and other WM-dependent visuals earlier in this project - verified directly on the **live desktop** instead: `qmllint` clean on all three touched files first, diffed against live (all three diffs purely additive/the intended size-and-weight lines, nothing else), deployed, and confirmed via three separate zoomed crops of the real bar that workspace numbers, the clock, and the kernel version string now render at visually matching size and weight.

- **[Done]** Dock overhaul: position (top/bottom/left/right), launcher position (start/end), per-monitor screens scoping, background opacity/color, and floating-mode auto-hide - a full settings surface against a real Noctalia dock reference screenshot the user supplied, on top of the dock's existing enabled/mode/pinning support.

  **DockConfig.qml** grew five new settings (`position`, `launcherPosition`, `screens`, `backgroundOpacity`, `backgroundColor`, `autoHide`), each with the same hand-editable-JSON-plus-Settings-GUI pattern already established for `mode`/`pinned`. `screens` reuses `ModulesConfig.qml`'s own "all"/"primary"/[exact output names] shape verbatim (`screenMatches()` mirrors `_screenMatches()`) - the dock was previously hardcoded to the primary monitor only, now a per-monitor `Variants` wrapper (same mirrored-output dedup as `Bar.qml`) creates one dock per matching, deduplicated monitor.

  **DockContent.qml** (new) factors the icon-strip layout out of `Dock.qml`'s two window variants: four explicit `Row`/`Column` combinations (position x launcherPosition) rather than one clever reordering trick, since stock `Row`/`Column` always lay out children in declared order with no generic "reorder a positioner's children" property to bind against. **DockIcons.qml** gained a `vertical` mode (position/drag-axis/running-indicator-edge all conditional) for left/right docks, which lay icons out top-to-bottom in a `Column` instead of left-to-right in a `Row`.

  **New WM windowrules** `topcenter`/`leftcenter`/`rightcenter` (`events.cpp`, `ConfigManager.cpp`) mirror the existing `bottomcenter`'s exact math for the other three monitor edges - floating-mode positioning is entirely title-matched (`zaris.conf`'s `windowrule=*center ...,title:^Dock-<position>$` lines), with `Dock.qml` itself encoding the current position into its own window title (`"Dock-" + DockConfig.position`) so a live Settings change re-triggers the right static rule with no WM-side awareness of the JSON setting needed. Per explicit user request: floating-mode "top" gets a 54px margin (44px `Bar.qml` height + the same 10px gap the other three positions use), not the plain 10px the others get - the bar always reserves the top edge natively (its own `PanelWindow` anchor, no windowrule involved), so a floating dock also at "top" needs a static offset to actually clear it rather than overlapping.

  **Auto-hide** (floating mode only - reserved is always fully visible, same as the bar): a small, always-present, fixed-size trigger window (a thin highlight strip, same title/position-matched windowrule family as the real dock) swaps for the real dock the instant the pointer hovers it, and swaps back ~600ms after the pointer leaves either. Hover state is tracked with `HoverHandler`, not a `MouseArea` - a real bug caught by testing: a `z: -1` background `MouseArea` never saw hover at all while the pointer was directly over an icon, since each icon's own (higher, default-*z*) `MouseArea` wins Qt's normal one-`MouseArea`-at-a-time hover exclusivity, so leaving the dock *from* an icon never fired the background one's `onExited` and the dock never re-hid. `HoverHandler` is a separate input-handler mechanism that observes hover state without competing with `MouseArea`s for it, confirmed fixing exactly that failure case (hover-over-an-icon-then-leave now correctly re-hides).

  **Two real, deeper bugs found and fixed along the way, not sandbox artifacts:**

  1. An earlier version of this pass used a `Loader` to fully destroy and recreate the floating dock's window on every `DockConfig.position` change, specifically to force a fresh size negotiation (a live `implicitWidth`/`implicitHeight` change alone doesn't reliably reach the real platform window once a `FloatingWindow` has already been mapped once - the same limitation `ControlCenter.qml`'s own header comment documents). This reliably reproduced a much worse regression: the recreated window came back **tiled instead of floated**, sized however the tiling layout gave it (confirmed via `wmctrl -lG` showing real, wrong geometry close to the monitor's full workarea) - on a clean, single-property-unchanged launch, not a timing race from rapid edits. Root cause: the WM's `float`/`*center` windowrules match by title at the exact instant a window is first mapped, and a `Loader`-instantiated window's `title` binding doesn't reliably finish applying before that first map the way a directly-declared window's does, so the rule can miss entirely. Fixed by reverting to a plain, always-instantiated `FloatingWindow` (the same "always exists, `visible` toggles" pattern already used everywhere else in this codebase - `Settings.qml`, `ControlCenter.qml`, etc.) with a `visible` false-then-true toggle on title change to force a remap instead of a full recreate.

     **Known, accepted, and documented limitation from that revert**: the remap correctly repositions on a live position change (the WM computes position fresh from monitor geometry every time), but does *not* reliably resize for an *orientation* change (top/bottom &harr; left/right) without a full `qs` restart - resizing depends on the real X11 window already being the right size when the WM re-reads geometry on remap, and Quickshell doesn't reliably push a `FloatingWindow`'s implicit size changes to the real platform window post-first-map (root cause of the *previous* bug's own workaround attempt, in fact - fixing this one traded a severe, silent-breakage bug for a narrower, honest, and clearly-documented one). Reserved mode (a `PanelWindow`, never subject to this) handles every orientation change live with no caveat at all - floating mode's live-reposition-but-not-resize gap is real but bounded to a less common combination (floating *and* changing between horizontal/vertical while already running).

  2. `wmctrl -l`/`-lG` were assumed to be a reliable way to inspect the dock's live geometry while debugging bug #1 above, and returned entirely plausible-looking (if wrong) results - only after the fix made the dock correctly floating again did `wmctrl` stop showing it at all, revealing that `EWMH::updateClientList()`'s own `!w.getIsFloating()` filter (added deliberately in an earlier pass, see above) means **no floating window, dock or otherwise, has ever been visible to `wmctrl` in this WM** - it was never a valid geometry-checking tool for this class of window, floating or tiled-masquerading-as-floating alike. Screenshots (and, for a real fix, a small purpose-built xcb harness's own `xcb_get_window_attributes` query) are the only reliable way to check a floating window's actual state going forward.

  Verified in a Xephyr sandbox against the real `zaris` WM binary (a Debug build, `~/.config/zaris/zarisd.conf`, same harness/discipline as the earlier fullscreen-dock-bug pass): all four reserved positions, all four floating positions (bottom, and top confirmed clearing the bar with the new 54px margin), launcher start/end, background opacity/color both confirmed with visually distinct test values, the Settings GUI itself (all controls render and click correctly), right-click pin/unpin still works correctly in a vertical (left-positioned) dock, the running-indicator dot correctly moves to the trigger's outward edge for a vertical dock, and the full auto-hide cycle (hidden -> hover trigger reveals -> hover an icon specifically -> leave -> re-hides -> hover reveals again) end to end. Full-file Python codepoint scan run on every touched file before deploy (clean). Diffed every changed file against the live config before copying (all additive/intended, including the two brand-new files with no live counterpart) and deployed live; the WM binary itself is rebuilt and staged at `~/.local/bin/zaris` (the running live WM process is unaffected, same safe `cp --remove-destination`-while-running technique as the earlier fullscreen-dock-bug pass) but **not yet active** until the next full logout/login, same limitation as before - this WM has no live-reload of its own binary.

- **[Done]** Settings window overhaul: attached to the bar the same way `CalendarFlyout.qml` attaches to the clock, Control Center now auto-closes when its own Settings (gear) button is clicked, and Settings gained a Close button matching Control Center's own.

  **`PopupWindow` instead of `FloatingWindow`**: `Settings.qml`'s root type changed from a centered, title-matched `FloatingWindow` (`windowrule=float,title:^Shell Settings$` / `windowrule=center,...`) to a `PopupWindow` anchored via `anchor.item`/`anchor.rect.x`/`anchor.rect.y` - the exact mechanism `CalendarFlyout.qml`/`Tooltip.qml` already use, per the user's explicit request to match the calendar's attach-to-bar behavior. `PopupWindow` exposes no `title` property at all (confirmed via qmltypes inspection - no such `Property` block under its `ProxyWindowBase` prototype), so both WM-side windowrule lines for "Shell Settings" are gone outright, replaced with an explanatory comment - positioning is now entirely anchor-based, no WM cooperation needed.

  **A real bug found via live testing, not just reasoning**: the obvious anchor target - Control Center's own gear button, the thing actually clicked - never worked. Anchoring `Settings.qml`'s `PopupWindow` there and setting `ControlCenterState.visible = false` in the same `onClicked` handler (tried both statement orders) resulted in Settings never appearing at all, confirmed twice via Xephyr screenshot rather than assumed. Root cause: a `PopupWindow` can't resolve/show itself against an `anchor.item` target that lives inside a window which has just gone invisible - QML doesn't render mid-function, so by the time Qt would need to resolve the anchor, Control Center (and its gear button) had already been hidden in that same tick. Fixed by adding `ControlCenterState.launcherItem` (the bar's own Control Center launcher `Image`, given `id: ccLauncherIcon`, set alongside the pre-existing `panel` property whenever `Bar.qml`'s launcher is clicked) and anchoring Settings there instead via a new `SettingsState.targetItem`, set to `ControlCenterState.launcherItem` right before opening - the same "one shared instance, retargeted per interaction" pattern `CalendarFlyoutState.qml` already established for the clock. The bar itself never closes, so this stays a valid anchor regardless of Control Center's own open/closed state.

  **Auto-close + Close button**: the gear button's `onClicked` now sets `SettingsState.targetItem`, then `SettingsState.visible = true`, then `ControlCenterState.visible = false`, closing Control Center the instant Settings opens, per the user's explicit request. Settings gained its own top-right Close `NIconButton` (`0xf00d`, the identical glyph and styling Control Center's own close button uses) that sets `SettingsState.visible = false`.

  Verified in a Xephyr sandbox against a real running qs instance: confirmed (via real XTest clicks, not just binding inspection) that clicking the bar's launcher icon opens Control Center and records `launcherItem`, clicking the gear button closes Control Center and opens Settings correctly anchored under the bar icon, the cycle is repeatable (closed and reopened a second time with identical correct behavior), and the new Close button dismisses Settings cleanly. Full-file Python codepoint scan and `qmllint` run clean on every touched file (`Settings.qml`, `SettingsState.qml`, `ControlCenter.qml`, `ControlCenterState.qml`, `Bar.qml`) before deploy. Diffed every file against the live config first (all diffs purely the intended changes) and deployed live; confirmed via real screenshots on the actual live desktop (not just Xephyr) that the full open-gear-click-close-reopen cycle works identically on the real system with zero regressions to the bar, Control Center, or any unrelated window.

  **Follow-up pass, same day**: two direct requests after seeing it live - make the window "much taller" and center it under the bar rather than right-aligning it. `implicitHeight` went 460→720 (leaving comfortable margin under a 1080-tall monitor once the 44px bar + 10px gap is subtracted), letting each category's content show far more before needing to scroll. Centering needed a wider anchor target than the small 26x26 launcher icon: `Bar.qml`'s full-width background `Rectangle` (already `anchors.fill: parent` inside the bar's `PanelWindow`) got `id: barSurface`, and `ControlCenterState.launcherItem` was renamed to `barItem` and repointed to that surface instead of the launcher icon - `anchor.rect.x` changed from `targetItem.width - implicitWidth` (right-aligned) to `(targetItem.width - implicitWidth) / 2` (centered), now centering under the *whole bar's* width rather than the icon's. The now-unused `ccLauncherIcon` id in `Bar.qml` was removed along with it. Same anchor-must-stay-valid reasoning as before still applies - `barSurface`, like the icon before it, lives in the bar's window, which never closes. Verified in a fresh Xephyr sandbox and then live on the real desktop: the window now shows roughly 14 module rows instead of 8 before needing to scroll, and sits centered under the bar instead of tucked under the launcher icon near the right edge. `qmllint` and a full-file codepoint scan stayed clean; diffed against live before deploying (all diffs the intended change only).

- **[Done]** Taskbar mode: the previously-backlogged "Dock-as-bar-module" idea, built out for real as a full second `BarConfig.layoutMode` alongside the original "statusbar" layout, plus a Settings restructure that came out of testing it and a real bug fix for every bar-anchored popup's open direction.

  **`BarConfig.qml`** (new singleton, `~/.config/quickshell/bar.json`, same hand-editable-JSON-plus-Settings-GUI pattern as `DockConfig`/`ModulesConfig`): `layoutMode` ("statusbar"/"taskbar"), `position` ("top"/"bottom" only for now, per explicit user request - left/right is backlogged the same way the dock already has them), `backgroundOpacity`, `height` (32-96px, clamped). `Bar.qml`'s `PanelWindow` anchors/`implicitHeight`/`exclusiveZone`/background alpha are now driven entirely from these instead of the previous hardcoded top/44/0.75.

  **Two layouts, one `Loader`** (same "explicit variants read easier than indirection that fakes one" reasoning `DockContent.qml` already established): "statusbar" is the original arrangement unchanged (logo+workspaces left, clock center, status modules+Control Center right). "taskbar" is a Plasma/Windows-style rearrangement per the user's exact spec - launcher+embedded app strip on the left, workspaces centered, status modules+clock+Control Center on the right (clock moved out of the center and into the right-side row, right before Control Center). The status-module row, the Control Center launcher icon, and the clock were each factored into their own reusable file (`BarStatusModules.qml`, `BarControlCenterLauncher.qml`, `BarClockText.qml`) specifically so the two layouts could share them verbatim rather than duplicating a ~15-module list between two Loader branches.

  **Embedded app strip**: reuses `DockIcons.qml` directly (not the standalone dock's `DockContent.qml` wrapper, which bakes in position/launcher-position logic that doesn't apply to an always-horizontal bar strip) with a new overridable `iconSize` (was hardcoded `readonly`) sized to fit `BarConfig.height`. Its actual pinned+running data now comes from a new `DockItemsService.qml` singleton - the exact same `wmctrl -lx`-polling, pin/running-merge logic that used to live directly inside `Dock.qml`, extracted out so both the standalone dock and the bar's embedded strip poll once and share one result instead of running the same `wmctrl` poll twice. `Dock.qml`'s own reserved/floating windows now also suppress themselves outright while `BarConfig.layoutMode` is "taskbar" (`BarConfig.layoutMode !== "taskbar"` added to both windows' visibility) - the embedded strip replaces the standalone dock rather than the two coexisting, per the user's explicit design.

  **Launcher anchored to the bar in taskbar mode**: `Launcher.qml` was a single centered `FloatingWindow`; it's now an `Item` holding the shared query/filter/launch state (same "one Item, two Window children" pattern `Dock.qml` already uses for reserved/floating) with two window variants sharing a new `LauncherContent.qml` for the actual search+list UI - the original centered `FloatingWindow` (still used in "statusbar" mode, unchanged behavior) and a new `PopupWindow` anchored to a new `LauncherState.anchorItem` (the bar's own taskbar-mode launcher icon, set right before toggling visible) for "taskbar" mode - "similarly to Windows UI, KDE Plasma, etc." per the user's explicit request. Only one variant's `LauncherContent` is ever the "active" one (a `required property bool active`, matched against which layout mode is current) so the reset-query-and-refocus-search-field behavior on open doesn't fire twice or fight itself between the two.

  **A real bug found via live testing (mid-pass user report), not just reasoning**: every bar-anchored popup (`Settings.qml`, `ControlCenter.qml`, `CalendarFlyout.qml`) unconditionally opened *below* its anchor with a fixed `+10` offset - fine while the bar only ever lived at the top, but with `BarConfig.position` now also "bottom", a downward-opening popup got pushed toward (or past) the bottom of the screen instead of staying attached to the bar, since Quickshell's own anchor-clamping-to-stay-onscreen behavior would silently reposition it somewhere disconnected-looking rather than erroring. Fixed with one shared helper, `BarConfig.popupAnchorY(targetItem, popupHeight)`: opens below the target when the bar is at the top, above it (negative offset) when the bar is at the bottom - now used by all four bar-anchored popups (Settings, Control Center, Calendar, and the taskbar-mode Launcher, which had already worked this out inline for itself and got switched to the shared helper too for consistency). **`ControlCenter.qml` itself was also converted from a `FloatingWindow` positioned via a fixed WM-side `topright 14 48` windowrule to a `PopupWindow` anchored to `ControlCenterState.barItem`** as part of this fix - a fixed screen-corner offset had no way to follow the bar to the bottom at all, windowrule or not, so this needed the same anchor-based treatment Settings/Calendar already had rather than a smaller tweak. The `windowrule=float,title:^Control Center$` / `windowrule=topright 14 48,...` pair is gone from `zaris.conf` accordingly (both shipped and live), replaced with an explanatory comment matching Settings' own precedent.

  **Settings shows every tab's content statically now, per explicit user request** (found during the same testing pass - a scrolling settings window interacting with a popup that also needs to stay fully on-screen above a bottom-positioned bar was flagged as a likely source of future hiccups). The per-module Enabled/Screens/In-tray table (16 rows, by far the tallest content of any tab) moved out of the "Bar" tab into a new dedicated "Modules" tab, leaving "Bar" with just the opacity/height sliders - exactly the "split into more tabs if it gets too tall" fallback the user suggested. `implicitHeight` raised 720→900 to comfortably fit the now-tallest tab (Modules) with real margin to spare on any actual monitor, confirmed empirically rather than assumed. Also added the two tabs requested at the very start of this pass: **"General"** (currently "About this system" - OS/hostname/uptime via the existing `HostService` - explicitly reserved for future non-bar-specific settings per the user's own answer when asked what should live there, rather than left as a true empty placeholder) and **"Layout"** (the `BarConfig.layoutMode` Status Bar/Taskbar toggle plus the shared `BarConfig.position` Top/Bottom toggle, with explanatory text for whichever mode is currently active).

  Verified in a Xephyr sandbox against a real running qs instance across the full matrix: both layout modes × both bar positions, confirming in each combination that the bar itself renders correctly, Control Center/Settings/the calendar flyout/the taskbar-mode launcher all open on the correct side of the bar (attached above it when at the bottom, confirmed via real screenshots, not just code inspection - this is exactly the scenario the user's own bug report described), every one of Settings' six tabs (including the 16-row Modules table) renders fully with zero scrolling needed, live `BarConfig`/`DockConfig` JSON edits hot-apply with no `qs` restart (mode/position switches confirmed via direct file writes simulating what the Settings GUI itself does), the embedded taskbar-mode dock strip's click-to-activate and right-click-to-pin both work identically to the standalone dock's own icons, and the standalone dock's own windows stay correctly suppressed while taskbar mode is active even with `DockConfig.enabled` true. Full-file Python codepoint scan and `qmllint` run clean on every touched/new file before deploy. Diffed every file against the live config first (all diffs the intended changes only) and deployed live; confirmed via a clean "Configuration Loaded" log and a live bar screenshot that the default (statusbar/top) layout renders with zero regressions - live interactive click verification beyond that was cut short by an unrelated pre-existing input-routing quirk with a fullscreen browser window on the reference machine (the browser appears to intercept pointer input ahead of the bar in that specific state), not something introduced by this pass; the Xephyr sandbox's exhaustive matrix testing already covers every actual behavior change directly.

- **[Done]** Custom color theming: a user-editable 5-color palette (Primary/Secondary/Tertiary/Text/Background) plus a window border accent color, five built-in presets, and Settings' new "Colors" tab - the first item off the "eventually" list the user raised while building the dock's own background color/opacity.

  **`ThemeConfig.qml`** (new singleton) spans two genuinely different config surfaces: `~/.config/quickshell/colors.json` (JSON, same FileView+JsonAdapter pattern as every other `*Config.qml`) for the five shell-side roles, and `~/.config/zaris/zaris.conf`'s own `col.active_border=0xAARRGGBB` line (plain text, read/written via `FileView.text()`/`setText()` - the same mechanism `HostService.qml` already uses to read `/etc/os-release`) for the WM-level border color, confirmed to already hot-reload live (`ConfigManager::tick()` polls the file's mtime) with one caveat: an already-focused window's border only actually repaints on its *next* focus change, since `setEffectiveBorderColor` is only called from focus-change handling, not from config-reload itself. The border-color write uses a regex that touches only that one line, preserving its alpha byte and every other line/comment in the file untouched - confirmed via a direct diff against a backup before/after a real write.

  **`Colors.qml`** now sources `bg`/`text`/`blue`/`teal`/`coral` from `ThemeConfig` instead of hardcoding them - every one of the ~50 files already referencing `Colors.bg`/`Colors.text`/`Colors.blue`/etc. (including `mPrimary`/`mSecondary`/`mTertiary`, which already aliased `blue`/`teal`/`coral`) picked up theming with no changes needed anywhere else. `pill`/`pillActive`/`textMuted`/`purple`/`red` deliberately stay fixed for this first pass (structural surface tones and the error/danger red, not part of the "4-5 custom colors" ask) - see the new "per-module color overrides" backlog item below for a possible future extension the user already flagged.

  **A real, separately-found bug**: `Bar.qml`'s own background `Rectangle` had a hardcoded literal RGB value that only ever *approximated* `Colors.bg` by convention (a leftover from before theming existed) rather than actually binding to it - confirmed live that applying a preset changed every other `Colors.bg` consumer (Settings, Control Center, the dock, the launcher) but silently left the bar's own background exactly where it was. Caught by the user asking "will changing the palette also change the color of the bar?" rather than assumed - fixed by computing the bar's background from the real `Colors.bg` (via `Qt.color()` + `Qt.rgba()` for the opacity blend, the same pattern `Dock.qml`'s own `dockBg` already uses) instead of the stale literal.

  **A second real bug, this one non-deterministic and harder to pin down**: applying a preset (five colorsFile fields, one right after another in a single synchronous function) silently dropped some of those five values, keeping their old setting instead - confirmed repeatedly live, and confirmed it wasn't just a stale-QML-cache testing artifact via a controlled A/B rerun with the cache cleared both times. Root cause: each individual `setXxx()` call's own `onAdapterUpdated: writeAdapter()` writes the file, which can re-trigger that same `FileView`'s `watchChanges`/`onFileChanged: reload()` before the *next* setter in the sequence has actually run - and that reload can stomp an in-flight property change with whatever was still on disk a moment earlier. A first attempted fix (toggling `watchChanges` off for the whole batch, writing once at the end) looked like it worked in one test but dropped fields again on a repeat run - the race window was narrowed, not closed. The actual fix spaces each of the five setters (plus the border accent write) out onto its own 60ms `Timer` tick instead of firing them all in the same JS execution, giving each write+reload cycle time to fully settle before the next property changes - confirmed fixing it live across many repeated preset applications in a row with zero dropped fields, unlike the `watchChanges` approach.

  Five built-in presets (Zaris Default - today's exact shipped values, a genuine reset rather than an approximation; Nord; Dracula; Catppuccin Mocha; Gruvbox Dark), each a one-click swatch card in the new Colors tab (a `Flow`, not a `Row` - the fifth card silently ran off the edge of the window with no wrapping until caught in testing and fixed) plus six individual swatch+hex rows (the same swatch+`NTextInput` pattern the dock's own background color already established) for hand-tuning any one color after a preset, or from scratch.

  Verified in a Xephyr sandbox against a real running qs instance: all five presets apply correctly and repeatably (re-tested back-to-back, including the exact race-condition regression test described above), individual hex edits work (unaffected by the batch-write bug, since editing one field is exactly the single-setter case that was never actually at risk), Control Center's background/text/icons/gauges/scrubber all visibly follow a non-default palette (confirmed live per explicit user request to check this specifically - Control Center already used `Colors.bg`/`Colors.text` directly, so no code change was needed there), and the bar's own background now correctly follows too post-fix. The live `zaris.conf` border-color write was tested against the real file with a byte-for-byte diff confirming only the intended line changed, then restored to its exact original content afterward. Full-file Python codepoint scan and `qmllint` run clean on every touched/new file before deploy. Diffed every file against the live config first (all diffs the intended changes only) and deployed live; confirmed via a clean "Configuration Loaded" log and a live bar screenshot that the default palette renders identically to before this pass (no regression).

- **[Done]** Kernel module off by default in the bar (moved to a dedicated Control Center section instead), plus user-configurable launcher/Control Center bar icons.

  **Kernel version relocated, not removed.** `ModulesConfig.qml`'s `kernel` entry defaults to `enabled: false` now (both the code-level `JsonAdapter` default and the actual shipped `shell/quickshell/modules.json` template, which explicitly listed `"enabled": true` and would otherwise have overridden the code default for any real install) - still a completely real, re-enableable bar module via Settings' Modules tab, exactly like before. In its place, `ControlCenter.qml` gained a small, always-visible "Running Kernel" row directly below the profile header - a second, independent `KernelVersion.qml` instance (its own one-shot `uname -r` `Process`) rather than sharing the bar's, the same "duplicate the self-contained module" approach this file already uses for its hidden `cpuSource`/`cpuTempSource`/`gpuTempSource` instances above. Deliberately not gated by `ModulesConfig.showInTray(...)` the way most other Control Center rows are - a fixed, permanent panel feature instead, so the kernel version stays visible somewhere out of the box even with the bar module fully disabled. Recolored to `Colors.text` (this panel's own neutral, themeable text color) rather than the bar's own accent-`textColor` prop, so it follows a palette change in Control Center like everything else there, per explicit request.

  **Configurable bar icons**: two new `BarConfig.qml` fields, `launcherIcon`/`controlCenterIcon` (absolute file paths, falling back to the original `artix.svg`/`zaris-logo-square.png` assets when unset) - not limited to the bundled assets, any image file works. `Bar.qml`'s two launcher-icon `Image`s (statusbar and taskbar layouts) and `BarControlCenterLauncher.qml` now read from these instead of a hardcoded asset path. New "Icons" section in Settings' Bar tab: a small live preview swatch plus a hand-editable path field for each, matching the dock's own swatch+`NTextInput` color-picker convention - caught and fixed a real layout bug in testing where a fixed `320px` input width ran both fields off the right edge of the window, replaced with a computed `row.width - 170 - 24 - 24` so they fit exactly regardless of window width.

  Verified in a Xephyr sandbox: the kernel module correctly disappears from the bar with the new default, Control Center's new "Running Kernel" row renders directly under the profile header with the expected icon+version and correct theme-following color, and writing a custom `launcherIcon` path (simulating what the Settings field's setter does) correctly swapped the bar's rendered icon live. Full-file Python codepoint scan and `qmllint` run clean on every touched file. Diffed every file against the live config first (all diffs the intended changes only, including a direct, deliberate edit to the live `~/.config/quickshell/modules.json`'s already-persisted `kernel.enabled` - a JSON default-value change alone doesn't retroactively affect a file that already has the key written) and deployed live; confirmed via a clean "Configuration Loaded" log and a live bar screenshot that the kernel version is gone from the bar with no other regressions.

- **[Done]** A new "Defaults" settings tab (Icon Theme, Font Family, Font Size, Default Web Browser), and the real bug behind it: Dock/Launcher icons not matching the rest of the desktop's icon theme.

  **Root cause, confirmed live rather than assumed**: `Quickshell.iconPath()` (what `DockIcons.qml`/`LauncherContent.qml` both call to resolve app icons) has no per-call theme parameter at all - it resolves against whatever Qt's own current icon theme is, which for a Qt app launched with `QT_QPA_PLATFORMTHEME=qt6ct` (this shell, already set at `qs` launch for native tray-menu theming) means qt6ct's own `icon_theme=` setting. Checked directly: `~/.config/qt6ct/qt6ct.conf`'s `icon_theme` was simply blank, while GTK apps on the same machine were already using `Papirus-Dark` (`gtk-icon-theme-name` in both `gtk-3.0`/`gtk-4.0` `settings.ini`) - Quickshell had no real theme to resolve against at all, independent of anything GTK apps were doing. Fixed directly on the reference machine (`icon_theme=Papirus-Dark`, matching what GTK already had) and made user-controllable going forward via the new Icon Theme dropdown.

  **`DefaultsConfig.qml`** (new singleton) spans several genuinely different config surfaces at once, more than any single `*Config.qml` so far: `~/.config/quickshell/defaults.json` for Zaris's own remembered choice (same FileView+JsonAdapter pattern as everything else), but the actual *application* of each setting reaches outside Quickshell entirely - icon theme writes `icon_theme=` into `qt6ct.conf` and `gtk-icon-theme-name=` into both GTK `settings.ini` files (plain `key=value` line replacement via `FileView.text()`/`setText()`, the exact mechanism `ThemeConfig.qml` already established for `zaris.conf`'s border color, generalized here into one shared `_setIniKey()` helper used by every write in this file); font family the same way via `gtk-font-name=` (deliberately *not* written to qt6ct's own font setting - Qt stores that as a fully packed, multi-field serialized `QFont` string with no safe way to hand-construct one, a real and accepted scope boundary rather than risking a malformed qt6ct.conf); default browser via the standard `xdg-settings set default-web-browser` CLI, not a Zaris-specific mechanism at all.

  **Enumeration**: icon themes are found by scanning `/usr/share/icons/*/`, `~/.local/share/icons/*/`, and `~/.icons/*/` for a real `index.theme` (excluding `Hidden=true` entries like the spec's own `hicolor` fallback, and - a real filtering bug caught in testing - excluding anything *without* a `Directories=` key, which excludes cursor themes like Bibata that have a minimal `index.theme` of their own but aren't icon themes at all and would otherwise show up as a bogus dropdown entry). Fonts come from `fc-list : family`, deduplicated. Browsers reuse `DesktopEntries.applications.values` (already available to the launcher) filtered by `categories.indexOf("WebBrowser")`, rather than a separate scan.

  **Font size** is one base value (default 11, matching the shell's own previous hardcoded `Style.fontSizeM` and GTK's own current default) that scales every one of `Style.qml`'s font size tokens proportionally via a new `DefaultsConfig.fontScale` - confirmed live that moving the slider instantly resizes every bit of text across the entire open Settings window, no restart needed (unlike icon theme/font family, this only ever touches Zaris's own live QML state, never an external config file).

  **A real, load-bearing distinction found and documented, not just assumed**: Settings' "Reload Shell UI" button (`DefaultsConfig.restartShell()`, `Quickshell.reload(true)`) only does a hard *QML* reload - confirmed via `ps` that the process id never changes when it's used. That's genuinely useful on its own (every `FileView` re-reads fresh from disk), but it does **not** make icon theme or GTK font changes visible, since Qt's own icon theme is resolved once at `QGuiApplication` construction and a QML-level reload never touches that - confirmed by testing both ways side by side (a plain reload showed no icon change; killing and relaunching `qs` did). The button and its surrounding Settings text were corrected mid-pass to say so honestly rather than implying a one-click fix that doesn't actually exist.

  Verified in a Xephyr sandbox: the Icon Theme dropdown lists exactly the five real installed icon themes (Breeze, Breeze Dark, Papirus, Papirus-Dark, Papirus-Light) with no cursor-theme or fallback-theme noise; selecting one writes all three files correctly (confirmed via byte-for-byte diffs against backups that only the intended line changed in each); the Default Browser dropdown correctly lists Brave/Zen Browser/Chromium, and switching between two of them and back was confirmed via `xdg-settings get default-web-browser` actually changing and reverting; Font Size scaling is instant and live; and - the real end-to-end test - killing and relaunching the sandboxed `qs` process after setting Papirus-Dark showed genuinely different, theme-correct icons in the Launcher's result list, not just correctly-written config files. Full-file Python codepoint scan and `qmllint` run clean on every touched/new file. Diffed every file against the live config first (all diffs the intended changes only) and deployed live, including applying the actual fix (`icon_theme=Papirus-Dark`) to the reference machine's real `qt6ct.conf` with a byte-for-byte diff confirming only that one line changed, followed by a real live `qs` restart (not just a QML reload, per the finding above) and a clean bar screenshot confirming no regressions.

- **[Done]** Cursor theme and cursor size added to the Defaults tab, a direct follow-on from the icon theme work above - the user's own reaction to that pass ("thanks for mentioning cursor theme, let's add that too").

  **Discovered a third config surface for this one, not two**: cursor theme/size turned out to already be wired into *three* places on the reference machine, not the two (qt6ct + GTK) icon theme needed - checked live with `xrdb -query` before assuming anything, rather than guessing from icon theme's own pattern. `~/.icons/default/index.theme`'s `Inherits=` line (the actual libXcursor default-theme pointer - already present, pointing at Bibata-Modern-Ice) and both GTK `settings.ini`'s `gtk-cursor-theme-name=`/`gtk-cursor-theme-size=` were expected, matching icon theme's own precedent - but `~/.Xresources`'s own `Xcursor.theme:`/`Xcursor.size:` lines were also already present and already loaded into the live X server, confirmed the same way. Xresources uses "Key: value" (colon-space) rather than every other file's "key=value", so it got its own `_setXresourceKey` helper rather than reusing `_setIniKey` with a swapped separator - and every write there is followed by a real `xrdb -merge` to push it into the live X resource database immediately (confirmed via `xrdb -query` before/after), which is genuinely instant at that layer even though - same honest caveat as icon theme - this WM's own cursor and any already-running GTK/Qt app still won't visually update until they're actually restarted.

  **Cursor theme enumeration needed different filtering logic than icon theme's**, not just the same scan reused: a theme can ship icons, cursors, both, or - Bibata specifically - cursors only, and `index.theme`'s own fields (`Directories=`, `Hidden=`) don't reliably tell which is which for cursors the way they do for icons. The one reliable signal, confirmed by checking real theme directories directly rather than guessing: whether the theme folder actually has a `cursors/` subdirectory - Adwaita and Bibata-Modern-Ice both do, none of the pure-icon themes (Breeze, Papirus family) do, cleanly separating the two dropdowns without any single theme showing up wrongly in both.

  **Cursor size** is one slider (16-48px range, default 24 matching this machine's own existing value) writing to all three surfaces above at once (Xresources, both GTK configs) with no shell-side scaling involved - unlike font size, there's no Zaris-internal cursor rendering to scale, this is purely a pass-through setting.

  Verified in a Xephyr sandbox: the Cursor Theme dropdown lists exactly the two real cursor themes with no icon-only themes bleeding in; selecting a theme and a size were each confirmed via byte-for-byte diffs against backups of all four files (index.theme, both GTK configs, Xresources) showing only the intended line changed in each; `xrdb -query` before and after confirmed the X server's own live resource database actually updated, not just the file on disk. Full-file Python codepoint scan and `qmllint` run clean. Diffed against live config (all diffs intended) and deployed live with a clean reload and bar screenshot confirming no regressions - the reference machine's own cursor theme/size were left exactly as they already were (Bibata-Modern-Ice, 24px), since that already matched what every file had before this pass touched anything.

- **[Done, later partly reverted - see below]** Weather "hide location" toggle, weather added to the calendar flyout, and a new Calendar section in Control Center - all three explicit requests, plus a real layout bug found and fixed while building the third one.

  **Extracted two reusable components** rather than duplicating Control Center's existing weather block and calendar-flyout logic a second time each: `WeatherWidget.qml` (icon/temperature/condition/hi-lo, pulled out of `ControlCenter.qml` verbatim) and `CalendarWidget.qml` (the month-grid nav/day-grid/"Today" logic, pulled out of `CalendarFlyout.qml`). Both now used in two places each - Control Center keeps its original instances (now componentized), and `CalendarFlyout.qml` gained a `WeatherWidget` of its own underneath its `CalendarWidget`. Each embedding gets its own independent state where that matters (`CalendarWidget`'s "which month is currently being viewed" is a per-view concern, not global data) - the same "duplicate the self-contained piece, don't share one instance" approach already established in `ControlCenter.qml` for `cpuSource`/`cpuTempSource`/`gpuTempSource`.

  **`WeatherService.qml`** gained a `hideLocation` boolean (`weather.json`, same FileView+JsonAdapter pattern as its existing `manualLocationQuery`) plus a new "Hide location" toggle in Settings (originally the Profile tab, later moved - see below). `WeatherWidget.qml` respects it directly - the temperature/condition/hi-lo line still shows, just not the city name - so both places weather now appears (Control Center and the calendar flyout) hide it consistently from one shared setting, not two separate ones that could drift apart.

  **`CalendarFlyout.qml`** widened 260→300px to comfortably fit the weather row now living under the calendar grid, with a subtle divider line between the two sections; `implicitHeight` stayed computed from `body.implicitHeight` as before, so it grew automatically with no hardcoded value to update. This part of the change stands unchanged - `CalendarWidget`/`WeatherWidget` together in the clock's flyout was never reconsidered, only Control Center's own Calendar section (immediately below).

  **A real, found-not-assumed layout bug, later made moot by reverting the change that caused it**: adding a full `CalendarWidget` (plus `WeatherWidget`) to Control Center's fixed-size content made the panel's true content taller than would fit under even a real 1080px monitor's bar at any reasonable fixed size (~1150px needed vs. ~1026px available) - confirmed by testing at exactly that number first and watching the calendar/weather silently clip off the bottom edge with no visual indication anything was missing. Fixed at the time by wrapping Control Center's content `Column` in a real `NScrollView` (the same component `Settings.qml` already uses) and bumping `implicitHeight` 830→900. **Reconsidered by the user on sight** ("I don't want users to have to scroll on the Control Center") - see the dedicated entry below for the revert; Control Center never actually shipped a scrolling state to real users, this was caught between building it and the next session picking it back up.

  Verified in a Xephyr sandbox: the calendar flyout renders its grid and weather section together correctly with the new divider; Control Center's new Calendar section renders below the media/gauges cluster and is reachable via real scroll-wheel input (simulated via `XTestFakeButtonEvent` buttons 4/5, confirmed the content underneath - the rest of the day grid and the weather row - actually scrolls into view, not just present-but-clipped); the "Hide location" toggle was tested end-to-end against the live `weather.json` (toggled on, confirmed the city name disappeared from Control Center's weather row while temperature/condition/hi-lo stayed, then reverted the live file to its exact prior state). Full-file Python codepoint scan and `qmllint` run clean on every touched/new file (`qmllint` on the restructured `ControlCenter.qml` came back completely clean, confirming the added `NScrollView` nesting didn't introduce any brace/structure mistakes). Diffed every file against the live config first (all diffs the intended changes only) and deployed live; confirmed via a clean "Configuration Loaded" log and a live bar screenshot that nothing regressed.

- **Control Center: removed the Calendar section, fixed two "off-center" layout bugs, and moved Weather settings to their own tab** - four explicit follow-up requests after seeing the Calendar section (immediately above) live.

  **Calendar section removed entirely** from `ControlCenter.qml` per explicit reconsideration: "I don't want users to have to scroll on the Control Center" - stated as a standing design rule, not just a one-off preference for this feature. The `NScrollView` wrapper added to accommodate it was reverted too (`content` Column back to its original `anchors.top`/`anchors.horizontalCenter` form, no explicit width), and `implicitHeight` reverted 900→830, its original fixed size. `CalendarWidget.qml` itself wasn't touched - `CalendarFlyout.qml` (under the bar's clock) still uses it, and stays that way; only Control Center's own second instance is gone. Weather stays in Control Center - only the Calendar section was in question.

  **Two real, found-not-assumed layout bugs**, both traced by actually reading through `ControlCenter.qml` rather than guessing at a fix from the description:

  1. *"Widget buttons smushed to one side"* - both toggle-tile `Grid`s (the Stay Awake/Power Profile/DND/etc. cluster, and the Wallpaper/Screenshot pair) had `width: root.contentWidth` (380px) set directly on the `Grid` itself, but a `Grid` lays its children out at their natural size from its own x origin and ignores an explicit width wider than that - two 150px tiles plus 10px spacing is only 310px, so the extra 70px sat as dead space on the right rather than centering the tiles, reading as the whole cluster being pushed left. Fixed by wrapping each `Grid` in a plain `Item` (`width: root.contentWidth`, `height: <grid>.implicitHeight`) and centering the `Grid` itself inside that `Item` via `anchors.horizontalCenter` - `Item` isn't a positioner, so unlike a `Column` it doesn't forbid anchoring its child.

  2. *"Settings/audio buttons off-center"* - not a bug in the buttons themselves (`NIconButton`'s internal glyph centering, `Style.pixelAlignCenter`, checked out fine) but a side effect of the `NScrollView` from the Calendar-section change (item 1 above): `NScrollView.qml`'s `rightPadding` reserves extra space on the right whenever content overflows (`reserveScrollbarSpace`), which shifted the whole horizontally-centered content column a few pixels left of the panel's true center - visible on every row, not just the toggle grid, which is what read as "off-center" on already-correctly-coded rows like the header's Settings/power/close buttons. Resolved as a side effect of reverting the `NScrollView` entirely (previous paragraph), not a separate fix.

  **Weather settings moved to their own "Weather" tab** in `Settings.qml`: the "Weather location" field, "Hide location" toggle, and its explanatory text moved out of the Profile category's content `Column` into a new one gated on `activeCategory === "weather"`, with a new `{ id: "weather", label: "Weather", icon: "" }` entry (Nerd Font `f0c2`, a generic cloud glyph, confirmed present in the installed font via `fontTools` before use) inserted into the sidebar between Profile and Bar. Profile now holds only the Display Name field.

  Verified in a Xephyr sandbox against the real current source (not the possibly-stale live deployed copy): screenshotted Control Center after the fix and confirmed the toggle-tile grid is now genuinely centered under the 380px content width with no leftover Calendar section; confirmed via the Settings window that a new "Weather" entry with the cloud icon appears in the sidebar between Profile and Bar, clicking it shows the relocated location field/toggle (and correctly resolved a real IP-based location, "Fuquay-Varina, North Carolina", confirming `WeatherService` still works from its new home), and Profile now shows only Display Name. Full-file Python codepoint scan and `qmllint` run clean on both touched files. Diffed both against the live deployed copies first (each diff matched the intended change set exactly, nothing stray) and deployed live; confirmed via a clean "Configuration Loaded" log and cropped screenshots of the live bar on all three monitors that nothing regressed (a first full-screen screenshot made the 44px bar look like it had vanished entirely - false alarm, it was just compressed to near-invisibility against 1920px of monitor height in the scaled-down image; a targeted top-strip crop confirmed it was rendering correctly the whole time).

- **Settings and Control Center now stay always-on-top of other windows, except fullscreen ones (games).** A genuine WM-side gap, not a Quickshell/QML issue: `Settings.qml`/`ControlCenter.qml` are built on `PopupWindow`, which creates a real X11 **override-redirect** window - confirmed live via `xprop`/a small ctypes `XQueryTree` probe, not assumed. Override-redirect windows bypass `SubstructureRedirect` entirely (no `MapRequest` ever reaches the WM for them), so they were never tracked in the WM's own `windows` vector, and nothing ever re-raised them once something else (a newly tiled/mapped window, confirmed live: a fresh `alacritty` window fully covered an already-open Control Center) took the top of the X11 stacking order instead.

  Also confirmed live via `xprop` that there's no way to distinguish specifically Settings/Control Center from Quickshell's *other* `PopupWindow`-based popups (the calendar flyout, the taskbar-mode launcher, tooltips) at the X11 level - every one of them reports identical `override_redirect=1`, the same `_NET_WM_NAME` ("quickshell", Quickshell's own app identity, not a per-window title - `PopupWindow` doesn't expose a title property at all), and the same `_NET_WM_WINDOW_TYPE_TOOLTIP` window type regardless of which QML file created it. Rather than chase an unreliable heuristic, every Quickshell override-redirect popup is now treated as always-on-top uniformly - a tooltip or the calendar flyout getting covered by a newly raised window would be just as real a bug as Settings/Control Center being covered, so this isn't overreach, just the only option X11 actually offers.

  New mechanism in `windowManager.cpp`/`events.cpp`: `Events::eventMapNotify` (a new `XCB_MAP_NOTIFY` case in the event switch - `XCB_EVENT_MASK_SUBSTRUCTURE_NOTIFY` was already selected on root, so these events were already arriving, just never handled) tracks any override-redirect, `_NET_WM_WINDOW_TYPE_TOOLTIP`, `_NET_WM_NAME == "quickshell"` window's XID in a new `alwaysOnTopWindows` vector. A new `CWindowManager::reassertAlwaysOnTop()`, called every event-loop tick from `handleEvent()` right after `refreshDirtyWindows()`, lazily prunes dead/unmapped entries (`xcb_get_window_attributes`) and raises every surviving one to the top of the X11 stack (`setAWindowTop()`, already existed) - **unless** the popup's own monitor (resolved from its geometry via the existing `getMonitorFromCoord()`) currently has a fullscreen window active (`CWorkspace::getHasFullscreenWindow()`, already existed and already correctly maintained - this fix only reads it, doesn't touch how it's set).

  One more real gap found while testing the fullscreen exception specifically: neither `toggleWindowFullscrenn()` nor `refreshDirtyWindows()`'s own fullscreen handling ever actually issued an XCB restack for the newly-fullscreened window - a window going fullscreen visually covers other *managed* windows simply by filling the monitor while already reasonably near the top of the stack from having just been focused/clicked, which was never true for an override-redirect popup that happened to already be open (confirmed live: with Control Center open, toggling an already-focused terminal to fullscreen via `SUPER+F` left Control Center sitting on top of the fullscreen window indefinitely - `reassertAlwaysOnTop()`'s new fullscreen check correctly stopped fighting to stay on top, but nothing had ever pushed it below in the first place). Fixed by having `toggleWindowFullscrenn()` issue one real `setAWindowTop()` raise for the newly-fullscreened window itself when entering fullscreen - XCB stacking order is global across all children of root regardless of override-redirect status, so this one raise puts it above any already-open always-on-top popup too, and `reassertAlwaysOnTop()` leaves it there afterward instead of immediately reclaiming the top spot on the very next tick.

  Verified against the real `zaris` binary (Debug build, isolated `$HOME`/`zarisd.conf`, same Xephyr-sandbox discipline as every other WM-binary fix here) end to end: a fresh `alacritty` window no longer covers an already-open Control Center (confirmed via screenshot, before/after); toggling that terminal fullscreen via a real simulated `SUPER+F` key chord (`XTestFakeKeyEvent`, a new small extension of this project's existing XTest test harness) correctly raises it above Control Center, confirmed both visually and via `XQueryTree`'s own stacking-order output; toggling back out of fullscreen correctly restores normal always-on-top behavior, confirmed by opening a second terminal afterward and watching Control Center stay above it. Clean rebuild of both the Debug and Release configs (only pre-existing warning categories, no new ones). Staged in `~/.local/bin/zaris` (safely swapped via `cp --remove-destination` while the real live WM process kept running unaffected) but **not yet active** - same no-live-reload limitation as every other WM-binary fix here, needs a full logout/login to take effect.

  **Unrelated operational note from this pass, not a code bug**: mid-session, the live desktop's screen went solid pink. Traced to two `qs -c /home/mike/.config/quickshell` processes having ended up running simultaneously against the live display (a leftover from an earlier restart in a prior session whose cleanup check apparently raced and missed the first instance) - two shells fighting over the same bar/Control Center windows, not anything the WM-side always-on-top change caused. Fixed by killing both and relaunching exactly one; confirmed via a fresh screenshot that the desktop rendered normally again.

- **Taskbar-mode launcher now grabs real keyboard focus when it spawns**, matching the statusbar-mode variant. Confirmed live via a small ctypes `XGetInputFocus` probe that the taskbar popup previously never received real X11 input focus at all (`PointerRoot`, not its own window ID) - the same override-redirect gap as the always-on-top fix above, but this time for focus instead of stacking: `remapFloatingWindow()`'s WM-driven `setFocusedWindow()` call, which is what makes the statusbar-mode `FloatingWindow` variant work, only ever runs for real managed windows, and `PopupWindow` never goes through that path.

  Two theories investigated and ruled out empirically before landing on the real fix, in the spirit of not guessing: a temporary `_NET_ACTIVE_WINDOW`-logging build confirmed Quickshell never sends that client message for these popups at all, so there was no existing request silently being dropped to just start honoring. The actual mechanism turned out to be `PopupWindow`'s own built-in `grabFocus` property (Quickshell's own Wayland-layer-shell-derived "keyboard interactivity" concept, exposed but never once used anywhere in this codebase before now) - confirmed live via `xprop` that setting it flips the popup's `_NET_WM_WINDOW_TYPE` from `_NET_WM_WINDOW_TYPE_TOOLTIP` to `_NET_WM_WINDOW_TYPE_NORMAL`, a real, deliberate, X11-visible signal. Set on `Launcher.qml`'s `launcherPopup` only - deliberately left off `Tooltip.qml`/`CalendarFlyout.qml` (also `PopupWindow`-based, also override-redirect), since those open on hover/click with no text entry, and grabbing real keyboard focus there would rip it away from whatever the user was actually typing into elsewhere just from a mouse passing over something.

  `Events::eventMapNotify` (the always-on-top fix's own new `XCB_MAP_NOTIFY` handler, above) now also checks for `_NET_WM_WINDOW_TYPE_NORMAL` alongside `_NET_WM_WINDOW_TYPE_TOOLTIP` - both count as "one of ours" for always-on-top tracking (so a `grabFocus`-opted-in popup doesn't silently fall out of that fix), and specifically the `NORMAL` one additionally gets a real `xcb_set_input_focus` call. Verified end-to-end against the real `zaris` binary in the same Xephyr sandbox: `XGetInputFocus` now returns the launcher popup's own window ID (not `PointerRoot`) the instant it opens, and a real simulated keystroke sequence (`XTestFakeKeyEvent`, typing "firefox") landed correctly in the search field and filtered the results list, confirmed via screenshot - genuine end-to-end proof, not just "focus looks assigned." Also re-confirmed the always-on-top fix still holds for this now-`NORMAL`-typed popup (a fresh terminal still doesn't cover it). Clean rebuild of both Debug and Release configs. Diffed `Launcher.qml` against the live deployed copy before shipping (diff matched intent exactly) and deployed live; confirmed via a cropped bar screenshot that nothing regressed. Staged in `~/.local/bin/zaris` alongside the always-on-top fix - same **not yet active** caveat, needs a full logout/login.

- **Settings' Launcher icon / Control Center icon text fields (and every other text field in Settings) can now actually be typed into.** Same root cause and same fix as the taskbar launcher's focus bug above - Settings is override-redirect too, and never had `grabFocus` set despite having real text entry throughout (icon paths, display name, weather location). Without it, `XGetInputFocus` sat at `PointerRoot` the whole time Settings was open, meaning keystrokes only actually reached it while the mouse pointer happened to still be sitting directly over the window - reproduced live exactly that way in a Xephyr sandbox: typed text landed fine with the (simulated) pointer frozen over the field, then vanished entirely, mid-edit, the instant the pointer moved elsewhere. That matches "it also does not let me type in those text boxes" precisely - a real user's mouse drifting slightly while typing a path is a completely ordinary thing to do, not an edge case. Fixed with the same `grabFocus: true` on `Settings.qml`'s root `PopupWindow`, unconditional here (unlike the launcher, which only sets it on its taskbar-mode variant) since every category has real text fields. Verified end-to-end: typed a full path into the Launcher icon field with the pointer deliberately moved off-window mid-edit, confirmed via screenshot it landed and stayed. Diffed against the live deployed copy before shipping (matched intent exactly) and deployed live; confirmed via a cropped bar screenshot that nothing regressed.

- **Fixed `NIconButton`'s icon glyphs still being visibly off-center** - a separate, still-open bug from the toggle-tile-grid centering fix already shipped (that one was the whole panel shifted by `NScrollView`'s padding; this one is about the glyph inside each individual circular button, confirmed by the user pointing at the Settings/power/close buttons and the audio mute buttons specifically, not the `NCircularGauge` stat dials). Root-caused with a throwaway `qs` test harness rendering a glyph's own tight content bounding box as a visible border directly (not guessed): several Nerd Font icon glyphs - the volume/speaker icon was the clearest offender - have real, uneven left/right bearing baked into the font itself, so centering the glyph's tight content box as one block (`NIconButton`'s old `x: Style.pixelAlignCenter(visualButton.width, width)`) still left visibly more empty space on one side of the rendered icon than the other, even though the *box* itself was correctly centered. Confirmed via the same harness that swapping to `anchors.fill: parent` + `NIcon`'s own already-declared (but previously inert, since `NIcon` had no real width/height to center within) `horizontalAlignment: Text.AlignHCenter`/`verticalAlignment: Text.AlignVCenter` - i.e. letting Qt's real text layout do the centering, which accounts for a glyph's per-character advance width correctly rather than treating it as one opaque block - renders visibly more centered for exactly that glyph. Verified against real Control Center buttons afterward (not just the isolated test glyphs): the header gear/power/close buttons and the media card's prev/pause/next buttons all render with visibly symmetric icon margins now. Diffed `NIconButton.qml` against the live deployed copy before shipping (matched intent exactly) and deployed live; confirmed via a cropped bar screenshot that nothing regressed.

- **Notification history panel now anchors under the bar's bell icon** (right below it in statusbar-top mode, right above it in taskbar-mode with the bar at the bottom) instead of always centering on screen. It was still on the older `FloatingWindow` + WM `float`+`center` windowrule pattern (`title:^Notifications$`) - the same pattern Bluetooth/Clipboard History/Wallpaper Picker/the avatar picker still use today, none of which were touched here since only Notifications was actually requested. Converted to `PopupWindow` matching Settings/Control Center/the calendar flyout/the taskbar launcher: `NotificationHistoryPanelState.qml` gained an `anchorItem` property (same "retarget per click" pattern `LauncherState`/`ControlCenterState` already use), `NotificationIndicator.qml`'s click handler sets it to itself before toggling visible, and the panel now uses `anchor.item`/`anchor.rect.x` (centered under the icon, same as `CalendarFlyout` centers under the clock - a small tray icon opening a much wider panel isn't a good fit for the taskbar launcher's left-aligned convention) and `BarConfig.popupAnchorY()` for the y-offset. The now-unused `windowrule=float,title:^Notifications$`/`windowrule=center,title:^Notifications$` lines were removed from both the shipped `zaris.conf` template and the live deployed one (diffed first, live copy's own personal divergences elsewhere confirmed untouched).

  Verified in a Xephyr sandbox in both directions: statusbar-mode with the bar at the top opens the panel flush below the bell icon (confirmed via screenshot, real notification history data - a genuine earlier `notify-send` from this session - rendering correctly); taskbar-mode with the bar moved to the bottom opens it flush above the bell icon instead (confirmed via screenshot and `XQueryTree`'s own geometry output - the panel's y position exactly matches the bar's top edge minus its own height, zero gap). Full-file Python codepoint scan and `qmllint` run clean on all three touched QML files (only pre-existing tolerated warning categories). Deployed live (QML files plus the live `zaris.conf` edit) and confirmed via a cropped bar screenshot that nothing regressed.

- **Confirmed the Control Center media card's art-thumbnail caveat really is a Chromium-as-Flatpak issue specifically**, per an explicit user question rather than left as an unconfirmed theory. `flatpak info --show-permissions org.chromium.Chromium` shows `filesystems=home;` plus a short specific-path allowlist - no `--filesystem=/tmp` (or a broader `host`/`host-os` grant) anywhere in it. Flatpak's bubblewrap sandbox gives every app a private, isolated `/tmp` by default unless a permission like that grants the real host one, so the `/tmp/.org.chromium.Chromium.<id>` path Chromium's MPRIS bridge advertises is genuinely valid *inside Chromium's own sandbox* but refers to nothing (or an unrelated file) on the real host filesystem, which is what `ControlCenter.qml`'s `Image` actually reads from (it isn't itself sandboxed). This corrects the original explanation on file (`ControlCenter.qml`'s own comment used to say Chromium "deletes the file almost immediately," implying a timing race) - it's not a race that happens to always lose, it's two different filesystems that only happen to share a path string. Still not fixable from this side either way (nothing changes what path Chromium's own MPRIS metadata reports), but a real non-Flatpak player (mpv, VLC, Spotify's native build, etc.) isn't affected, since there's no sandbox boundary for its own temp files to cross. Comment-only change in `ControlCenter.qml`, deployed live.

- **Colors tab: Bar color, separate from Background.** First item off the 2026-09-09 feature backlog. `ThemeConfig.qml` gained a `barBackground` field (`colors.json`), empty by default meaning "follow the regular Background color" - most users want the bar to just match everything else, so this is an override rather than a required sixth color. `Colors.qml` gained `barBg: ThemeConfig.barBackground || ThemeConfig.background`, and `Bar.qml` switched from `Colors.bg` to `Colors.barBg` for its own background specifically (every other panel/popup - Settings, Control Center, the calendar flyout, etc. - still uses `Colors.bg` directly, unaffected). `applyPreset()` now also resets `barBackground` back to empty as part of its existing write-queue, so switching presets gives a fully coherent look rather than leaving a stale custom bar color behind. New "Bar color" row in Settings' Colors tab, same label+swatch+hex-field pattern as the other five color rows, with a placeholder reading "Follows Background" when unset.

  Verified in a Xephyr sandbox: setting a distinct bar color (a bright green, `#007700`) changed only the bar's own background, confirmed via screenshot that Settings/Control Center/every other panel kept using the regular Background color unaffected; clearing the field back to empty and committing (matches every other color field's existing clear-button behavior - the "X" only clears the text, an Enter/blur after is what actually persists it) correctly reverted the bar to following Background again; applying a preset (Nord) while a custom bar color was set correctly reset it back to "Follows Background" too, confirmed via screenshot. Diffed all four touched files against the live deployed copies before shipping (each matched intent exactly) and deployed live; confirmed via a cropped bar screenshot that nothing regressed.

- **`NComboBox`'s dropdown popup no longer elide-truncates long item names** - specifically reported against Settings' Font Family dropdown, but this is a shared component used by every dropdown in Settings (Icon Theme, Cursor Theme, Default Browser, etc.), so the real fix went into `NComboBox.qml` itself rather than special-casing one instance. Root cause: `popup.implicitWidth` was hardcoded to `combo.width` (the *closed* box's own width), so any item name wider than that got silently elided in the open list - confirmed live, several real installed font names ("JetBrainsMono Nerd Font Mono", etc.) were cut short this way. Fixed by measuring the widest item's name at the popup's own font size (`TextMetrics`) and sizing the popup to fit it, capped at a `maximumPopupWidth` (420px) so one extreme outlier can't blow the popup off-screen.

  One remaining limitation, tried and not resolved rather than left unconfirmed: a combo box sitting close to its host window's right edge (Font Family's own position in Settings) can still have its single longest items clipped by the window's own fixed bounds, since the wider popup still opens flush with the combo's left edge and grows rightward past it. Attempted clamping the popup's `x` position via both `Overlay.overlay` (turned out to be `null` outside a real `ApplicationWindow` - Settings is a bare Quickshell `PopupWindow`) and the `Window` attached property (resolved without error but the clamp had no visible effect, live, repeatedly) - root cause not found, left as a known gap rather than shipping a positioning fix that doesn't actually do anything. The core ask - the popup no longer elides every item down to the closed box's width - is real and verified; only the single widest entries in a handful of right-positioned combo boxes are still affected. Verified live in a Xephyr sandbox (Settings' Font Family dropdown, scrolled to the JetBrains entries specifically, before/after screenshots). Diffed against the live deployed copy before shipping (matched intent exactly) and deployed live; confirmed via a cropped bar screenshot that nothing regressed.

- **Defaults tab: Default File Explorer, Text Editor, Image Viewer, Email client, and PDF viewer**, alongside the existing Default web browser. File Explorer/Text Editor/Email Client reuse the same `DesktopEntries.applications` approach the browser dropdown already used, filtered on real freedesktop.org `Categories=` values ("FileManager"/"TextEditor"/"Email" - confirmed present on this machine's own installed apps first: `thunar.desktop`, `code-oss.desktop`/`kate`/`vim`, Betterbird's Flatpak entry). Image Viewer/PDF viewer needed a different approach - checked Quickshell's own `DesktopEntry` qmltypes first rather than assuming, and it exposes no `MimeType=` data at all, and there's no dedicated freedesktop category for either "image viewer" or "PDF viewer" the way there is for the other three - so those two get their own `Process`-based scanner instead (same shape as the existing icon/cursor-theme scanners), grepping installed `.desktop` files' real `MimeType=` lines directly (confirmed working via a plain `grep -l` first, before writing the scanner around it).

  All six apply immediately and persist for real: `xdg-settings set default-web-browser` for the browser (unchanged, already existed) and `xdg-mime default <app>.desktop <mimetype>` for the other five (`inode/directory`, `text/plain`, `image/png image/jpeg image/gif image/webp image/bmp image/svg+xml`, `x-scheme-handler/mailto`, `application/pdf` respectively) - the standard, correct mechanism for exactly this "default app" concept beyond what `xdg-settings` itself covers (it only ever handled the browser case).

  Adding six full dropdown rows pushed the Defaults tab past Settings' fixed window height, confirmed live (the "Reload Shell UI" button and its explanatory text got clipped at the bottom) - fixed with a real space saving rather than just growing the window blindly: the six per-dropdown descriptions ("Applied immediately via xdg-mime - no restart needed.") were genuinely redundant, differing only in "xdg-settings" vs "xdg-mime" for the browser specifically, so consolidated into one shared note above the whole group instead of six repeated two-line blocks. `implicitHeight` still needed a bump after that (900 → 970), sized against the real worst-case ceiling for this window (a bar-position-aware popup opening upward above a bottom-positioned bar at its maximum configurable height, 96px, plus its 10px gap - 974px is the actual limit on a 1080px-tall monitor before that specific combination pushes the popup off-screen; 970 leaves a few px of margin under that), not picked arbitrarily.

  Verified end-to-end in a Xephyr sandbox, not just that it renders: opened the Default File Explorer dropdown and confirmed it correctly found the one real installed match ("Thunar File Manager"); selected it and confirmed via the sandbox's isolated `defaults.json` that `defaultFileExplorer: "thunar"` persisted; confirmed the real `xdg-mime` association actually took effect too, querying `mimeapps.list` directly afterward (`inode/directory=thunar.desktop`) - genuine proof the whole chain works, not just that the UI looks right. Also confirmed the Defaults tab now fits fully within the fixed window height with the "Reload Shell UI" button fully visible. Full-file Python codepoint scan and `qmllint` run clean on both touched files (only pre-existing tolerated warning categories). Diffed both against the live deployed copies before shipping (matched intent exactly) and deployed live; confirmed via a cropped bar screenshot that nothing regressed.

- **Defaults tab: Screenshot folder setting** (typed path; a Browse button came later - see the `FolderPickerPanel.qml` entry further down). New `DefaultsConfig.screenshotFolder` (defaults to `~/Pictures/Screenshots` when unset, matching the folder `screenshot.sh` already hardcoded). `screenshot.sh` gained a second CLI argument (folder path, falling back to the same default if omitted, so a direct/manual invocation still works unchanged) - `ControlCenter.qml`'s Screenshot tile now always passes `DefaultsConfig.screenshotFolder` explicitly.

  Adding this row (on top of the six Default-app dropdowns from the previous pass) pushed the Defaults tab's real content height well past Settings' fixed window size, confirmed live rather than assumed at every step: reclaimed real space first (trimmed this row's own description to one line, and reworded the long pre-existing icon/cursor/font restart-note paragraph shorter without losing any of its actual content) before growing `implicitHeight` at all, then grew it in small, re-verified increments (900 → 970 → 1000 → 1040) rather than guessing a final number - 1040 is a few px past the theoretical safe ceiling for the bar-position-aware-popup-opening-upward edge case (see the window's own updated comment for the exact math), accepted deliberately since that's a narrow combination and the overflow even then is small.

  Verified in a Xephyr sandbox: the field renders with the correct default placeholder/value, and the whole Defaults tab (all six dropdowns, the new field, the restart-note paragraph, and the "Reload Shell UI" button) fits fully on screen with nothing clipped, confirmed via screenshot at each height increment rather than just the final one. `screenshot.sh`'s own diff kept clean and minimal (one new optional argument, backward compatible). Diffed every touched file against the live deployed copies before shipping (all matched intent exactly) and deployed live (including re-`chmod +x`-ing the shipped script); confirmed via a cropped bar screenshot that nothing regressed.

- **Profile tab: profile picture changing.** Added a "Profile picture" text field (typed path) plus a folder-open Browse `NIconButton` to Settings' Profile tab, right under Display name. Browse doesn't open an in-app file picker - it launches the user's configured Default File Explorer (`DesktopEntries.byId(DefaultsConfig.defaultFileExplorer).execute()`, disabled with an explanatory tooltip if that Defaults-tab setting isn't set yet) - a deliberate sidestep of the `QtQuick.Dialogs.FileDialog` click-unresponsiveness blocker documented above, since this only needs to *launch* an app, not show a clickable in-process dialog. Typing a path and pressing Enter/blurring the field calls the same `AvatarPickerPanelState.setAvatar()` that the existing grid-picker panel already used.

  Refactored `setAvatar()`/its backing `cp`-then-version-bump `Process` out of `AvatarPickerPanel.qml` and into the shared `AvatarPickerPanelState.qml` singleton itself, so both surfaces (the grid picker and this new Settings field) call the exact same mechanism instead of two independent copies that could drift out of sync. Also removed the avatar's click-to-open-`AvatarPickerPanel` `MouseArea` and hover-border from `ControlCenter.qml` per the request - the Control Center avatar is now purely a display element, picture changing is a Settings-only affordance.

  The open question from when this was first noted - what pixel size/scaling approach looks best for a user-supplied image at Control Center's 44x44 avatar size - was deliberately left untouched (still a raw `Image` + `MultiEffect` circular mask at verbatim source resolution, no real downscaling/`sourceSize` tuning), since it's a design judgment call flagged as unresolved rather than something to guess at.

  Verified end-to-end in a Xephyr sandbox, not just that it renders: confirmed clicking the avatar no longer opens anything (fresh `xtree.py` dump showed only Control Center's own window, no picker FloatingWindow); clicked Browse and confirmed via a fresh `xtree.py` dump that it genuinely launched Thunar (a real top-level `Thunar/thunar`-class window appeared) with no QML errors in the qs log; typed a real image path into the field, pressed Enter, and confirmed `~/.face` was created and byte-identical to the source file (`cmp`); confirmed Control Center's avatar then actually displayed that new image after reopening it, not just that the file changed on disk. Also caught and fixed a real bug during this verification: the Browse button's folder-open glyph (``) was silently dropped by the edit tool exactly as the Nerd Font glyph workaround note warns about - caught via a Python codepoint scan before shipping, fixed via the established `chr(0xf07c)` injection script, then re-verified live via screenshot that the glyph actually renders (not blank/tofu). Full-file Python codepoint scan and `qmllint` clean on all four touched files (only pre-existing tolerated warning categories). Diffed every touched file against the live deployed copies before shipping (all matched intent exactly), deployed live, restarted the real `qs` process, re-applied `xset s off -dpms`, and confirmed via a cropped bar screenshot that nothing regressed.

- **Settings sidebar: move the Weather tab to the very bottom.** Pure reorder of `settingsWindow.categories` in `Settings.qml` - moved the `weather` entry from between Profile and Bar to after Dock, with no content/logic changes needed anywhere else, since every tab's content section is just a `visible: settingsWindow.activeCategory === "..."` binding independent of the array's order. (Nerd Font glyph icons in this array can't round-trip through a plain text-match edit tool - a Python line-splice script was used instead, preserving every entry's existing glyph bytes untouched rather than retyping them.) Verified in a Xephyr sandbox: Weather now renders last in the sidebar, every other tab kept its original relative order and still opens correctly (checked Bar specifically, since it's the default `activeCategory`), and Weather's own content (location field, hide-location toggle) still renders correctly at its new position. `qmllint` and a full-file codepoint scan both clean (same pre-existing tolerated warnings only). Diffed against the live deployed copy before shipping, deployed live, restarted the real `qs` process, re-applied `xset s off -dpms`, confirmed via a cropped bar screenshot that nothing regressed.

- **Settings: add a Date/Time tab** - a new `DateTimeConfig.qml` singleton (own `datetime.json`, same `FileView`+`JsonAdapter` pattern as every other `*Config.qml`) backs a new "Date/Time" category with three `NComboBox` rows: first day of the week (Sunday/Monday - drives `CalendarWidget.qml`'s month grid, which was previously a hardcoded `readonly property int firstDayOfWeek: 0`, shared by both Control Center's Calendar section and `CalendarFlyout.qml`'s popup since both already used the one `CalendarWidget.qml`), date format, and time format. Both format dropdowns are a fixed set of presets (e.g. "Wednesday September 9 2026" vs "09/09/2026", "15:57" vs "3:57 PM") rather than a free-form Qt date-format string field - keeps it foolproof rather than exposing full pattern syntax a typo could silently mis-render. `BarClockText.qml`'s clock text (previously a hardcoded `Qt.formatDateTime(clock.date, "dddd MMMM d yyyy HH:mm")`) now reads `DateTimeConfig.clockFormat` instead. Dropdown preview text is computed against today's actual date/time rather than a hand-typed example, so it can't go stale or read wrong for the user's actual locale.

  Caught and fixed a real bug during this work, not just a new-feature oversight: `NComboBox.qml`'s existing `widestItemWidth` (from the earlier popup-width fix) was a `readonly property var` binding that writes to a separate `TextMetrics` item's `.text` as a side effect of its own evaluation - a real QML anti-pattern that happened to never surface before because every prior caller's `model` (`DefaultsConfig`'s app-picker dropdowns) starts as an empty array and only fills in later once its `Process`-based scanner finishes, spreading the recomputation out over time. This tab's new dropdowns are the first ones with a plain synchronous array ready at construction time, and running live in a Xephyr sandbox surfaced 10-50 "Binding loop detected for property widestItemWidth" warnings per combo box in the qs log during Settings' initial layout pass - not a crash (Qt's own binding-loop detector caught and broke each one rather than infinite-looping), but real avoidable noise from a real fragility, fixed at the root rather than worked around per-callsite: `widestItemWidth` is now a plain `property real` recomputed imperatively (`_recomputeWidestItemWidth()`) on `onModelChanged`, `Component.onCompleted`, and a `Connections` hook on `Style.onFontSizeMChanged` (replacing the reactivity the old declarative form got for free from reading `Style.fontSizeM` directly). Re-verified in the sandbox after the fix: zero "Binding loop" lines in the log, and the live deploy's own log came back clean too.

  Verified end-to-end in a Xephyr sandbox: opened the new tab, confirmed the clock-icon glyph and all three dropdowns render with correct current-value pre-selection; opened the Time format dropdown and confirmed the 12-hour option's preview read "3:55 AM" (today's actual time, not a hardcoded example); selected it and confirmed `datetime.json` persisted `"timeFormat": "h:mm AP"` and the live bar clock actually re-rendered as "...2026 3:55 AM"; changed First day of the week to Monday, confirmed `datetime.json` persisted `firstDayOfWeek: 1`, then opened the bar's calendar flyout (not Control Center - Control Center's own Calendar section was already removed earlier this project to keep it scroll-free, see its own header comment - the shared `CalendarWidget.qml` lives in `CalendarFlyout.qml` now) and confirmed the day-of-week header row now reads "MO TU WE TH FR SA SU" with today (Wednesday the 9th) correctly falling under the "WE" column. `qmllint` and a full-file codepoint scan clean on all five touched/created files (only pre-existing tolerated warning categories - `Style`'s own `onFontSizeMChanged` connection included). Registered the new singleton in `qmldir`. Diffed every touched file against the live deployed copies before shipping, deployed live, restarted the real `qs` process, re-applied `xset s off -dpms`, and confirmed via a cropped bar screenshot that nothing regressed (live correctly still shows the 24-hour default, since the real user hasn't set a preference yet).

- **Settings sidebar reorder.** Layout kept exactly where it already sat; Colors, Bar, and Dock moved to immediately follow it. Everything else (General, Defaults, Profile, Date/Time, Modules, Weather) kept its rough prior relative order, just shifted after that new Layout/Colors/Bar/Dock block. Final order: General, Defaults, Layout, Colors, Bar, Dock, Profile, Date/Time, Modules, Weather. Same Python line-splice reorder technique as the Weather-to-bottom move (glyph bytes untouched). Verified in a Xephyr sandbox: sidebar renders in the exact target order, clicking into a tab still opens the correct content (checked Bar specifically). `qmllint`/codepoint scan clean (same pre-existing warning count as before this edit, 11). Diffed against the live deployed copy, deployed live, restarted `qs`, re-applied `xset s off -dpms`, confirmed via a cropped bar screenshot that nothing regressed.

- **Settings Audio tab + bar volume-icon popup mixer.** Both backlog items shipped together since they share one component: `AudioMixer.qml` (Volumes/Devices sub-tabs, real Pipewire data) is now embedded both in a new `AudioMixerPanel.qml` (a `PopupWindow` anchored under the bar's volume icon, same "opens attached to where you clicked" treatment as `NotificationHistoryPanel.qml` - see that file's own header comment for why that's the pattern for a popup a bar icon opens directly, as opposed to Bluetooth/Clipboard/Wallpaper/the avatar picker, which open from Control Center tiles and stay centered `FloatingWindow`s) and Settings' new "Audio" category. `VolumeControl.qml`'s left-click used to launch `pavucontrol` directly - now opens this popup instead; right-click's quick mute-toggle is unchanged.

  *Volumes* sub-tab: Output/Input sliders + mute (same `Pipewire.defaultAudioSink`/`defaultAudioSource` binding `ControlCenter.qml`'s own audio section already used, which still exists there separately and unaffected), plus a live "Applications" list - one slider per currently-active playback stream, appearing/disappearing in real time as apps start/stop playing audio, verified live by starting and killing a real `paplay` process mid-session and watching the row appear then vanish. *Devices* sub-tab: real output/input hardware device lists as `NRadioButton` rows (`autoExclusive: false`, since `checked` is a live derived binding off `Pipewire.defaultAudioSink`/`defaultAudioSource` rather than something the button should ever assign itself - a normal auto-exclusive `RadioButton` writing `checked` directly on click would sever that binding) - clicking one sets `Pipewire.preferredDefaultAudioSink`/`preferredDefaultAudioSource`, confirmed live via `pactl get-default-sink` that this is a genuine, real default-device switch, not just a UI-local selection (switched the live machine's real output to its HDMI device and back to verify, restoring the original default afterward since this talks to the actual system Pipewire daemon, sandboxed or not).

  Real-vs-stream node filtering (`isSink`/`isStream`/the `type` bitmask) isn't documented anywhere in Quickshell's own qmltypes beyond "opaque flags enum" - verified empirically instead of guessed, via a throwaway `qs -p` debug script dumping `Pipewire.nodes` live against this machine's real hardware (a USB headset + HDMI output) and a real `paplay` stream: a genuine output device reads `isSink=true isStream=false type=17`; its own mic reads `isSink=false isStream=false type=9`; an active playback stream reads `isSink=true isStream=true type=21`. `type & 1` (the shared "Audio" bit across all three) is enough to exclude this machine's own non-audio Pipewire nodes (Dummy-Driver/Freewheel-Driver/Midi-Bridge/a Bluetooth MIDI bridge, all `type=0`) from the device lists - `isSink`/`isStream` alone already separate device vs. stream.

  The popup's own gear button jumps to Settings' Audio tab and closes itself - needed a new `SettingsState.requestedCategory` mechanism (set right before opening, consumed and cleared by Settings.qml the moment it becomes visible) since nothing let an external caller open Settings to a specific tab before this. Caught and fixed a real bug while wiring this up: the obvious anchor choice, `SettingsState.targetItem = ControlCenterState.barItem` (what Control Center's own gear button uses), is only ever assigned lazily the first time Control Center's bar launcher icon is clicked (`BarControlCenterLauncher.qml`) - confirmed live that clicking the volume icon's gear button *without* ever having opened Control Center first left `ControlCenterState.barItem` null, which made Settings' own `visible: SettingsState.visible && !!SettingsState.targetItem` silently never show anything at all. Fixed by anchoring to the popup's own `AudioMixerPanelState.anchorItem` instead, which is guaranteed valid at that point (the popup couldn't be open otherwise) - confirmed live afterward that the jump now works reliably regardless of whether Control Center was ever opened first.

  `qmllint` and a full-file codepoint scan clean on every touched/created file (only pre-existing tolerated warning categories). Both new components needed real `qmldir` entries (a plain component, not just a singleton, needs one too - confirmed live via "AudioMixer is not a type" before adding it) and `AudioMixerPanel {}` needed adding to `shell.qml`'s composition root alongside the other bar-triggered panels, same as any of them. Diffed every touched/created file against the live deployed copies before shipping, deployed live, restarted the real `qs` process, re-applied `xset s off -dpms`, and confirmed via a cropped bar screenshot that nothing regressed.

- **5-day forecast in the Weather widget.** `WeatherService.qml`'s forecast request widened from `forecast_days=1` to `5` and gained `weather_code` in the `daily=` parameter list (it already had `temperature_2m_max`/`min` there) - genuinely just widening the existing Open-Meteo call, no second endpoint needed. New `forecastDays` array (`{label, weatherCode, highF, lowF}` per day, index 0 labeled "Today", the rest a short weekday name via `Qt.locale().dayName` - matching `CalendarWidget.qml`'s own day-of-week header convention rather than hand-rolling names). `WeatherWidget.qml`'s root changed from a `Row` to a `Column` wrapping the existing current-conditions `Row` plus a new forecast `Row` (5 `Repeater`-generated day cells: label, condition icon, hi/lo) underneath - visible only once `WeatherService.haveData` and real forecast data exists. This changed WeatherWidget's own contract: every embedder now needs to set `width:` explicitly (the forecast row divides that width by 5 for even cells) - Control Center's own embedding already did; `CalendarFlyout.qml`'s didn't, so that one gained `width: parent.width`.

  Verified live in a Xephyr sandbox: real API data rendered correctly in both embeddings (5 real day cells with real hi/lo temps and condition icons, not placeholder data), Control Center's fixed-height window (must never scroll, per standing constraint) had comfortable headroom left below the new row rather than clipping it, and the calendar flyout's window (which computes its own height from content, unlike Control Center) grew automatically to fit with no hardcoded size to update. `qmllint`/codepoint scan clean on all three touched files (the pre-existing Weather Icons glyph block, 0xe300-0xe3e3, is an already-established exclusion range in the codepoint scan, not new). Diffed every touched file against the live deployed copies before shipping, deployed live, restarted `qs`, re-applied `xset s off -dpms`, confirmed via a cropped bar screenshot that nothing regressed.

- **Settings: Wallpaper tab.** New "Wallpaper" category, entirely a Settings-side surface for `WallpaperService.qml`'s already-existing folder/rotation backend (originally built for `WallpaperPickerPanel.qml`'s own grid picker) - no new backend work needed, just exposing what was already there: a folder path field + Browse button (same Default-File-Explorer-launch pattern as the Profile-picture field, not an in-app dialog - see that entry's own writeup for why), a live "N image(s) found" count, a "Rotate automatically" toggle, a Rotation-interval dropdown (5/15/30/60/180 min presets, disabled when rotation is off), and an "Open Wallpaper Picker" button that opens the existing grid picker directly from Settings.

  Verified live in a Xephyr sandbox: typed a real folder path (containing 3 actual PNGs) into the field, confirmed on Enter that `wallpaper.json` persisted the new `directory` and the image count updated to "3 image(s) found" (a genuine rescan, not just text echoed back); toggled rotation on and confirmed both the interval dropdown became enabled and `wallpaper.json` persisted `rotationEnabled: true`; clicked "Open Wallpaper Picker" and confirmed the existing picker panel opened showing the exact same folder/rotation state and the same 3 real thumbnails - genuine shared state with `WallpaperPickerPanel.qml`, not a parallel/duplicated picker. `qmllint`/codepoint scan clean (only pre-existing tolerated warning categories). Diffed against the live deployed copy before shipping, deployed live, restarted `qs`, re-applied `xset s off -dpms`, confirmed via a cropped bar screenshot that nothing regressed.

  Note: this is distinct from the separate, still-open "Wallpaper Selector" backlog item below (an enhanced bar-anchored picker with an apply-to-all-monitors toggle, filter field, breadcrumb, and `GridView` badges, per its own reference screenshot) - the button added here just opens today's existing, simpler `WallpaperPickerPanel.qml`, not that richer picker.

- **Control Center: renamed "Network" to "Ethernet"; swapped Wifi/Bluetooth tile positions.** Pure label + tile-order change in `ControlCenter.qml`'s quick-toggle grid - the Network tile's own `NText` now reads "Ethernet" (nothing else about it changed: same `NetworkToggle`, same nmcli-backed logic), and the Bluetooth/Wifi `Rectangle` blocks swapped file positions so the grid now reads Ethernet/Wifi, Clipboard/Bluetooth instead of the old Ethernet/Bluetooth, Clipboard/Wifi - Wifi now sits next to Ethernet as requested. A plain-text Edit match failed on this file the same way it has before elsewhere (an invisible Nerd Font glyph a few lines above the actual target text differed byte-for-byte from what got typed into the tool call, even though both render identically blank) - worked around with a line-number-indexed Python splice instead of retyping any of the surrounding glyph-bearing lines. Verified live in a Xephyr sandbox via screenshot that the grid renders in the new order with the new label. `qmllint`/codepoint scan clean (zero warnings, not just the usual pre-existing set). Diffed against the live deployed copy before shipping, deployed live, restarted `qs`, re-applied `xset s off -dpms`, confirmed via a cropped bar screenshot that nothing regressed.

- **Control Center: Settings gear always opens to General.** One-line addition to the gear's `onClicked` - sets `SettingsState.requestedCategory = "general"` right before opening (the same mechanism `AudioMixerPanel.qml`'s own gear button introduced earlier), so this is General-only regardless of whatever tab Settings was last left on, distinct from Control Center icons that jump to their own specific tab. Verified live in a Xephyr sandbox: opened Settings via the gear, navigated to the Bar tab, closed it, reopened Control Center and clicked the gear again - confirmed it landed back on General rather than staying on Bar. `qmllint`/codepoint scan clean. Diffed against the live deployed copy before shipping, deployed live, restarted `qs`, re-applied `xset s off -dpms`, confirmed via a cropped bar screenshot that nothing regressed.

- **Control Center: "No Media Playing" placeholder.** The media card's `visible` was previously gated on `!!MediaService.currentPlayer` alone, so the whole card (and its reserved width in the Row it shares with the CPU/temp/battery gauge cluster) just vanished with nothing in its place whenever no player was active. Added a sibling `Column` (same 230px width, same 130px art-area height as the real card) gated on the exact inverse condition (`!MediaService.currentPlayer`, same tray flag) - a centered music-note icon + "No Media Playing" text, so exactly one of the two always shows whenever the mediaPlayer tray flag is on, with no layout jump between the two states. Verified live in a Xephyr sandbox (no MPRIS player running, matching this machine's real idle state) that the placeholder renders correctly in the media card's usual slot, sized consistently with the gauge cluster beside it. `qmllint`/codepoint scan clean. Diffed against the live deployed copy before shipping, deployed live, restarted `qs`, re-applied `xset s off -dpms`, confirmed via a cropped bar screenshot that nothing regressed.

- **Settings: Notifications, OSD, Night Light, Clipboard, and Battery tabs.** Five new categories, each surfacing a service that either already had readonly config properties with no setters, or (OSD) no persisted settings at all:
  - *Notifications*: a Do Not Disturb toggle and a "Clear All Notifications" button. The toggle logic (`dunstctl set-paused toggle`) moved from being local to `Dnd.qml` into a new `DndState.toggle()`, so both the bar icon and this new Settings row drive the exact same mechanism rather than duplicating it (`Dnd.qml` now just delegates). Per-app/per-type notification filtering is explicitly noted as not built yet - that's the separate, larger "surface every notification + ignore list" backlog item below, not attempted here.
  - *OSD*: a real, previously-nonexistent config surface - `OSDState.qml` had zero persisted settings before this (just transient show/hide trigger state), with the auto-hide delay a bare hardcoded `1500` on its own `Timer.interval`. Added an `osd.json`-backed `enabled`/`hideDelayMs`, both new: an "enabled" toggle (`show()` now no-ops when disabled - the volume/brightness keybinds still actually change the values, just no popup appears) and an auto-hide duration slider (0.5-5s).
  - *Night Light*: `NightLightService.qml` already had `scheduleEnabled`/`sunset`/`sunrise`/`nightTemp` config properties (readonly, hand-edit-`nightlight.json`-only per its own header comment) but genuinely no setter functions at all - added `setScheduleEnabled`/`setSunset`/`setSunrise`/`setNightTemp`, each re-running `applySchedule()` (or re-firing `redshift` directly for a live temperature change) so a Settings change takes effect immediately rather than waiting for the next scheduled boundary.
  - *Clipboard*: `maxEntries` was a hardcoded `readonly property int: 50` with no backing config file at all - added a small dedicated `clipboard-settings.json` (deliberately separate from the existing entries-storage file, which has different `watchChanges` semantics) so it's a genuine setting now, exposed as a 20/50/100/200-entry dropdown, plus a live entry-count readout, a "Clear History" button (`wipeAll()` already existed), and a red warning line (reusing `Colors.red`) when `clipnotify` isn't installed, since capture is silently inactive in that case.
  - *Battery*: `BatteryService.qml` already had `warningThreshold`/`criticalThreshold` config properties but no setters either - added `setWarningThreshold`/`setCriticalThreshold`. Tab shows live battery percentage/charging state plus both thresholds as sliders, with a "no battery detected" fallback for desktop systems.

  Verified every tab live in a Xephyr sandbox, not just that each renders: toggled Do Not Disturb and confirmed via `dunstctl is-paused` that the real dunst daemon state actually changed (then toggled back to restore it); triggered a real OSD popup via `qs ipc call osd volume ...` and confirmed it actually appeared with the right percentage, that raising the auto-hide slider kept it visible longer, and that turning "Show on-screen popup" off suppressed it entirely despite the IPC call still firing; toggled the Night Light schedule and dragged its color-temperature slider, confirming both persisted to `nightlight.json`; changed the Clipboard max-entries dropdown and confirmed `clipboard-settings.json` persisted the new value; dragged both Battery threshold sliders and confirmed `battery.json` persisted both. `qmllint`/codepoint scan clean across all seven touched/created files (only pre-existing tolerated warning categories). Diffed every touched file against the live deployed copies before shipping, deployed live, restarted `qs`, re-applied `xset s off -dpms`, confirmed via a cropped bar screenshot that nothing regressed.

- **Bar tab: Anchored vs. Floating mode - decided against, dropped.** Asked the user directly rather than guessing at the three implementation approaches this was blocked on (WM-side live windowrule updates, QML-side PopupWindow anchoring, or some other live-x/y mechanism) - decision was to skip a floating bar mode entirely and keep Anchored as the bar's only mode. Removed from the backlog rather than left paused.

- **Bar tab: Launcher/Control Center icon Browse buttons.** Asked the user directly how to resolve the paused `QtQuick.Dialogs.FileDialog` unclickability bug (documented above) rather than continuing to guess at its root cause - decided on a custom in-QML picker, the same approach `WallpaperPickerPanel.qml` already proved out for wallpaper selection. New `IconPickerPanel.qml`/`IconPickerPanelState.qml`: a `FloatingWindow` with a directory field (defaulting to `~/.config/quickshell/assets/`, this shell's own bundled icons) + Rescan button + a thumbnail `NGridView` (svg/png/jpg/jpeg/webp), scanned via the same plain-argv `find` `Process` pattern `WallpaperService.qml`/`AvatarPickerPanel.qml` already use. One shared panel instance covers both the Launcher and Control Center icon fields (`IconPickerPanelState.target`, set by `open("launcher"|"controlCenter")` right before opening) rather than two near-identical panels - clicking a thumbnail calls the matching `BarConfig` setter and closes. Both Bar-tab fields gained a folder-open Browse `NIconButton` next to their existing text field, same placement/pattern as the Profile-picture and Wallpaper-folder fields' own Browse buttons. Needed a new `windowrule=float,title:^Choose Icon$`/`center` pair in `zaris.conf` (added to both the repo copy and the live deployed copy, diffed first - the live file has real personalized differences elsewhere, like monitor layout and the lock-screen setup, that must never get blind-overwritten) - confirmed live that the WM hot-reloads `zaris.conf` on its own (`ConfigManager::tick()` polls the file's mtime every tick and reloads without a restart), so this took effect without restarting the WM itself.

  Verified live in a Xephyr sandbox: opened from the Bar tab, confirmed the picker scans and renders real thumbnails (including a real `.svg` rendering correctly, matching the launcher's own default icon), clicked one and confirmed `bar.json` persisted the new `launcherIcon` path and the bar's actual on-screen launcher icon updated live to match. Also investigated a real, reproducible side effect noticed during this testing: selecting a thumbnail while Settings is open closes Settings too, not just the picker - traced this down by reproducing the *exact same* behavior with the already-shipped `WallpaperPickerPanel.qml` (selecting a wallpaper while Settings is open closes Settings there too), confirming it's a pre-existing quirk of the whole "picker opened from within Settings" architecture, not a regression introduced by this new picker - left as-is rather than a scope-creeping fix, since it's consistent with already-accepted, already-shipped behavior. `qmllint`/codepoint scan clean on all four touched/created files (only pre-existing tolerated warning categories). New singleton and component both needed their own `qmldir` entries, and `IconPickerPanel {}` needed adding to `shell.qml`'s composition root, same as every other bar-triggered panel. Diffed every touched file against the live deployed copies before shipping, deployed live, restarted `qs`, re-applied `xset s off -dpms`, confirmed via a cropped bar screenshot that nothing regressed.

- **Settings: Bar Modules + Control Center tabs, with real drag-and-drop.** Asked the user directly how deep to go, given the bar's actual layout turned out to not be data-driven at all - Bar.qml hardcoded left/center/right structurally (logo+workspaces always left, clock always center, status modules always right), with modules.json only ever controlling per-module enabled/screens/tray flags, never position. Decided on a full rewrite rather than a narrower "reorder within the existing right-side tray only" option.

  **ModulesConfig.qml**: every module entry gained `section` ("left"/"center"/"right", default "right" - preserves today's exact layout for anyone who's never touched the new tabs) and `order` (a sort key, falling back to the module's own position in `moduleIds` when unset, for the same reason). New `barModuleIds`/`trayModuleIds` name exactly which modules are eligible for each new tab (`mediaPlayer` excluded from Bar Modules - it has no bar-side component at all, Control Center's media card replaced Bar.qml's old one; Control Center's own list scoped to the seven modules that render as Control Center's uniform toggle-tile shape - stayAwake/dnd/nightLight/network/wifi/clipboard/bluetooth - checked each one's actual QML shape directly rather than assumed, since a couple default to hidden and were easy to miss). New `orderedBarModules`/`orderedTrayModules` (what Bar.qml/ControlCenter.qml actually render from, panel-aware for per-monitor screen scoping) and `barModulesForSettings`/`trayModulesForSettings` (what Settings' own tabs list, deliberately ignoring per-monitor scoping - editing the assignment isn't rendering one monitor's bar). `reorderBarSection`/`reorderTrayModules` commit a drag-and-drop's full final ordering; `addToBarSection`/`addToTray`/`removeFromBar`/`removeFromTray` back the tabs' add-dropdown/remove-× actions (removing just disables a module rather than silently reassigning it to the other surface - moving something to Control Center is a distinct, explicit action of that tab's own "add").

  **BarStatusModules.qml rewrite**: was one hardcoded `Row` of every status module in a fixed order, always on the right - now a `section`-parameterized component (`Repeater` + a per-id `Loader`/`switch` picking the right specialized component, preserving each module's own icon/color/props exactly) instantiated three times in `Bar.qml`'s statusbar layout (left/center/right). `SystemTrayRow` and `VolumeControl` (both deliberately excluded from ModulesConfig gating already, for reasons predating this rewrite) stay pinned to fixed positions within the right section specifically, rather than becoming reorderable themselves - this exactly reproduces the original hardcoded layout's default visual order for anyone who's never touched the new tab. The "taskbar" bar layout mode (embedded dock + launcher already fixed left, workspaces fixed center) ignores section assignment entirely rather than silently dropping whatever was assigned left/center - falls back to rendering every enabled module in one right-side row, ordered. Also fixed a real latent gap surfaced by this rewrite: `wifi` had a `WifiToggle` component ready (Control Center's own Wifi tile already used it) but no bar-row entry at all, so setting its `tray` to `false` previously rendered nothing in the bar - now wired in like every other module.

  **ControlCenter.qml**: its 7-tile uniform toggle grid (previously 7 near-identical hand-authored `Rectangle` blocks) is now one `Repeater` driven by `ModulesConfig.orderedTrayModules`, with a `Loader`-selected icon component per id. Six of the seven module components (Dnd/NightLight/StayAwake/NetworkToggle/WifiToggle/BluetoothIndicator) share an identical `clickable`/`textColor`/`activeColor`/`toggle()` interface, so the shared click handler calls `.toggle()` polymorphically without needing per-id special-casing - only Clipboard (a plain icon that opens a panel, not a stateful toggle) needs its own case. The power-profile tile (no modules.json entry) stays fixed first; Wallpaper/Screenshot (a second, visually separate `Grid`, and Screenshot has no modules.json entry either) stay exactly as they were, deliberately excluded from the reorderable set rather than force-interleaved.

  **New `ModuleChip.qml`** + a shared drag mechanism added to `Settings.qml` (`chipDrag*` state, `startChipDrag`/`updateChipDrag`/`endChipDrag`, `pointInZone`/`computeInsertIndex`): deliberately not per-chip `Drag`/`DropArea` reparenting (the more "native" QML pattern) - a dragged chip is itself a `Repeater`-owned delegate, and reparenting one away mid-drag risks fighting the Repeater's own child bookkeeping. Instead the real chip stays in place (dimmed) for the whole drag; a single shared ghost `Rectangle` follows the cursor via a window-wide capture `MouseArea` (click-through/invisible while no drag is active); only on release does `endChipDrag` hit-test the drop point against whichever tab's drop-zone `Rectangle`s are visible, compute an insertion index by comparing the drop point against the target zone's existing chip positions, and commit the reorder - at which point the `Repeater` naturally regenerates every chip in its new position/section, so nothing ever needs un-reparenting.

  New Settings categories "Bar Modules" (three Left/Center/Right drop zones + an "Add a module..." dropdown per section) and "Control Center" (one flat drop zone + dropdown) replace the old single flat Modules tab (its Module/Enabled/Screens/In-tray table) entirely. Per-monitor "screens" scoping (previously an All/Primary quick toggle in that old table) is hand-edit-only now - a deliberate, documented scope trade for the new reorder capability, not silently dropped.

  Verified extensively live in a Xephyr sandbox, well beyond "does it render": added a module via the dropdown (kernel -> Left) and confirmed both `modules.json` persisted the right section/order/enabled/tray and the *live bar itself* immediately showed the kernel version next to the logo; removed it and confirmed both the chip and the bar text disappeared together; dragged a chip (Bluetooth) across sections (Right -> Left) via simulated press-move-release X11 events and confirmed it landed in the correct section both in the config and in the live bar; dragged a chip within the same section and confirmed the resulting sort order was correct (some untouched siblings keep their `moduleIds`-fallback order rather than being freshly renumbered on every drag - confirmed this is harmless by design, not a bug, since `order()`'s fallback is specifically built to sort correctly interleaved with explicit values); toggled a real Control Center tile (Do Not Disturb) through the new Repeater and confirmed via `dunstctl is-paused` that it drove the actual dunst daemon, not just its own visual state; dragged a Control Center chip and confirmed both the Settings list and the *live Control Center popup* reordered together. Caught and fixed a real labeling bug during this testing: the Control Center tab's chips initially showed the Bar Modules tab's own generic labels ("Network status") instead of Control Center's actual tile wording ("Ethernet") - added a dedicated `ccModuleNames` map for the handful that read differently in that context. `qmllint`/codepoint scan clean on every touched/created file (only pre-existing tolerated warning categories - Loader.item's `missing-property` warnings, inherent to any dynamic per-id `Loader` dispatch, joined the existing set here for the first time). Diffed every touched file against the live deployed copies before shipping - the real, already-customized live `modules.json` (many modules already flipped to Control Center via the old table) was checked directly first to confirm the new schema stays fully backward compatible with it - deployed live, restarted `qs`, re-applied `xset s off -dpms`, confirmed via a cropped bar screenshot (matched pixel-for-pixel against a before-deploy baseline, not just "looks fine") that nothing regressed.

- **Control Center: Do Not Disturb tile removed, Battery tile added, Battery opens Settings.** DND's tile is gone from Control Center's toggle grid entirely - `ModulesConfig.trayModuleIds` dropped "dnd" (it's still a real bar module, `Dnd.qml`, just no longer a Control Center one). Its toggle moved onto the bar's notification bell icon instead: `NotificationIndicator.qml` gained a right-click handler calling `DndState.toggle()` (the same left-click-opens/right-click-toggles split `VolumeControl.qml` already used for its own panel+mute pair), and the bell icon itself now doubles as the DND state indicator - it swaps to `Dnd.qml`'s own bell-slash glyph while paused, since there's nowhere else in the bar DND's state would otherwise be visible now that its tile is gone.

  A new, fixed "Battery" tile now sits in the slot DND's tile used to occupy (second position, right after the power-profile tile) - not part of the reorderable Repeater the other six toggle tiles come from, since Battery isn't a toggle (nothing to switch on/off): it shows `BatteryService.batteryIcon` (the same dynamically-charging-state-aware icon the gauge cluster already uses) and a "Battery" label, and clicking it opens Settings directly to the new Battery tab via `SettingsState.requestedCategory` (the same mechanism the Settings gear and `AudioMixerPanel.qml`'s own gear button already use). Battery's existing presence in the CPU/CPU-temp/GPU-temp/Battery gauge cluster further down Control Center is untouched by this - that's the separate, still-open vertical-layout redesign item, not duplicated or removed here.

  Verified live in a Xephyr sandbox: confirmed the DND tile is gone and Battery renders in its place with the correct icon; clicked Battery and confirmed Settings opened directly to its Battery tab; right-clicked the bar's actual notification bell icon and confirmed via `dunstctl is-paused` that it toggled the real dunst daemon state (not just a visual flag) and that the bell glyph visibly swapped to bell-slash, then toggled back to restore state. `qmllint`/codepoint scan clean on all three touched files. The real, already-customized live `modules.json` already had `"dnd": {"tray": true}` set (from earlier use of the old flat Modules table) - confirmed this deploy correctly makes it simply stop rendering anywhere in Control Center (matching intent) while the right-click toggle on the bell keeps working regardless of that stored value, rather than needing any migration. Diffed every touched file against the live deployed copies before shipping, deployed live, restarted `qs`, re-applied `xset s off -dpms`, confirmed via a cropped bar screenshot that nothing regressed.

- **Control Center: vertical CPU load/CPU temp/GPU temp/RAM gauge stack.** The 2x2 gauge `Grid` became a single-column `Column`, matching the reference screenshot's narrow stacked layout. New `MemUsage.qml` (mirroring `CpuLoad.qml`'s own `/proc`-polling style) computes RAM usage from `/proc/meminfo`'s `MemTotal`/`MemAvailable` (not `MemFree` alone - that doesn't count reclaimable cache/buffers as "available", which would read as permanently near-full even at idle, the same distinction `free -h`'s own "available" column exists to fix). Battery's gauge is gone from this cluster - it already got its own dedicated, always-visible tile earlier in this same work session (see that entry above), so keeping a second Battery presence here would've been redundant.

  These four gauges are deliberately NOT part of `ModulesConfig`'s bar-or-Control-Center module system the way the toggle-tile row is - they have no bar-row equivalent and were never meant to move there, so folding them into that dual-purpose enabled/screens/tray shape would misrepresent what they are. Instead, four new independent booleans (`ccGaugeCpu`/`ccGaugeCpuTemp`/`ccGaugeGpuTemp`/`ccGaugeRam`, all defaulting to `true` - "these 4 should always be shown by default" per the backlog) with their own setters, surfaced as four toggle switches in Settings' Control Center tab under a new "System gauges" section.

  Switching from a 2-row grid to a 4-row column meaningfully increased this cluster's own height, which pushed Control Center's fixed-height content past its bottom edge (confirmed live via screenshot, not assumed) - the weather forecast row started clipping. Fixed by shrinking each gauge's `diameter` (46 -> 38) and tightening the column's own spacing (10 -> 6) rather than growing Control Center's already-fixed `implicitHeight` - reclaiming space first, the same "shrink/reclaim before growing the window" approach used earlier in this same session for Settings' own fixed-height tabs. Confirmed via a precise pixel crop at the window's exact bottom edge (`xtree`-measured, not eyeballed) that the weather row now renders fully with real margin to spare, no clipping.

  Verified live in a Xephyr sandbox: all four gauges render correctly in the new vertical arrangement alongside the media card; toggled one off in Settings and confirmed it disappeared from the live Control Center immediately, with the remaining gauges reflowing correctly. `qmllint`/codepoint scan clean on all four touched/created files (only pre-existing tolerated warning categories; `MemUsage.qml`'s own new supplementary-plane RAM-chip glyph, 0xf035b, joined the established Nerd Font exclusion set the same way 0xf0581 did earlier). Diffed every touched file against the live deployed copies before shipping - the real live `modules.json` already had `cpu`/`cpuTemp`/`gpuTemp` all set `tray: true` from earlier customization, but since the new gauge visibility is governed entirely by the new independent `ccGauge*` booleans (defaulting to shown) rather than those `tray` flags, this didn't need any migration - deployed live, restarted `qs`, re-applied `xset s off -dpms`, confirmed via a cropped bar screenshot that nothing regressed.

- **Colors tab: preset management.** `ThemeConfig.qml`'s `presets` array used to be a single hardcoded, read-only list of the five built-in palettes - split into `builtinPresets` (unchanged, still fixed/non-removable, so there's always a guaranteed way back to a known-good starting point) plus a new `customPresets` backed by its own `presets.json` (same `FileView`+`JsonAdapter` pattern as every other `*Config.qml` - a deliberately separate file from `colors.json`, since that one is "what's live right now" and this is "a library of things that could become live"). `presets` itself is now just the concatenation of both, so `applyPreset()`/Settings' existing preset grid needed no changes to keep working with both built-in and saved presets alike.

  New `addCustomPreset(name)` snapshots the five live colors + border accent under a user-typed name (capped at 12 total between built-in and saved, matching the backlog's own limit); `removeCustomPreset(id)` only ever matches a saved preset, a no-op against any built-in id. Settings' Colors tab gained a small "×" remove button on each preset card (visible only for saved presets, sitting on top of the existing full-card click-to-apply `MouseArea` so it consumes its own click rather than also re-applying the preset it's removing) and a new name field + "Save current colors as preset" button underneath the grid (disabled, with a "Preset limit reached" label, once the cap is hit). The Settings window itself widened from 680 to 740px - the preset grid's own `Flow` needs roughly 470px of content width to genuinely fit 4 cards per row (110px card + 10px spacing × 4), which the previous width couldn't reliably guarantee.

  Verified live in a Xephyr sandbox: confirmed all 5 built-ins render 4-then-1 across two rows at the new width; typed a name, saved, and confirmed both `presets.json` persisted the exact live colors under that name and the new card appeared in the grid with its own remove button; clicked remove and confirmed the card disappeared, `presets.json` went back to an empty custom list, and - importantly - the live colors themselves were untouched by the removal (confirming the remove button's click doesn't also fire the underlying apply-preset handler). `qmllint`/codepoint scan clean on both touched files. Diffed both against the live deployed copies before shipping, deployed live, restarted `qs`, re-applied `xset s off -dpms`, confirmed via a cropped bar screenshot that nothing regressed.

- **Control Center: power button now opens a themed popup instead of launching `powermenu.sh` directly.** New `PowerMenuPanel.qml`/`PowerMenuPanelState.qml` (same `FloatingWindow` + visibility-singleton pattern as `BluetoothPanel.qml`/`WallpaperPickerPanel.qml`) - a single centered row of 5 equal-width square tiles (Lock/Suspend/Reboot/Logout/Shutdown), each icon+label with a small numbered badge (1-5) in its top-right corner matching the reference screenshot, Shutdown alone in a red/warning accent (border + icon + label), the other four neutral. Reuses `powermenu.sh`'s own exact underlying commands per action (`i3lock`, `loginctl suspend`, `loginctl reboot`, `pkill zaris`, `loginctl poweroff`) via `Quickshell.execDetached` rather than re-deriving them, so the two stay in sync automatically - `powermenu.sh` itself is untouched and still reachable directly (e.g. from a keybind or terminal), this panel is just a second, richer entry point onto the same actions. No confirmation step before acting, matching the original rofi dmenu list's own immediate-on-selection behavior. The numbering is purely visual (matching the reference), not wired to real number-key shortcuts - this WM has no existing mechanism for a popup-scoped keybind distinct from its own global keybind table. `ControlCenter.qml`'s power button now toggles `PowerMenuPanelState.visible` (also closing Control Center itself, same pattern every other Control-Center-launched panel already uses) instead of calling `powermenu.sh` directly. `qmldir` registered both the singleton and the component; `shell.qml` got a `PowerMenuPanel {}` instance; `zaris.conf` (both the shipped default and the live deployed copy, diffed first - each has real personalized differences that must never be blind-overwritten) gained a `float`/`center` windowrule pair for `title:^Power Menu$`, matching every other panel's own rule pair.

  Verified live in a Xephyr sandbox running the debug WM binary (needed for the windowrule to actually float/center the panel, since a bare Xephyr+qs sandbox has no WM to apply it - `zarisd.conf`, the debug binary's own separate config file, got a matching temporary windowrule added for the test): opened the popup via the real Control Center power button and confirmed it renders exactly as designed (icons, labels, numbered badges, Shutdown's red accent) and floats centered on screen; confirmed opening it also closed Control Center behind it; confirmed the close (×) button dismisses the panel cleanly with no dangling window or state. Lock/Suspend/Reboot/Logout/Shutdown's actual click handlers were verified structurally (identical `Quickshell.execDetached` call shape already proven by every other exec-backed toggle in this codebase) rather than fired for real even in the sandbox - all five launch commands that affect the whole system regardless of which X display triggered them (`loginctl suspend/reboot/poweroff` and `pkill zaris` are session/system-wide, not scoped to a particular `DISPLAY`), so actually clicking them would have risked suspending, rebooting, logging out of, or shutting down the real host machine, not just the sandbox. `qmllint`/codepoint scan clean on all touched/created files (the two new Nerd Font icon glyphs used here beyond the six already-established ones - lock `0xf023` and logout `0xf2f5` - confirmed present in the installed font via the same `fontTools` check every other icon addition this session used). Diffed every touched file against the live deployed copies before shipping (all diffs exactly the intended change set, nothing stray), deployed live, restarted `qs`, re-applied `xset s off -dpms`, confirmed via a cropped bar screenshot on the live 3-monitor desktop that nothing regressed.

- **Settings: new Network tab with Ethernet/Wifi mini-tabs, and the bar's Network/Wifi icons now open Settings there instead of whatever they did before.** New `NetworkInterfacesService.qml` singleton polls `nmcli device status` for every ethernet/wifi device (not just the one holding the default route, unlike the bar's own `NetworkStatus.qml`) plus each device's live throughput - a second, batched `sh` `Process` reads `/sys/class/net/<dev>/statistics/{rx,tx}_bytes` for every known device in one shell loop per 2-second tick (device names passed as trailing argv elements, `"$@"` inside the script rather than string-interpolated into it, so a device name can never be read as shell syntax even though in practice it's always plain alnum), with throughput computed as a simple delta-over-wall-clock-time against the previous tick's raw counters - the same idea `CpuLoad.qml` already uses for CPU percent from `/proc/stat`, just wall-clock-timed here since these files expose no "cycle count" the way `/proc/stat` does. Settings' new "Network" category (segmented `NTabBar` Ethernet/Wifi, driven by a new `networkSubTab` property - separate from `activeCategory`, since this is a tab *within* a tab) lists every matching device as a pill row: icon (reusing `NetworkToggle.qml`/`WifiToggle.qml`'s own existing glyphs, not new ones), name, connection state, live down/up throughput, a Connect/Disconnect button (`nmcli device connect/disconnect`, via the new service's `toggle()` - deliberately separate from `NetworkToggle.qml`/`WifiToggle.qml`'s own single-device/radio-level toggles used elsewhere, this is a genuinely different "every interface" view), and a "Details" button that lazily fetches and shows the device's real IPv4 address (`ip -4 -o addr show <dev>`, only run once expanded, not batch-polled with everything else).

  `NetworkStatus.qml` (the bar's own "Network" module, showing the default-route interface) and `WifiToggle.qml` (the bar's "Wifi" module, previously just a radio on/off toggle) both gained a click path into this new tab - `SettingsState` gained a `requestedNetworkSubTab` one-shot request/consume field (same pattern as `requestedCategory`, extended in `Settings.qml`'s own `onVisibleChanged` alongside it) so either icon opens Settings straight to its own mini-tab. `WifiToggle.qml` specifically needed a new `settingsShortcut` boolean rather than just changing its click handler outright, since it's a shared component also used as Control Center's own Wifi toggle tile - Control Center's instantiation leaves `settingsShortcut` at its default `false`, so its full-tile click keeps toggling the radio exactly as before; only the bar's own instantiation (`BarStatusModules.qml`) sets it `true`. Threading `SettingsState.targetItem` (what Settings anchors under) down to these two bar modules needed a new `barSurfaceItem` property passed through `BarStatusModules.qml` from `Bar.qml`'s own `barSurface` id (previously only `BarControlCenterLauncher.qml` needed this reference) - added to all four of `Bar.qml`'s `BarStatusModules` instantiations (statusbar layout's left/center/right, taskbar layout's single any-section row).

  **A real, found-not-assumed bug surfaced and fixed while wiring up `NetworkStatus.qml`'s new click handler**: the click never registered at all - confirmed directly (a temporary `onPressed` debug log on the module's own `MouseArea` never fired while the mouse sat over the icon, even though hover and the tooltip itself worked correctly). Root cause: `NetworkStatus.qml` was the one bar module still built on the raw `QtQuick.Controls` `ToolTip` attached property for its hover tooltip, instead of the shared `TooltipService`/`Tooltip.qml` every other clickable bar icon already uses (`BatteryIndicator.qml`, `NButton.qml`, etc.) - confirmed by grepping for `QtQuick.Controls` imports across the bar's own modules and finding this was the only one. `QtQuick.Controls.ToolTip` creates a real popup surface positioned directly over its trigger item, and on this `PanelWindow`-based bar (not a `QtQuick.Controls` `ApplicationWindow`, which is what that attached property is normally meant to sit inside) that popup was silently swallowing the click meant for the `MouseArea` underneath it - a pre-existing characteristic of this one file that simply never surfaced before, since it never had a click handler to reveal it until now. Fixed by converting it to `TooltipService.show()`/`hide()` on the `MouseArea`'s own `onEntered`/`onExited`, matching every other bar icon - this both fixed the click-blocking bug and brought the file in line with the rest of the codebase.

  Verified live in a Xephyr sandbox: temporarily flipped the sandbox's own copy of `modules.json` to `tray: false` for `network`/`wifi` (the live config already has both `tray: true`, i.e. Control-Center-shown, same as most modules by now) so both rendered in the bar for testing, confirmed clicking the bar's Network icon opens Settings directly to the Network tab's Ethernet mini-tab showing real devices (`eth1: Connected - Wired connection 2` with live-updating throughput, `eth0: Disconnected`); clicked into the Wifi mini-tab and confirmed the real `wlan0` device listed; clicked "Details" on both a connected and disconnected device and confirmed the button correctly toggled to "Hide details" while showing a real resolved IPv4 address (`192.168.1.176/24`) for the connected one and "No IPv4 address" for the disconnected one; confirmed clicking the bar's Wifi icon opens straight to the Wifi mini-tab rather than toggling the radio. `qmllint`/codepoint scan clean on all touched/created files (the one new file, `NetworkInterfacesService.qml`, needed its root object changed from `QtObject` to Quickshell's own `Singleton` type after `qmllint` caught "cannot assign to non-existent default property" for its child `Process` elements - `QtObject` has no default property to parent plain children into, unlike every other singleton in this codebase that embeds a `FileView`/`Process` directly, which all use `Singleton` for exactly this reason, confirmed by checking `ClipboardHistoryService.qml`'s own root type). Diffed every touched file against the live deployed copies before shipping (all diffs exactly the intended change set), deployed live, restarted `qs`, re-applied `xset s off -dpms`, confirmed via a cropped bar screenshot on the live 3-monitor desktop that nothing regressed.

- **Settings' Notifications tab: per-app and per-urgency ignore list.** The "surface every notification, including Quickshell's own internal ones" half of the original backlog wording turned out to already be true of this architecture, not something needing new code: Zaris has no Quickshell-internal notification mechanism separate from dunst at all (confirmed via `grep` - only `BatteryService.qml`'s low-battery warning calls `notify-send`, and that already goes through dunst same as everything else), and OSD-type volume/brightness popups already never touch the notification daemon in the first place (`OSDState.qml` is Zaris's own on-screen-display widget, no `notify-send` anywhere in its path) - both already-true facts a previous pass through this same tab had already found and left as an honest placeholder comment ("every notification ... currently surfaces the same way") rather than assumed. The one genuinely missing piece was the ignore list itself, built now: `NotificationHistoryService.qml` split its single `notifications` array into `_rawNotifications` (everything `dunstctl history` reports, unfiltered) and a new `readonly property var notifications` (what the bell badge and history panel actually read, filtered against two new settings) - since both existing consumers already just read `.notifications`, neither `NotificationIndicator.qml` nor `NotificationHistoryPanel.qml` needed any changes at all. New `notification-settings.json` (`ignoredApps`/`ignoredUrgencies` arrays, same `FileView`+`JsonAdapter` pattern as every other settings file) backs `setAppIgnored(name, ignored)`/`setUrgencyIgnored(urgency, ignored)`, plus a `knownApps` computed property (every distinct app name ever seen in the current history, deduplicated and sorted) so Settings can list real apps as toggle rows instead of requiring the user to type an exact name from memory.

  Settings' existing "Notifications" tab (previously just a Do Not Disturb toggle, a Clear All button, and the honest "not built yet" placeholder text) gained two new sections: "Notification urgency" (three toggles, LOW/NORMAL/CRITICAL - the only categorical "type" dunst's own history JSON actually exposes, so this is what "ignore a notification type" maps onto) and "Notification apps" (one toggle per app in `knownApps`, with an empty-state message when none have shown up yet). Each toggle reads ON as "shown" (matching every other `ToggleSwitch` in this codebase reading ON as "feature active") rather than ON meaning "ignored," which would invert the usual convention and likely read as backwards.

  Verified in a Xephyr sandbox: confirmed the urgency toggles persist correctly to `notification-settings.json` and reflect back into the UI on toggle; since the reference machine's live `dunstctl history` came back empty in this environment even after a fresh `notify-send` call (not chased further - not blocking, since the filtering logic itself needed verifying, not dunst's own history retention), temporarily seeded `NotificationHistoryService._rawNotifications` via a debug `Component.onCompleted` (with the auto-refresh `Timer` briefly disabled so a real, empty `dunstctl history` poll wouldn't immediately overwrite the seeded data) to confirm `knownApps` correctly deduplicates and sorts real-shaped data (two `Firefox` entries collapsed to one row, alongside `Discord`) and that toggling an app off correctly writes to the settings file - all debug scaffolding removed afterward and diffed clean against the un-seeded original before shipping. `qmllint`/codepoint scan clean on both touched files. Diffed both against the live deployed copies before shipping (diffs exactly the intended change set), deployed live, restarted `qs`, re-applied `xset s off -dpms`, confirmed via a cropped bar screenshot that nothing regressed.

- **Defaults tab: Screenshot folder's Browse button**, unblocked now that `IconPickerPanel.qml` had already proven the custom-in-QML-picker workaround for the `QtQuick.Dialogs.FileDialog` click issue - scoped here to a genuinely different UX, since picking a *folder* means navigating into/out of directories rather than choosing one file from a flat list. New `FolderPickerPanel.qml`/`FolderPickerPanelState.qml`: an `NListView` of the current directory's real subfolders (`find <dir> -maxdepth 1 -mindepth 1 -type d`, device names never interpolated into a shell string), an "Up one level" button (disabled at `/`), a path label, and a "Select This Folder" button. Deliberately built around a plain JS callback (`open(startDirectory, callback)`) rather than `IconPickerPanelState.qml`'s own fixed `target` enum ("launcher"/"controlCenter") - a folder-valued setting could reasonably live anywhere in Settings, not just one or two known fields, so the Defaults tab's Browse button passes `DefaultsConfig.setScreenshotFolder` directly as the callback rather than this panel needing to know about `DefaultsConfig` at all. The Screenshot folder row became a `Row` (`NTextInput` narrowed by the new button's width, same pattern the Bar tab's Launcher/Control Center icon fields already established) with a new folder-icon `NIconButton` calling `FolderPickerPanelState.open(DefaultsConfig.screenshotFolder, path => DefaultsConfig.setScreenshotFolder(path))`. `qmldir`/`shell.qml` registered both new files; `zaris.conf` (shipped default and live deployed copy, diffed first) gained a `float`/`center` windowrule pair for `title:^Choose Folder$`.

  Verified in a Xephyr sandbox (bare, no WM - this panel's *content* doesn't need one, only its floated/centered *position* would, and Settings itself is a WM-independent `PopupWindow` the same as every other verification this session): opened the picker from the real Defaults tab, confirmed it lists three real seeded subfolders (`Screenshots`/`Vacation`/`Wallpapers`) correctly sorted; clicked "Up one level" and confirmed it moved to the parent directory and rescanned; navigated into `Vacation` and confirmed the path label and (empty) subfolder list updated correctly; clicked "Select This Folder" and confirmed `defaults.json` persisted the exact chosen path. Also hit the same already-documented "Settings closes when a picker's selection completes while Settings is open" architectural quirk noted for `IconPickerPanel.qml`/`WallpaperPickerPanel.qml` - reproduced identically here, not a new bug, not investigated further for the same reason it wasn't before. `qmllint`/codepoint scan clean on all touched/created files. A first attempt to also verify the windowrule (float/center) via the debug WM binary in a separate Xephyr sandbox found the window really did open at the expected floated/centered geometry (confirmed via a raw `XQueryTree` dump - present, mapped, viewable, at the windowrule-implied position) but its content rendered as a blank/black capture, consistent with this session's own earlier-documented debug-WM flakiness rather than a bug in this feature - not chased further given the bare-sandbox pass above already exercised every piece of the panel's actual logic. Diffed every touched file against the live deployed copies before shipping (all diffs exactly the intended change set), deployed live, restarted `qs`, confirmed via a cropped bar screenshot that nothing regressed. The windowrule itself was not re-verified on the live desktop this pass (an attempt to click through it live was aborted early after two stray clicks landed on an unrelated window and an extra empty workspace, rather than risk further misclicks against real desktop state) - structurally identical to `IconPickerPanel.qml`'s own already-shipped-and-proven-live windowrule pattern, so treated as low-risk, but worth a real live click-through next time the machine is in front of the user.

  **Correction, reported live the next session**: that "blank/black capture" dismissed above as debug-WM flakiness was not flakiness at all - it was this exact panel genuinely stuck behind Settings, reported live as visible flicker and a missing "select folder and apply" affordance (the user, unable to see the picker's own already-built "Select This Folder" button, had gone looking for one in their system's default file manager instead - Thunar, a general-purpose file browser with no such mode, not a bug in anything here). Root-caused for real this time by reading the WM's own C++ source (`windowManager.cpp`/`events.cpp`): Settings is built on `PopupWindow`, a real X11 override-redirect window, and `Events::eventMapNotify` tracks every such Quickshell popup in `alwaysOnTopWindows`, which `reassertAlwaysOnTop()` continuously re-raises above everything else on *every single event-loop tick* - `FolderPickerPanel.qml`'s `FloatingWindow` is a plain, untracked, non-override-redirect window that can never win that fight and stays stuck underneath, no matter how it's clicked or focused.

  The natural-seeming fix - converting the picker to `PopupWindow` too, so it joins the same tracked/re-raised set - was tried and partially confirmed working (rendering-wise it now painted correctly on top of Settings, verified via screenshot) but surfaced two further problems, the second one serious enough to abandon the approach entirely rather than patch around it: the picker's own buttons still didn't receive clicks even while rendering on top (confirmed with a temporary debug `MouseArea` logging every press - zero presses ever reached the window, even dead center); and attempting to fix *that* by adding `grabFocus: true` (matching Settings' own) caused a genuine, reproducible, escalating CPU busy-loop between the two simultaneously-tracked always-on-top windows - confirmed twice, independently, via `ps aux` showing `qs` and the WM both climbing well past 100% CPU while completely idle (no input at all) after the picker opened, in both cases requiring a hard `kill -9` to stop. This is a real limitation in the WM's always-on-top mechanism (evidently only ever exercised with one such popup open at a time before now), not something fixable with a small QML change - a genuine WM-level (C++) fix was set aside as a bigger, riskier undertaking than warranted here, presented to the user as an explicit option alongside two alternatives.

  **Shipped fix, chosen by the user over investigating the WM-level path**: `FolderPickerPanel.qml` stays a `FloatingWindow` exactly as before (reverted the `PopupWindow` attempt in full, confirmed via `git diff` against the last commit that nothing of that attempt leaked through) - `FolderPickerPanelState.open()` now hides Settings for the picker's duration (`SettingsState.visible = false`) instead, sidestepping the always-on-top conflict entirely by ensuring only one such tracked popup is ever open at once; a new `restoreSettings()` is called from a `onVisibleChanged` handler on the picker's own window (fires on *any* close path, not just one button) to bring Settings back the moment the picker closes. Also added a real Cancel (×) button to the picker itself, sharing the top row with the existing "Up one level" button and path label - the picker previously had no way to back out at all short of picking a folder, since (confirmed by re-checking earlier screenshots of this and other `FloatingWindow` panels this session, e.g. `PowerMenuPanel.qml`) this WM renders no window-decoration chrome of its own for floating windows, so there was never a WM-provided close button to rely on either.

  Verified live in a Xephyr sandbox running the debug WM binary (the real target for this fix, unlike the bare no-WM sandbox the original pass used - the always-on-top behavior only exists with a real WM applying it): opened the picker from Settings' real Defaults tab and confirmed Settings fully disappears while the picker becomes fully interactive - navigated up a level, into a real subfolder, and confirmed the live subfolder list and path label update correctly at each step; clicked Cancel and confirmed the picker closes and Settings reappears automatically, back on the same Defaults tab; reopened, picked a different real folder, clicked "Select This Folder", and confirmed both `defaults.json` persisted the exact chosen path *and* Settings reappeared showing that new value in the field - the full loop, not just pieces of it. Confirmed via repeated `ps aux` checks (including a deliberate 5+ second idle wait with zero input) that CPU stays low and flat throughout, unlike the abandoned approach. `qmllint`/codepoint scan clean on both touched files. Diffed both against the live deployed copies before shipping (diffs exactly the intended additive change), deployed live, restarted `qs`, confirmed via a cropped bar screenshot that nothing regressed.

- **Control Center: brightness slider**, closing the gap in the already-largely-built "Quick settings panel" backlog item (WiFi/DND/Bluetooth/night-light toggles and a volume slider already existed there - brightness was the one piece still missing). New `root.primaryBrightnessMonitor` (`BrightnessService.getMonitorForScreen(Quickshell.screens[0])`) plus one compact icon+slider row calling `BrightnessService.setBrightness(value)` - same "primary monitor represents them all" reasoning `OSD.qml`'s own `showPrimaryBrightness()` already established, since `BrightnessService.setBrightness()`/`increaseBrightness()`/`decreaseBrightness()` already apply to every monitor at once regardless of which one a value is read from. Gated on `brightnessControlAvailable` (true only when DDC/CI, an internal backlight device, or an Apple Display is actually detected) so it stays hidden on hardware with no working brightness path, same as the volume rows already do for missing audio devices.

  **A real, found-and-fixed regression from adding a new always-visible row to Control Center's fixed-height content**, same class of problem the earlier CPU/RAM gauge-stack pass already hit: a first version (icon + label text above a full-width slider, matching the volume rows' own shape) pushed the weather row at the bottom past the panel's fixed 830px height, confirmed live via screenshot - not assumed. Fixed two ways, both "shrink/reclaim before growing the window" per this session's own established practice: the row itself was rebuilt as one compact icon+slider line with no separate label (brightness has no per-device variable name worth a text row the way volume's device description does), and the content column's own `spacing`/`topMargin` tightened `12`/`12` → `8`/`8` across every section, not just around the new row. Re-confirmed via a taller Xephyr screen (past this session's earlier debug-WM z-order issues, a bare no-WM sandbox was used instead, and sized *taller* than the panel's own content specifically so a still-cut-off bottom edge couldn't hide behind the test window's own edge the way it could in a same-size or shorter test window) that the weather line now renders in full with real margin below it.

  Verified in a Xephyr sandbox: since this specific reference machine's own DDC-capable monitors don't resolve under a nested Xephyr screen (`BrightnessService.qml`'s own `connectorMatches()` comment already documents why - XRandR output names inside a Xephyr session don't match any real `DisplayPort-N`/`HDMI-A-N` pattern), the row's real visibility gate was temporarily forced to confirm rendering/layout, then reverted before shipping - the gating condition itself is a simple, directly-reviewable boolean expression, not something that needed a forced-true smoke test to trust. Dragging the slider was confirmed to move the handle and fire `BrightnessService.setBrightness()` with no new warnings in the log. `qmllint`/codepoint scan clean. Diffed against the live deployed copy before shipping (diff exactly the intended change set), deployed live, restarted `qs`, confirmed via a cropped bar screenshot that nothing regressed - the slider's real end-to-end behavior against this machine's actual DDC monitors was not re-verified live this pass, for the same live-desktop-risk reasons noted on the Folder picker entry just above.

- **Settings: reported live as "appears for a second then disappears, and clicking the icon again doesn't do anything."** Root-caused as far as possible without being able to reproduce it directly (extensive attempts in a Xephyr sandbox running the debug WM binary - repeated open/close cycles, moving the pointer outside Settings' bounds, clicking outside it, a 15-iteration rapid-fire stress loop - never once reproduced the disappearing half, so the fix below is a well-reasoned, defensible mitigation given the evidence, not a confirmed-by-reproduction root cause): Settings is a `PopupWindow`, tracked by the WM's own always-on-top mechanism (`windowManager.cpp`'s `alwaysOnTopWindows`/`reassertAlwaysOnTop()` - the same mechanism the Folder picker entry above dug into) and continuously re-raised every event-loop tick; Control Center is *also* one of those tracked popups (confirmed by re-reading `Events::eventMapNotify` - the tracking condition checks only window type/`_NET_WM_NAME`, not `grabFocus`, so Control Center qualifies too even though it never sets `grabFocus` itself). Every place Settings opens from within Control Center (the gear button, the Battery tile) set `SettingsState.visible = true` *before* `ControlCenterState.visible = false` - meaning for one brief moment both were simultaneously tracked and visible, a real (if narrow) window for the WM's continuous re-raising to race with Settings' own first paint.

  Two fixes, addressing each half of the report separately since only one was ever actually reproduced: (1) reordered both call sites to close Control Center *before* opening Settings, removing that brief double-visible window entirely - confirmed safe with the "PopupWindow can't anchor to a target inside a window that's just been hidden" concern noted in `ControlCenter.qml`'s own header comment, since that comment is about anchoring Settings to something *inside* Control Center's own window (an approach already abandoned in favor of anchoring to the bar's surface instead, which stays valid regardless of Control Center's own visibility). (2) Added `onClosed: SettingsState.visible = false` to Settings' own `PopupWindow` - a safety net for the *actually confirmed* mechanism behind "clicking again does nothing": Quickshell's `PopupWindow` exposes a real `onClosed` signal (confirmed present in Quickshell's own qmltypes, and confirmed unused anywhere in this codebase before now) for whenever the underlying popup closes for reasons outside this file's own control - without something wired to it, a stray close leaves `SettingsState.visible` stuck reading `true` while the real window is actually gone, so clicking the gear again (`SettingsState.visible = true`, already `true`) is a silent no-op that never re-triggers the `visible` binding.

  Verified live in a Xephyr sandbox running the debug WM binary: confirmed Settings still opens and closes normally through every existing path (gear button, sidebar category clicks, the close button) with no regressions; confirmed `onClosed` doesn't fire spuriously during ordinary use (navigating categories, closing via the close button, reopening); ran a 15-iteration rapid open-Control-Center/click-gear/close-Settings stress loop specifically trying to catch the original race - never reproduced a stuck-closed state, every reopen attempt succeeded. Since the disappearing-in-the-first-place half was never actually reproduced, this is shipped as a genuine improvement addressing the *stuck* half with certainty (the `onClosed` safety net can only help, never hurt, regardless of what actually causes a stray close) and the reordering as the best-reasoned mitigation available for the *disappearing* half - worth re-reporting if it still happens, since the true root cause remains unconfirmed. `qmllint`/codepoint scan clean on both touched files. Diffed both against the live deployed copies before shipping (diffs exactly the intended change set), deployed live, restarted `qs`, confirmed via a cropped bar screenshot that nothing regressed.

- **Bar/Dock launcher icon defaults changed, Control Center icon's pixelation fixed.** Three explicit requests in one pass. (1) The bar's launcher icon default changed from `artix.svg` to `zaris-logo.png`, matching what `DockLauncherIcon.qml` was already showing - and (2) that file itself now reads `BarConfig.launcherIcon` directly instead of hardcoding a second, independent default, so a future change to the bar's launcher icon (via Settings) updates the dock too rather than the two silently drifting apart again. (3) The Control Center icon's default changed from the plain `zaris-logo-square.png` to a new `zaris-logo-circle-glow.png` - a copy of the user's own live custom Control Center icon (a personal circular Z logo with a soft glow border) at the time this default was set, per explicit request ("change the control center one to my current image"); this only changes the *fallback* for anyone who's never set their own (the user's own `bar.json` already had this exact image explicitly set, so nothing changed for them specifically - the point was making it the new starting point for anyone else).

  **Also root-caused and fixed real pixelation**, reported live specifically on the circular Control Center icon: none of the five `Image` elements referencing `BarConfig.launcherIcon`/`controlCenterIcon` (the bar's own launcher + Control Center icons in both layout modes, the two preview thumbnails in Settings' Bar tab, and now `DockLauncherIcon.qml`) set `sourceSize` - without it, a large custom user image (the live example is 1254x1254) gets decoded at full native resolution and then minified by the GPU at render time down to a ~22-52px icon, which reads as visibly aliased/pixelated, especially on a circular image where the curved edge is exactly the kind of high-frequency detail naive GPU minification handles poorly. `IconPickerPanel.qml`/`WallpaperPickerPanel.qml`'s own thumbnail grids already set `sourceSize` for this exact reason (confirmed by checking - the convention already existed, just hadn't been applied to these five spots). Fixed by adding `sourceSize.width`/`height` (roughly 2x each element's own display size, for a crisp look on HiDPI without decoding arbitrarily large sources needlessly) to all five - forces Qt to do the downscale once, at load time, with a real resampling filter, rather than leaving it to runtime GPU texture minification. Harmless on already-small sources (including SVGs, where `sourceSize` also controls rasterization resolution) since it's a cap, never an upscale.

  Verified in a Xephyr sandbox: confirmed the bar's launcher icon now shows the "ZarisWM" Z-logo (matching `zaris-logo.png`) instead of the Artix logo by default; confirmed the Control Center icon shows the new circular-glow default when `controlCenterIcon` is cleared to simulate a fresh install; temporarily enabled the (currently disabled-by-default) floating Dock and confirmed its launcher icon now renders the identical "ZarisWM" image as the bar's own launcher, proving the shared-config wiring. `qmllint`/codepoint scan clean on all touched files. Diffed every touched file against the live deployed copies before shipping (all diffs exactly the intended change set) plus copied the new bundled asset into the live assets folder, deployed live, restarted `qs`, and confirmed directly on the live desktop (not just the sandbox) via tight pixel crops that the user's own real circular Control Center icon and the bar's launcher icon both now render crisp with no visible pixelation, and that nothing else regressed across all three monitors.

- **Bundled compositor, milestone 1: inert `XComposite`/`XDamage` plumbing.** First real implementation step, following the prep/planning pass above. A new `enable_compositor` config flag (default `0`) gates every single line of new code added this pass - building and even running this exact binary is a complete no-op for anyone who hasn't explicitly opted in, since there's no redraw step yet (that's milestone 1b onward) and turning this on early makes every window go blank (manual redirection stops the X server's own automatic painting, and nothing is compositing yet - documented behavior at this stage, not a bug).

  `xcb-composite`/`xcb-damage`/`xcb-render` added to `CMakeLists.txt` (all three already installed on this dev machine per the prep pass's own `pkg-config` check). When enabled: `xcb_composite_redirect_subwindows` on the root window in `XCB_COMPOSITE_REDIRECT_MANUAL` mode, checked the same way `SubstructureRedirect`'s own selection already is (via `xcb_request_check`) - but not fatal on failure the way that one is, since compositing is still an optional layer at this stage, not core WM function; gracefully logs and stays disabled instead if the extensions are missing or another compositor already holds the redirect. Every window gets its own `XDamage` object (`XCB_DAMAGE_REPORT_LEVEL_NON_EMPTY`) via a new `CWindow::DamageObject` member, created in `addWindowToVectorSafe()` (the one common chokepoint every window-adding code path already goes through) and destroyed in `closeWindowAllChecks()` - the natural symmetric cleanup point, since that's exactly where a window's tracking entry is about to disappear for good. A new `DamageNotify` event dispatch uses the same runtime-offset pattern `RandRScreenChange` already established (extension events aren't compile-time constants a plain `switch` can match), and correctly calls `xcb_damage_subtract` to acknowledge each notification - not optional busywork, the Damage protocol requires it, or the X server considers the region still damaged and stops sending further non-empty notifications for it.

  Verified in a Xephyr sandbox with the debug binary, both states: with the flag off (the only state that matters for real users right now), the log shows the compositor setup step running and correctly doing nothing, and the desktop is pixel-identical to before - the critical safety property, since this is core WM C++ code, not QML, and a mistake here has much higher stakes than anything else this session touched. With the flag on: confirmed via log output that the redirect succeeds and Damage objects get created/fire/get acknowledged correctly (`Damage notify on window ... (damage object ...)` appearing repeatedly, matching real redraw activity like the bar's own clock ticking); confirmed windows visually go blank exactly as documented; went one step further than "didn't crash" - a direct `XCompositeNameWindowPixmap` + `XGetGeometry` probe (via a small ctypes script) confirmed the redirected pixmap is real and correctly sized (1400x38, 32-bit depth, matching the bar's actual geometry), proving the X server is still rendering into it even though nothing paints it to screen yet; confirmed the WM stays stable and idle-CPU throughout, including after real click interaction. `cmake`+`make` in the existing `build-debug` tree built clean with no new warnings (only pre-existing ones, unrelated to any touched line). Never touched the live installed WM binary or any live/shipped config file - sandbox-only verification, and the flag stays off in every real config regardless.

- **Bundled compositor, milestone 1b: trivial `XRender` passthrough draw.** Makes the desktop look normal again with `enable_compositor=1` - milestone 1 alone left every window blank by design (documented above), which is exactly the state this milestone fixes. `xcb-renderutil` (`xcb_render_util_find_visual_format`/`xcb_render_util_query_formats`, a small helper library already installed alongside the base `xcb-render` package) added to `CMakeLists.txt`/`defines.hpp` to avoid hand-rolling `PictFormat` lookup from the raw `QueryPictFormats` reply. A single root `Picture` (`CWindowManager::RootPicture`) is created once, right after the redirect succeeds in `setupManager()`, wrapping the root window itself - this is the actual on-screen destination every window gets painted onto from here on.

  A new `CWindowManager::compositorRepaint()` joins `AnimationUtil::move()` in the existing GLib tick thread (`Events::handle()` in `events.cpp`) rather than triggering off individual events, reusing the same already-proven periodic-timing infrastructure the animation system and config hot-reload already rely on. Each tick: `xcb_query_tree` on the root window gets the *real, authoritative X11 stacking order* directly from the server (deliberately not the WM's own internal `windows` deque, which doesn't track override-redirect popups like the Bar/Settings at all and isn't guaranteed to mirror true stacking order); each viewable child gets its pixmap re-named fresh via `xcb_composite_name_window_pixmap` every repaint (not cached long-term - a cached `Picture` tied to a stale pixmap shows garbage the instant the X server reallocates the backing pixmap on resize, and a real cache is milestone 6's job, once there's an actual performance problem worth the complexity) and composited onto `RootPicture` via `xcb_render_composite` with `PICT_OP_SRC` (plain overwrite) rather than `PICT_OP_OVER` (alpha blend) - every window is treated as fully opaque for this pass, which looks identical to `OVER` for the overwhelmingly common opaque case and is a deliberately deferred simplification, not an oversight: real blending only starts to matter once later milestones (shadows, blur) introduce genuine translucent content, and there's no soft/anti-aliased alpha anywhere yet anyway (corners are still a hard `XShape` binary clip, per milestone 3 below). Window geometry comes from a direct `xcb_get_geometry` on each window rather than the WM's own `RealSize`/`EffectivePosition` bookkeeping, confirmed safe by first checking there's no window reparenting anywhere in this codebase (`grep` for `xcb_reparent`/`ReparentNotify` turned up nothing) - meaning every top-level child of root is a real client window with geometry already root-relative, no intermediate frame window to account for. Window borders are native X11 borders (confirmed via `grep` for `XCB_CW_BORDER`/`xcb_configure_window(..., BORDER_WIDTH, ...)`), so they're automatically part of each window's own redirected pixmap content with no special-case handling needed.

  **A real bug found and fixed while verifying this, not just a smooth first pass**: the root `Picture` was initially created using `VisualType` (the WM's own 32-bit ARGB visual, chosen for the windows *it* creates so they can have alpha) instead of `Screen->root_visual` (the root window's *own* actual visual, chosen independently by the X server and typically the screen's plain default-depth visual). A Render `Picture`'s format has to match its underlying drawable's real depth, so `xcb_render_create_picture` for the root `Picture` was silently failing (an unchecked call at the time), leaving `RootPicture` a bad, never-actually-created ID - every subsequent `xcb_render_composite` onto it then failed with a `BadPicture` X error (error code 143, decoded by checking the Render extension's own `first_error` offset), which is exactly why the very first sandbox pass rendered solid black instead of the terminal window underneath. Root-caused by temporarily switching several of the relevant calls to their `_checked` variants and printing `xcb_request_check`'s error code/resource ID, rather than guessing - found the mismatch on the first pass once errors were actually visible. Fixed by looking up the `PictFormat` from `Screen->root_visual` instead, and by making the root `Picture`'s own creation checked/fatal-to-the-feature (logs and disables the compositor rather than silently limping on with a broken `RootPicture` if it ever fails again for some other reason) - all temporary debug-only checked cookies and log lines removed again afterward, back to the same plain/unchecked style used for the per-window Pictures created every tick (matching milestone 1's own established performance-over-diagnostics tradeoff for the hot path).

  Verified in a Xephyr sandbox with the debug binary, both states: with the flag off, log output and a root-window screenshot are identical to a plain no-compositor run (the correctness bar for this state, unchanged from milestone 1). With the flag on: opened one, then a second, real `alacritty` terminal and confirmed both render with real, live, correctly-positioned content (visible text, a native red X11 border, correct top-window stacking) rather than the blank windows milestone 1 left behind - the actual proof-of-success condition for this milestone, unlike milestone 1 where blank was the expected outcome. Confirmed via repeated `ps`-based CPU sampling over several seconds, with two real windows open and the tick-driven repaint running continuously at 60fps, that CPU stays low and flat (under 1%) rather than climbing - ruling out the kind of busy-loop this session's earlier folder-picker investigation had to abandon a whole approach over. `cmake`+`make` built clean with no new warnings beyond the same pre-existing set milestone 1 already had. Never touched the live installed WM binary or any live/shipped config file - both the sandbox's `zaris.conf` and its separate `zarisd.conf` (the debug binary's own config file) needed `enable_compositor` toggled for this pass, and both were confirmed reset back to `0` afterward; the real live `~/.config/zaris/` was grepped and confirmed to have no `enable_compositor` line at all, so it was never at risk regardless.

- **Bundled compositor, milestone 2: GLX context + trivial GL passthrough.** Replaces milestone 1b's XRender draw with a GL one via `GLX_EXT_texture_from_pixmap` (confirmed present on this machine's own GLX server extension string via `glxinfo`) - the first, and only, place Xlib appears anywhere in this otherwise pure-XCB codebase, since GLX's texture-from-pixmap API (`glXCreatePixmap`, `glXBindTexImageEXT`) is Xlib-only with no complete `xcb-glx` equivalent covering it. Kept to a brand new, independent Xlib connection (`CWindowManager::GLDisplay`) used *only* for GL/GLX calls, never shared with `DisplayConnection` (the one real `xcb_connection_t` every other WM responsibility runs on) - chosen specifically so a GL-side mistake can never touch the main event loop. `gl`/`x11` added to `CMakeLists.txt`.

  Deliberately additive, not a replacement: `compositorRepaint()` (the dispatcher every tick now calls) picks the GL path only once one-time setup (`compositorSetupGL()`, called from `setupManager()` right after milestone 1b's own XRender setup succeeds) confirms `GLReady`; any failure along the way - `XOpenDisplay`, GLX version, the `GLX_EXT_texture_from_pixmap` string check, resolving `glXBindTexImageEXT`/`glXReleaseTexImageEXT` via `glXGetProcAddressARB`, finding an `GLXFBConfig` matching the root window's own already-fixed visual, context/window/current-binding creation - leaves `GLReady` false and `compositorRepaint()` keeps using milestone 1b's XRender path instead, exactly as if milestone 2 didn't exist. `CWindowManager::GLFBConfigsByVisual` (an `unordered_map<xcb_visualid_t, SGLTexFromPixmapConfig>`) is built once at setup by enumerating every `GLXFBConfig` the server advertises and keeping the ones usable for texture-from-pixmap, keyed by visual id - the exact same "resolve once per visual, not per window per frame" shape the XRender path's own `PictFormat`-by-visual cache already established, for the same reason (a client window may use a different visual than the WM's own).

  **Three real, distinct bugs found and fixed while verifying this - none were a smooth first pass, each caught by the same "add a checked probe or a benchmark, don't guess" discipline milestone 1b's own bug used**:
  1. **A GLX context can only be current on one thread at a time.** Setup (`compositorSetupGL()`) runs on the main thread and left the context bound there; the tick thread's own `compositorRepaintGL()` then tried to bind the *same* context on a *different* thread every frame, which is invalid GLX usage - it failed with a `BadAccess` X error. Worse, this was the first Xlib call of any kind in the whole codebase, and Xlib's default error handler prints the error and then calls `exit()` - so this one bug was silently taking the entire WM process down. Fixed two ways: `compositorSetupGL()` now explicitly releases the context (`glXMakeContextCurrent(GLDisplay, None, None, NULL)`) before returning, freeing it for the tick thread to claim exclusively from then on; and a custom Xlib error handler (`zarisXlibErrorHandler`, installed via `XSetErrorHandler` the moment Xlib enters the picture at all) now logs any future GLX/Xlib error through the same `Debug::log` every other subsystem uses and returns instead of exiting - matching the "never let a compositor problem take down the whole desktop" principle every other compositor-only code path already follows, just newly relevant now that Xlib's own default behavior violates it by default.
  2. **A cross-connection resource race.** `DisplayConnection` (XCB) and `GLDisplay` (Xlib) are two independent sockets to the same server with no ordering guarantee between them. `compositorRepaintGL()` was creating each window's redirected Pixmap on `DisplayConnection` (fire-and-forget, no reply awaited) and immediately referencing that same Pixmap ID from `GLDisplay`'s own `glXCreatePixmap` call moments later - with no synchronization, the second connection's request could reach the server before the first one had actually finished creating the resource, surfacing as an intermittent `GLXBadPixmap`/`BadDrawable` ("failed to create drawable", Mesa's own stderr message). Fixed by switching that one call to its checked variant (`xcb_composite_name_window_pixmap_checked`) and calling `xcb_request_check` on it - a real round trip that guarantees the server has finished before `GLDisplay`'s request depending on it is ever sent. (Milestone 1b's own equivalent call didn't need this fix - it stayed fire-and-forget correctly, since both the Pixmap-naming call and the `Picture` creation that follows it run on the *same* connection, and a single connection's own requests are always processed by the server in the order sent regardless of replies.)
  3. **Silent no-op rendering from an unswapped back buffer.** After fixing both bugs above, every draw call reported success (`glXMakeContextCurrent` returning true, a valid non-zero `GLXPixmap`, `glGetError()` returning `GL_NO_ERROR`) and yet the screen stayed blank. The `GLXFBConfig` chosen for the on-screen `GLXWindow` (matching the root window's own visual, the only real constraint that search applied) turned out to be double-buffered rather than the single-buffered one assumed - so every frame was actually landing correctly in the *back* buffer and simply never reaching the screen. Fixed by calling `glXSwapBuffers` unconditionally at the end of every repaint - the standard, always-safe thing any real GL application does regardless of which buffering mode it ends up with (a harmless no-op if the config genuinely were single-buffered, required here since it wasn't).

  **A real, honestly-reported open question rather than a clean bill of health**: with all three bugs above fixed, the Xephyr sandbox itself reports the compositor pegging close to 100% CPU on a single core continuously (`ps` sampling over several seconds, 1-2 real windows open) - a real difference from milestone 1b's XRender path, which stayed under 1% doing the equivalent work. Investigated rather than dismissed: the sandbox's own GL renderer is confirmed to be `llvmpipe` (a pure-software rasterizer, logged directly via `glGetString(GL_RENDERER)` at setup) - Xephyr, being a nested X server, has no way to expose the *host* GPU's hardware GLX to a client running inside it, so this was always going to be software-rendered no matter what. To separate "llvmpipe in a nested sandbox is slow" from "this approach is inherently slow," a small standalone benchmark program (not part of Zaris, not committed, `/tmp` scratch only) was written and run directly against the real live display's real GPU driver - it performs the exact same create-`GLXPixmap`/bind-texture/draw-quad/release/destroy cycle `compositorRepaintGL()` does per window per frame, against a throwaway `Pixmap` and off-screen `GLXPbuffer` it creates itself (touching no real window, redirecting nothing, never becoming a WM - a read-only diagnostic, safe to run against the live session). Result on the real hardware (`AMD Radeon RX 7800 XT (radeonsi, navi32, ACO, ...)`, confirmed via the same `GL_RENDERER` string): **0.656ms for 4 windows in one frame** - roughly 4% of a 60fps frame budget, not remotely close to saturating a core. This is strong, but not conclusive, evidence that the near-100%-CPU sandbox reading is a software-rendering artifact rather than a defect in the approach itself - the benchmark doesn't include the real cross-connection XCB round trip bug-fix #2 above added, and the actual live compositor itself has deliberately never been run against the real display per this session's own standing constraint. Documented here exactly that honestly rather than claimed as resolved; worth a real measurement the next time this is picked up, ideally by adding a lightweight frame-time log line and enabling the flag briefly on the live desktop once milestone 3+ makes it visually worth doing so.

  Verified in a Xephyr sandbox with the debug binary, both states: with the flag off, log output shows no GL setup attempted at all (not even `XOpenDisplay`) and a root-window screenshot is pixel-identical to a plain no-compositor run, and `ps` confirms 0% CPU - the flag-off no-op guarantee holds completely even with all of milestone 2's new code present. With the flag on (after all three bugs above were fixed): opened a real `alacritty` terminal and confirmed it renders with real, live, correctly-positioned content including its native red X11 border, matching milestone 1b's own XRender output pixel-for-pixel as far as a visual inspection could tell - the actual proof-of-success condition for this milestone. `cmake`+`make` built clean with no new warnings beyond the same pre-existing set every earlier milestone already had (confirmed via a full clean rebuild, not just an incremental one, to rule out any warning hiding behind stale object files). Never touched the live installed WM binary or any live/shipped config file - only the sandbox's own `zaris.conf`/`zarisd.conf` needed `enable_compositor` toggled for this pass, both confirmed reset back to `0` afterward; the real live `~/.config/zaris/` was grepped and confirmed to have no `enable_compositor` line at all throughout.

- **Bundled compositor, milestone 3: anti-aliased rounded corners via a GLSL shader.** The first milestone that changes how the desktop actually *looks*, not just how it's drawn internally. Replaces the old hard `XShape` clip with a signed-distance-function (SDF) test against each fragment's own position within the window, run in a small GLSL 1.10 fragment shader - an original implementation of a standard, widely-published technique (the rounded-box SDF formula popularized by Inigo Quilez's own distance-function articles), not ported from picom or any other compositor's actual source, per this file's own licensing note above. GLSL 1.10 was chosen deliberately over anything newer specifically because it's the one shader version guaranteed to coexist with the legacy/fixed-function immediate-mode vertex submission (`glBegin`/`glVertex2f`) the rest of this compositor still uses - `gl_MultiTexCoord1` carries each vertex's own *local* pixel position (0,0 to the window's own width,height) as a second, separate set of texture coordinates alongside the real UV coordinates in `gl_MultiTexCoord0`, so the fragment shader can compute distance-to-nearest-corner directly without wrestling with `gl_FragCoord`'s own screen-space-vs-window-space orientation quirks.

  This system's `GL/gl.h` only statically declares roughly up to GL 1.2/1.4 - none of the GLSL 2.0 entry points this needed (`glCreateShader`, `glUseProgram`, `glUniform2f`, etc., 17 functions in total) have a linkable prototype at all, so every one of them is resolved manually via `glXGetProcAddressARB` at compositor setup time (`GL/glext.h` supplies the `PFNGL*PROC` typedefs), the exact same pattern already established for `glXBindTexImageEXT`/`glXReleaseTexImageEXT` in milestone 2. `applyShapeToWindow()`'s own hard clip is now skipped specifically when `GLReady` (not just whenever `CompositingEnabled`) - the milestone 1b/2 XRender fallback path still has no rounding logic of its own, so it still needs the old `XShape` clip exactly as before; only once the GL shader path is actually up does the client's own drawing get let through uncut at the corners, so the shader has real, uncut pixel data to round smoothly instead of re-rounding an already-hard-cut source. (Confirmed this matters, not just theorized: XComposite's redirected pixmap genuinely reflects `XShape`'s own clip today - a side-by-side pixel comparison between compositing-off and milestone-2-GL-passthrough-on showed the *exact same* jagged staircase corner in both, proving the old shape clip was still fully in effect the whole time milestones 1b/2 were shipped, not bypassed by manual redirection the way it might plausibly not have been.)

  **A real, second correctness problem found and fixed, not just the shader itself**: introducing genuine partial transparency at the rounded edges (unlike milestones 1b/2, which only ever drew fully-opaque rectangles with `PICT_OP_SRC`-style overwrite) broke the "never clear the framebuffer, just draw over it" approach those two milestones relied on - without a real clear/redraw of *everything* each frame, a window's previous position leaves a permanent "ghost" of its last rounded edge behind once it moves or resizes, since nothing ever repaints over that now-vacated sliver again. Fixed by capturing a one-time snapshot of the root window's own pre-compositor pixel content (the real wallpaper, however it got there) into a texture right after GL setup succeeds, and redrawing that snapshot as a full-screen base layer at the start of every single frame, before any window quads - real compositors always fully repaint every pixel every frame for exactly this reason, rather than relying on untouched pixels staying correct once any real transparency exists. Verified directly: tiled a second `alacritty` window into a first one's place (a real resize, not a synthetic test) and confirmed zero ghosting at the vacated edge, both via a visual crop and by zooming into the actual corner pixels. **A real, known, honestly-flagged limitation of this fix**: the snapshot is captured once, so a wallpaper change *during* a live session won't be reflected until the compositor restarts - not handled this pass; worth revisiting once there's a live-wallpaper-change signal worth hooking into.

  **Verified the anti-aliasing is real, not just visually assumed**: sampled raw pixel values along the rounded edge directly (not just eyeballing a screenshot, which at this resolution can make even a real gradient look like a hard edge) - found genuine intermediate blend values between the background and border colors (e.g. `(34,11,11)`, `(50,14,14)`, `(64,24,24)` progressing smoothly rather than jumping straight from `(0,0,0)` to a solid color), which a hard binary clip could never produce. `border_size`/`rounding` were temporarily raised in the sandbox's own debug config to make the effect visible at this test window's small scale (both confirmed reset back to their original values afterward).

  Real alpha blending (`glEnable(GL_BLEND)` with the standard `GL_SRC_ALPHA`/`GL_ONE_MINUS_SRC_ALPHA` pair) is genuinely active for the first time as of this milestone, not just at the rounded edges - the shader's output alpha is `texture_alpha * roundedness_alpha`, and `roundedness_alpha` is a flat `1.0` everywhere except right at a corner, so a window's *own* real alpha (e.g. the Bar's configurable `backgroundOpacity`) now passes straight through to a genuine destination blend instead of being ignored by a flat opaque overwrite the way milestones 1b/2 both did. This is exactly the mechanism this backlog entry's own original wording predicted would fix the long-dead Bar/Dock opacity sliders as a side effect - reported here as an expected consequence of the code as written, not a re-guess, but **not directly re-verified against the real Quickshell-rendered Bar this pass** (an attempt at a quick synthetic test - a small ctypes script creating its own semi-transparent ARGB test window - kept segfaulting on ctypes argument-marshaling issues unrelated to the compositor itself, and wasn't worth chasing further given the mechanism is already independently verifiable by reading the shader/blend code directly). Worth a real live check once Settings/Control Center's own translucent panels make it easy to eyeball directly.

  Verified in a Xephyr sandbox with the debug binary, both states: with the flag off, `ps` confirms 0% CPU and a screenshot shows the exact same jagged pre-existing `XShape` corner as before this milestone - confirming the fallback path is completely untouched by any of this milestone's changes. With the flag on: real anti-aliased rounded corners on a real `alacritty` window, confirmed via the raw-pixel-value check above; a genuine resize (via tiling a second window in) confirmed no ghosting at the vacated edge. `cmake`+`make` built clean with no new warnings beyond the same pre-existing set every earlier milestone already had. CPU still runs hot under the sandbox's own software (llvmpipe) GL renderer, now doing modestly more work per frame than milestone 2 (an extra full-screen background blit, plus real per-fragment shader math) - not separately re-benchmarked against real hardware this pass, since milestone 2's own benchmark already showed enormous headroom (0.656ms for 4 windows/frame on the real GPU) and shader math of this size is trivial for any GPU from the last 15+ years; still an open, honestly-unconfirmed question for the live compositor itself, exactly as milestone 2 already documented. Never touched the live installed WM binary or any live/shipped config file - only the sandbox's own `zaris.conf`/`zarisd.conf` needed touching (`enable_compositor`, and temporarily `rounding`/`border_size` for visibility), all confirmed reset back afterward; the real live `~/.config/zaris/` was grepped and confirmed to have no `enable_compositor` line at all throughout.

- **Bundled compositor, milestone 4: drop shadows.** A soft, blurred dark quad rendered directly behind each window, using the exact same rounded-box SDF formula milestone 3's own corner rounding already established - just with a much wider `smoothstep` band (`blur`, 24px) standing in for a real Gaussian falloff, and no texture sampling at all (a flat, configurable color, currently a fixed black at 45% alpha rather than anything user-configurable yet - matches this milestone's own "simpler than blur-behind" framing; real per-user shadow color/opacity/offset controls are a polish-pass concern, not this milestone's job). A second GLSL program (`GLShadowShaderProgram`) shares the exact same vertex shader milestone 3's window program already uses (attaching one shader object to two programs, deleting it only once both are linked) - `vLocalPos` means the same thing in both, local pixel position relative to the window's own top-left corner, just allowed to extend beyond `(0,0)-(width,height)` out into the shadow's own 24px margin on every side. Drawn immediately before each window's own quad, in the same per-window loop iteration - correct stacking (a window's shadow sits on top of whatever's below it, but under the window itself, and appropriately behind any window stacked above it) falls straight out of the existing bottom-to-top draw order with no extra bookkeeping. Skipped entirely for a fullscreen window, whose shadow would extend off-screen on every side for no visible benefit.

  Introduced 17 new function pointers total for the two shader programs combined across milestones 3+4 (the one new addition this pass, `glUniform4f`, for the shadow's `vec4` color uniform) - same manual `glXGetProcAddressARB` resolution as everything else GLSL-related, for the same reason (this system's `GL/gl.h` doesn't statically declare anything past roughly GL 1.2/1.4).

  **A second real, distinct bug found while verifying this - not the shadow shader itself, but the background-snapshot mechanism milestone 3 introduced**: set the sandbox's own root background to a distinct, known solid color (`xsetroot -solid "#202030"`, so any capture bug would be immediately obvious rather than blending into an already-black test background) specifically to stress-test the shadow's soft edges against something visibly different - and the "captured" background rendered back as solid black instead of the real color, in every single frame. Root-caused rather than guessed: confirmed the real root content genuinely was the right color the whole time (toggling `enable_compositor` off and re-screenshotting showed the correct `(32,32,48)`), meaning the bug was specifically in how milestone 3's `glCopyTexImage2D`-based capture (reading from the just-created on-screen `GLXWindow` itself) worked. This connects directly back to milestone 2's own first bug: that same `GLXWindow` already proved itself to *not* be a simple alias onto root's real on-screen storage on this software (llvmpipe) GL stack, since it needed an explicit `glXSwapBuffers` before anything drawn to it ever became visible at all - meaning its "read" side starts out backed by its own separate, blank buffer too, not root's actual existing pixels, so capturing from it before ever drawing to it just captured that blank initial state. Fixed by capturing the background a completely different way that sidesteps this GL-buffer-aliasing question entirely: a plain Xlib `XGetImage` straight off `Screen->root` (not through GL at all), uploaded into the same texture via `glTexImage2D` instead of `glCopyTexImage2D`. Verified fixed via the same distinct-color test - the captured/redrawn background now matches the real root content exactly.

  Verified in a Xephyr sandbox with the debug binary, both states: with the flag off, `ps` confirms 0% CPU, matching every earlier milestone's own flag-off guarantee. With the flag on: two real `alacritty` windows tiled side by side against the distinct-color background, confirmed a real, visibly soft shadow in the gap between them and above them via both a visual crop and a raw-pixel-value sample along the edge (a genuine smooth gradient - e.g. `(32,32,46)` down to `(20,20,29)` over about 20px - not a hard-edged fake). `cmake`+`make` built clean with no new warnings beyond the same pre-existing set every earlier milestone already had. CPU still runs hot under the sandbox's own software (llvmpipe) renderer (now drawing one extra quad per window on top of milestone 3's own per-frame cost) - the same already-documented, unconfirmed-on-real-hardware caveat as milestones 2 and 3, not re-benchmarked separately this pass since a second shadow quad is a trivial addition to a GPU's workload relative to what milestone 2's own benchmark already showed enormous headroom for. Never touched the live installed WM binary or any live/shipped config file - only the sandbox's own `zarisd.conf` needed touching (`enable_compositor`, and temporarily `rounding` for visibility, plus a temporary `xsetroot` fill this pass specifically to catch the background-capture bug above), all confirmed reset back afterward; the real live `~/.config/zaris/` was grepped and confirmed to have no `enable_compositor` line at all throughout.

- **Bundled compositor, milestone 5: background blur (dual-kawase).** The most visually complex piece, and the last one that changes how the desktop actually renders before the remaining milestone (6) shifts to pure polish/performance. A real, working implementation of the technique popularized by Marius Bjørge's "Bandwidth-Efficient Rendering" (ARM, 2015) - alternating downsample/upsample passes through a small chain of progressively half-resolution render targets, each pass using dual-kawase's specific 4-or-8-tap offset sampling pattern (not a naive box blur) to approximate a much larger real Gaussian blur far more cheaply than actually computing one. A standard, widely-reproduced public technique (used by KWin, Godot, and plenty of independent tutorials, not picom-specific) - written here from the general public description of the algorithm, per this file's own licensing note on not porting any specific compositor's actual shader source. Deliberately scoped to 3 levels rather than the 5-6 a full implementation might use - a real, working dual-kawase chain either way, just with a more modest blur radius; increasing the level count later (once milestone 6's performance work makes the cost worth it) is a small, contained change, not a redesign.

  **Which windows get blur, and why that couldn't be narrower**: applied behind every window this compositor already tracks in `alwaysOnTopWindows` (the Bar, Settings, Control Center, tooltips, the calendar flyout, the taskbar launcher) - the *same* set milestone... well, the same mechanism the always-on-top fix earlier this session already had to solve an identical problem for: X11 offers no reliable way to distinguish specifically Settings/Control Center from Quickshell's other override-redirect popups (confirmed by that earlier fix's own comment in `events.cpp` - every one of them reports the identical `override_redirect=1` and `_NET_WM_NAME` "quickshell" regardless of which QML file created it). Guessing a narrower heuristic here would be exactly as unjustified as it would have been for that earlier fix, so this reuses that same existing, already-populated, already-correct window set rather than inventing new detection logic - a tooltip or the calendar flyout getting blur too isn't overreach, it's the only option X11 actually offers, same reasoning, different feature.

  Two new GLSL programs (`GLBlurDownsampleProgram`/`GLBlurUpsampleProgram`) share the same vertex shader every other program here already uses (`vLocalPos` just goes unused) - one new set of manually-resolved entry points this pass, the five FBO functions (`glGenFramebuffers`/`glBindFramebuffer`/`glFramebufferTexture2D`/`glCheckFramebufferStatus`/`glDeleteFramebuffers`), same "not statically declared in this system's `GL/gl.h`, resolve manually" story as every GLSL-2.0-era function before it. `compositorDrawBlurBehind()` is a new, dedicated function (not folded into `compositorRepaintGL()`'s own body) given how much state it touches: capture the on-screen content within the window's own rect via `glCopyTexImage2D`, downsample 3 times into freshly created textures+FBOs, upsample 3 times back up into a second freshly created set, draw the final result at the window's own rect (rounded via the exact same shader/radius the window's own content uses right after, so the blur doesn't peek out past the window's rounded corners as a plain square), then tear down every level's texture/FBO - called from the per-window loop after that window's own shadow (so the blur fills the window's footprint sitting on top of the shadow's outer margin) and before its own content (so that content's real alpha blends against a freshly blurred backdrop instead of whatever was directly beneath it). All of this is per-frame, per-blur-window allocation - the same "simplicity over performance for now" tradeoff every other pass in this compositor already makes, explicitly deferred to milestone 6.

  **A real bug found while verifying this, distinct from (though related to) milestone 3's own background-capture bug**: worth being precise about why *this* capture is fine when that one wasn't, since both use `glCopyTexImage2D` against the same kind of GLXWindow. Milestone 3's bug was reading a freshly-created on-screen buffer *before anything had ever been drawn to it at all* - proven separate/blank on this software (llvmpipe) GL stack until the first real `glXSwapBuffers`. This capture happens well *after* real drawing into that same buffer, earlier in the very same frame (the background layer, plus every lower-stacked window) - reading back content this exact draw sequence just painted is completely ordinary, always-supported GL behavior, not the same buffer-aliasing hazard. Confirmed empirically, not just reasoned about: set a real checkerboard-pattern wallpaper (`xwallpaper --stretch`, restarting the compositor afterward so milestone 3's own one-time snapshot would actually capture the new pattern) and gave a real `alacritty` window genuine translucency (`window.opacity = 0.5` in its own config, real per-pixel alpha via the ARGB visual, not a synthetic test) - with the always-on-top gate *temporarily* bypassed (a one-line, clearly-marked, non-committed test edit, reverted immediately after) to prove the blur math itself works using an ordinary window as a stand-in, since spinning up a full Quickshell session just to reach a real Settings/Control Center window wasn't worth the time for this one check. Result: the checker pattern showed sharp and high-contrast everywhere outside the translucent window, and dramatically softened - genuinely blurred, not just dimmed - inside it, confirmed via both a visual crop and raw pixel sampling. Reverted the bypass and re-confirmed with the *real* shipped condition restored: the same ordinary `alacritty` window (not being always-on-top) now shows the checker pattern sharp straight through its translucent area with no blur at all, exactly as the real, narrower gate should produce.

  Verified in a Xephyr sandbox with the debug binary, both states: with the flag off, `ps` confirms 0% CPU, matching every earlier milestone's own flag-off guarantee. With the flag on and the real always-on-top gate in place (not the temporary bypass): confirmed via the ordinary-window check above that blur correctly does *not* apply outside the intended window set. The actual blur effect itself was proven correct and real (not that it's wired to exactly the right windows in a live Quickshell session, which remains as untested as every other "real Quickshell session" claim across this whole compositor project so far - the underlying `alwaysOnTopWindows` membership check itself is a single, simple, already-battle-tested lookup, not new logic, so this is a reasonable place to stop rather than stand up a full shell session for this one check). `cmake`+`make` built clean with no new warnings beyond the same pre-existing set every earlier milestone already had. CPU under the sandbox's own software (llvmpipe) renderer is noticeably higher than milestone 4's own reading with blur actively running (a 7-pass render-to-texture chain per blur window per frame is real, substantial extra work) - the same already-documented, unconfirmed-on-real-hardware caveat as every GL milestone so far, more relevant here than ever given how much more work this pass does; genuinely worth a real measurement against real hardware before this ships any further, unlike the more trivial per-frame costs milestones 2-4 added. Never touched the live installed WM binary or any live/shipped config file - only the sandbox's own `zarisd.conf` needed touching (`enable_compositor`, temporarily `rounding`), plus a temporary wallpaper/alacritty-opacity change for this pass's own verification, all confirmed reset back afterward; the real live `~/.config/zaris/` was grepped and confirmed to have no `enable_compositor` line at all throughout.

- **Bundled compositor, milestone 6: polish - idle-skip performance, and the opacity sliders confirmed for real.** The last milestone in the plan, and the first one to actually stand up a real Quickshell session (`qs -c ... -d`, the live shell config, not a bare `alacritty` stand-in) against the debug WM - every earlier milestone's own "not re-tested against a real Quickshell session" caveat gets resolved by this one, for the specific claim that mattered most (the opacity sliders).

  **Performance: skip the repaint entirely on an idle tick.** Every earlier milestone redrew the whole screen unconditionally 60 times a second, whether or not anything had actually changed - the ROADMAP's own framing for this milestone ("so redraws only touch actually-damaged regions instead of the whole screen every tick") was scoped down deliberately to the simpler, higher-leverage half of that: skip the *entire* repaint when nothing changed, rather than also implementing true sub-region partial redraw (`glScissor`-based, tracking an actual damaged rectangle rather than a boolean) - a real, further optimization, but a materially bigger one, and not the bottleneck this session's own testing ever actually hit. A new `CompositorDirty` flag, set unconditionally from `recieveEvent()` on literally any real (non-ignored) event rather than hunting down and individually flagging every visually-relevant event type - a deliberate simplicity/safety tradeoff (an occasional redundant repaint costs nothing that matters; missing a real visual change and leaving stale content on screen would be a far worse bug). `compositorRepaint()` skips its whole body when `!CompositorDirty`, *unless* some window is actively `getIsAnimated()` - position/size animation doesn't necessarily generate a fresh X event every tick on its own (`AnimationUtil::move()` pushes interpolated geometry via `xcb_configure_window` directly, without routing back through `recieveEvent()`), so relying on the event-driven flag alone would visually freeze mid-slide.

  **A dramatic, directly-measured win**: idle CPU (a real Quickshell Bar plus two `alacritty` windows mapped, zero input) dropped from consistently 80-100%+ under every one of milestones 2 through 5's own sandbox testing to **roughly 1%** - confirmed responsive throughout, not just quiet: real mouse motion, real typed text (via `XTestFakeKeyEvent`, not just a static screenshot) both correctly triggered a fresh repaint (visible new terminal content appearing) and the desktop correctly settled back to near-zero CPU afterward. This is a genuinely large win specifically *because* every one of milestones 2-5's own "CPU runs hot under llvmpipe, probably fine on real hardware" caveats were about the *active* per-frame cost - this fix instead eliminates almost all of the frames that were ever being drawn in the first place on a desktop nobody is actively touching, which matters regardless of how fast any individual frame is on whatever hardware it runs on.

  **A real, separate, higher-impact bug found and fixed while finally testing against the real Bar**: standing up a genuine `qs` session for the first time this whole project and lowering the Bar's real `backgroundOpacity` (`bar.json`, both a dramatic 0.15 test value and the real default 0.75) showed the Bar rendering fully opaque solid black regardless - the opacity slider still looked completely dead even with a real, working compositor now doing real `GL_BLEND`. Root-caused rather than guessed: a direct pixel readback of the Bar's own real redirected pixmap (`XGetImage` on the `XCompositeNameWindowPixmap`-named pixmap, not just eyeballing a screenshot) showed the *source* data already had exactly correct alpha (`a=38` at `backgroundOpacity: 0.15`, `38/255 ≈ 0.149` - matching) - with a suspiciously tiny, near-black RGB (`(2,2,4)`) at that same low alpha. That's the signature of **premultiplied alpha** (RGB pre-scaled by its own alpha, the convention both Qt's `ARGB32` rendering and X11/XRender's own `ARGB32` format use) - but this compositor's blend function, `GL_SRC_ALPHA`/`GL_ONE_MINUS_SRC_ALPHA`, is the textbook pairing for *straight* (non-premultiplied) alpha, which double-applies the alpha scaling on top of data that was already scaled, silently darkening every translucent draw down toward black. This had been present, unnoticed, since milestone 3 first turned on real `GL_BLEND` - never caught earlier because every verification through milestone 5 tested against plain, fully-opaque `alacritty` windows (where straight-vs-premultiplied makes no visible difference at alpha=1) or a synthetic test window using non-premultiplied data, never the real, premultiplied, actually-translucent Bar. Fixed in three places, all consistent with the same premultiplied convention: the window shader now outputs `texColor.rgb * alpha` (extending the texture's own existing premultiplication to cover the corner-rounding alpha too, not just leaving `texColor.rgb` as-is); the shadow shader now explicitly premultiplies its own plain uniform color by its total alpha (a no-op for the current pure-black shadow color, since black premultiplied by anything is still black, but correct for any future non-black shadow color); and the blend function itself changed to `GL_ONE`/`GL_ONE_MINUS_SRC_ALPHA`, the correct pairing for premultiplied source data. Verified mathematically, not just visually: sampled the Bar's real background pixel against both a pure-black desktop and a bright solid blue (`#4488cc`) one, and confirmed the exact expected blend result in both cases (`src_premultiplied + dest*(1-alpha)`) - and confirmed no regression for fully-opaque content (the fix is a no-op whenever alpha is 1 everywhere, which was every single case every earlier milestone's own testing ever exercised).

  **A related, quieter bug caught and fixed in the same pass**: `compositorDrawBlurBehind()`'s own downsample/upsample chain renders into freshly allocated FBO textures (`glTexImage2D` with `NULL` data - genuinely undefined initial content per the GL spec) while `GL_BLEND` was left enabled the whole time from the per-window loop's own setup, meaning every blur pass was blending its output against undefined memory rather than overwriting it. Not yet visibly wrong in milestone 5's own testing (undefined memory from a freshly allocated texture happened to read back as effectively black/zero on this particular driver, which is close enough to a correct additive-blend base case to go unnoticed), but a real latent bug rather than a hypothetical one. Fixed by explicitly disabling `GL_BLEND` for the whole blur chain and re-enabling it before the final blurred quad is drawn (which *does* want blending, so its own soft edge blends against the shadow beneath it rather than cutting off hard).

  Verified in a Xephyr sandbox with the debug binary, both states: with the flag off, `ps` confirms 0% CPU, matching every earlier milestone's own flag-off guarantee (unaffected by any of this milestone's changes, which are all inside code paths the flag-off state never reaches). With the flag on: a real `qs` session (the live shell config) plus two real `alacritty` windows confirmed idle CPU around 1%, confirmed responsive to real input (mouse motion, typed text), and confirmed the Bar's real background genuinely blends with the desktop behind it at both a dramatic low opacity and the real default - a visibly blue-tinted Bar against a blue test wallpaper, not solid black, with clean edges and no fringing at the Bar's own rounded corners or the terminals' own. `cmake`+`make` built clean with no new warnings beyond the same pre-existing set every earlier milestone already had. Never touched the live installed WM binary or any live/shipped config file - only the sandbox's own `zarisd.conf` (`enable_compositor`, temporarily `rounding`) and the sandbox's own *copy* of `bar.json` (temporarily `backgroundOpacity`, reset back to the real default `0.75` afterward) were touched, both confirmed reset; the real live `~/.config/zaris/` and `~/.config/quickshell/bar.json` were confirmed completely untouched throughout (diffed byte-for-byte against the sandbox's own copy at the end, identical).

- **Bundled compositor: wallpaper rendered upside down on the real live desktop (all monitors), reported after the first real `enable_compositor=1` live test.** The very first bug caught by actually running this on the real desktop rather than only in a Xephyr sandbox - and specifically the kind that a symmetric or solid-color sandbox test could never have revealed. Milestone 3's background-snapshot quad (`GLBackgroundTexture`, redrawn as every frame's base layer - see that milestone's own writeup) had its V texture coordinates flipped from what the underlying data actually needed: `XGetImage` fills the texture with row 0 = the top of the captured region (standard X11/Xlib convention), which OpenGL then treats as texture coordinate `t=0` once uploaded via `glTexImage2D` - but the quad's own texcoords mapped the screen's top edge to `t=1` and the bottom edge to `t=0`, exactly backwards. Every sandbox verification through all 6 milestones used either a plain solid color or a symmetric checkerboard for the test wallpaper, neither of which can show a vertical flip at all - confirmed as the actual root cause (not guessed) by reproducing it in a Xephyr sandbox with an unambiguous top-red/bottom-blue gradient test image: top read back as blue, bottom as red, exactly reversed, matching the live report exactly. Fixed by flipping every V coordinate in that one quad; re-verified with the same gradient image afterward (correct red-top/blue-bottom, matching the no-compositor baseline exactly) plus a real window on top to confirm no other regression.

  This one *did* need to go on the real live desktop to fix, unlike everything else in this file - the live binary was rebuilt (`build/`, the Release configuration, not `build-debug/`) and installed over `~/.local/bin/zaris` via a safe swap (copy to a temp file in the same directory, then `mv` over the target - a plain `cp` in place failed with "Text file busy" since the live WM process had the old binary open; the atomic rename left that already-running process completely undisturbed, only affecting what a *future* WM start picks up). The previous live binary was preserved first as `~/.local/bin/zaris.pre-compositor-backup` for rollback. `~/.config/zaris/zaris.conf` also got its own real `enable_compositor=1` line added (with a comment noting it needs a full WM restart/reboot to take effect, since it's read once at startup, not hot-reloaded) - the first and only real config file this whole project has ever intentionally modified, done explicitly at the user's own request to test the finished compositor for real, not as part of any earlier milestone's own sandbox-only verification.

- **Bundled compositor, milestone 7: real-desktop performance - pipelined XCB round trips.** Reported live, immediately after the wallpaper-flip fix above: with the compositor genuinely on for the first time, every window this compositor's blur/shadow path touches (the Bar, Control Center, Settings, the calendar flyout, the notification/sound popups - the same always-on-top set milestone 5 already established) loaded and operated "very very slowly," while an ordinary window (a terminal) felt fine.

  **The more obvious suspect was ruled out first, not assumed away**: a standalone benchmark of the *exact* dual-kawase FBO chain `compositorDrawBlurBehind()` runs (3 downsample + 3 upsample passes, matching that function's own code precisely, including a real `glCopyTexImage2D` capture from actually-just-rendered content, not a stale buffer) against the real GPU driver came back at roughly a quarter of a millisecond for one realistic 800x830 panel - nowhere near slow enough to explain the report. The real cause was something every sandbox test this whole project ever ran had systematically under-counted: `compositorRepaintGL()`'s per-window loop issued three separate XCB requests per window - `xcb_get_window_attributes`, `xcb_get_geometry`, and (since milestone 2) a *checked, blocking* `xcb_composite_name_window_pixmap` - each one a fully synchronous round trip, one window at a time, every single dirty frame. Every sandbox test ran against a handful of windows on one monitor; the real desktop has three permanently-tracked Bar windows alone (one per monitor) before counting anything the user actually has open - XCB round-trip latency that's imperceptible once is not imperceptible repeated dozens of times, sequentially, every frame.

  Fixed by pipelining rather than removing anything: every `get_window_attributes`/`get_geometry` request for every window on screen is now issued up front, *before* any reply is collected (XCB cookies queue a request without blocking, so issuing N requests costs roughly the same as issuing one) - collapsing what were `2N` sequential blocking round trips into one overlapped batch. The pixmap-naming step goes further: fired unchecked for every window with no per-window wait at all, then the *entire frame* gets exactly one verification round trip (a plain `xcb_get_input_focus`, chosen only because it's cheap and harmless to call) issued after every pixmap-naming request - safe specifically because XCB guarantees requests on the same connection are processed by the server strictly in the order sent, so one reply to a request issued after all the pixmap-naming calls proves the server has already finished every one of them too. This preserves milestone 2's own original correctness fix (the cross-connection race between `DisplayConnection` and `GLDisplay` that the per-window check was originally added to guard against) while paying for the guarantee once per frame instead of once per window.

  Verified in a Xephyr sandbox with the debug binary: two real `alacritty` windows rendered correctly (rounded corners, shadows, no errors) under the restructured pipeline, idle CPU stayed low (~1.8%, consistent with milestone 6's own dirty-flag skip still working correctly), and the flag-off state remained a confirmed 0%-CPU no-op (this whole restructure lives entirely inside the GL-only code path). **Honestly scoped, not fully closed**: the sandbox's own single-monitor, few-window setup can't reproduce the real multi-monitor, real-window-count scale that actually triggered this report, so this fix is shipped as a well-reasoned, definitely-correct, definitely-not-worse optimization against a real identified inefficiency - not as a confirmed-by-measurement fix of the user's exact reported slowness. Deployed to the live desktop the same way as the wallpaper fix (rebuilt `build/` release binary, installed over `~/.local/bin/zaris` via the same safe atomic swap); if this doesn't fully resolve the reported slowness, the next step is adding real timing instrumentation to the live binary to measure exactly where the remaining time goes, rather than continuing to guess.

- **[Done]** Desktop widgets, first one: Clock. Third of the four original Phase 3 candidates picked up (after Do Not Disturb and Control Center) - floating, wallpaper-anchored widgets, matching Noctalia's own four (Clock/Media/Weather/SystemStats). Only Clock built this pass; the other three are deliberately deferred, not abandoned - continuing shell UI/functionality work more broadly took priority once the underlying mechanism was proven out with one widget.

  **New WM-side primitive, not just QML**: every existing Quickshell popup (Settings, Control Center, the calendar flyout, ...) uses `alwaysOnTopWindows`/`reassertAlwaysOnTop()` to stay raised - a desktop widget needs the opposite, sitting *behind* every real window so normal use is never blocked by it. Added the mirror image: a new `alwaysbottom` window rule (`CWindow::AlwaysBottom`, `CWindowManager::alwaysOnBottomWindows`/`reassertAlwaysOnBottom()`/`setAWindowBottom()`, called every event-loop tick alongside the existing always-on-top pass, using `XCB_STACK_MODE_BELOW`) - populated once in `doPostCreationChecks()` rather than per-map-event like the popup mechanism, since desktop widgets are normal managed `FloatingWindow`s (real ICCCM windows with real titles), not Quickshell's override-redirect `PopupWindow`s, so the existing popup-detection heuristic (matching on `WM_NAME == "quickshell"` since none of those expose a real per-window title) doesn't apply and isn't needed here. `alwaysbottom` implies `nointerventions`+`float`, same as the existing `nointerventions` rule, so it's automatically excluded from tiling, focus-stealing-on-map, and window-cycling for free via checks already in place. No fullscreen-coverage carve-out unlike `reassertAlwaysOnTop()` - a fullscreen window covering a desktop widget is the expected behavior, not something to fight.

  Also added a `topleft` window rule (mirror of the existing `topright`, monitor-relative corner anchor) - none of the five existing position rules (`center`/`topright`/`topcenter`/`bottomcenter`/`leftcenter`/`rightcenter`) covered a plain top-left corner, which is where a clock widget conventionally sits.

  `DesktopClock.qml`: `SystemClock` + `DateTimeConfig.timeFormat`/`dateFormat` (same source `BarClockText.qml` already uses) in a big time / smaller date `Column`, no background box at all - reads as part of the desktop, not another panel. Two new `Style.qml` tokens (`fontSizeDesktopL`/`fontSizeDesktopXL`) since the existing scale tops out at 24pt, too small for a desktop centerpiece. Fixed position for now (`windowrule=topleft 40 60`), not drag-to-reposition like Noctalia's own version (which saves per-widget position/scale to disk) - a real follow-up, matches Dock's own "static default position for now" precedent.

  **On/off toggle**: new `DesktopWidgetsConfig.qml` singleton (`~/.config/quickshell/desktopwidgets.json`, a plain per-id `enabled` map - `ModulesConfig.qml`'s fuller shape with `screens`/`tray`/`section`/`order` doesn't apply, desktop widgets aren't per-monitor or bar-embedded), plus a new "Desktop Widgets" Settings category with one toggle per `widgetIds` entry (just "Clock" so far). `DesktopClock.qml`'s `visible` gated on `DesktopWidgetsConfig.isEnabled("clock")`.

  Verified live end-to-end, not just in a sandbox: `cmake --build` clean, `qmllint` clean on every touched/new file, WM log confirmed all three window rules matching and applying (`float`/`alwaysbottom`/`topleft`) with the widget landing at the expected offset from whichever monitor had the cursor at session start (same monitor-selection behavior every other position rule already has - not new to this feature), confirmed floating and excluded from `_NET_CLIENT_LIST` same as Dock, confirmed the Settings toggle round-trips correctly through the real JSON file and the widget actually hides/shows live.

- **[Done]** Bar polish: shortened the default bar clock format. Spotted by actually looking at the live bar (a real screenshot, not guessed) - the old default (`dateFormat` "dddd MMMM d yyyy" + `timeFormat` "HH:mm", combined as `clockFormat`) rendered "Tuesday September 15 2026 09:37" in the bar, long enough to visibly dominate it next to the rest of the module row. Changed the default `dateFormat` to "ddd" ("Tue"), giving "Tue 09:37" out of the box - the full date is still one click away via the existing calendar flyout (`BarClockText.qml`'s click handler, unchanged). Per explicit user direction ("I like giving the user the option to pick what they want"), the old verbose format wasn't removed, just demoted from default: added "ddd" as a new first entry in `DateTimeConfig.dateFormatOptions` (Settings → Date/Time → Date format dropdown) alongside the existing six presets, so anyone who wants the old full date back (or any other format) still can. Verified live: before/after screenshots of the real bar confirm the shortened format renders correctly after a `qs` restart. Caught a real gap along the way, not just a documentation nit: `maim` isn't actually installed on this reference machine, despite being `screenshot.sh`'s real dependency (`DEPENDENCIES.md`/`shell/README.md` both already list it correctly) - meaning the shipped screenshot keybind is currently broken here, not just an ad-hoc debugging inconvenience. Worked around it for this pass's own screenshots via `import -window root` (ImageMagick, already installed), but `maim` itself still needs installing for the real feature to work.

- **[Done]** Swapped the bar/dock launcher icon to Arch Linux's own logo, replacing the plain Zaris-branding default - copied straight from this machine's own `/usr/share/pixmaps/archlinux-logo.svg` (a real system file, no web fetch needed) into `assets/arch-logo.svg`, `BarConfig.qml`'s `launcherIcon` default updated to match. Also removed the orphaned `assets/artix.svg` - nothing actually referenced it any more (the default had already moved to `zaris-logo.png` in an earlier pass), just a stale asset left behind plus stale mentions in `shell/README.md`/`shell/INTERFACE.md` claiming it was still the bar's logo - fixed both. `shell/README.md`'s "Known machine-specific bits" note (still describing the old single Artix/OpenRC machine) updated too, now that this project has run on two.

- **[Done]** Launcher icon and Workspaces are now real, toggleable/reorderable/section-assignable bar modules, not fixed leftmost elements. Previously hardcoded directly in `Bar.qml`'s "statusbar" layout (a plain `Image`+`MouseArea` and a bare `Workspaces {}`), completely outside `ModulesConfig.qml`'s existing enabled/screens/section/order system that every other bar item (kernel, CPU, volume, ...) already goes through via Settings' "Bar Modules" tab - per explicit user request ("any and all of these bar widgets should be toggle-able... change which ones are visible, in what order, where they're pinned"). Added `launcher`/`workspaces` to `ModulesConfig.moduleIds` (defaulting to `section: "left"`, `order: 0`/`1` - every other module defaults to "right" via `section()`'s own fallback, so these two needed an explicit default to keep rendering first/left for anyone who's never touched the Bar Modules tab) and two new cases + `Component`s in `BarStatusModules.qml`'s existing per-module switch/Loader, then deleted the now-redundant hardcoded copies from `Bar.qml`. The generic Bar Modules tab UI needed zero changes - it was already driven entirely by `ModulesConfig.barModuleIds`/`moduleNames` with no hardcoded id list of its own, so adding the two new `moduleNames` entries was enough for Settings to pick them up automatically. Deliberately scoped to the "statusbar" layout only - "taskbar" mode's launcher+embedded-dock-strip+centered-workspaces arrangement is a different, deliberately fixed architecture (`Bar.qml`'s own longstanding comment on why that layout ignores section assignment entirely), left untouched.

  Hit one real bug getting there: `Image`'s own `implicitWidth`/`implicitHeight` are read-only (follow the source's native pixel size), so the delegate `Item`'s `width: loader.item.implicitWidth` binding (the same pattern every other module already relies on) crashed the whole shell config on load (`Invalid property assignment: "implicitHeight" is a read-only property`) the first time through - fixed by wrapping the launcher `Image` in a plain `Item` with its own explicit `implicitWidth`/`implicitHeight: 22`, which (unlike `Image`) allows that. `qmllint` came back clean on the broken version too - a real gap in what it catches, this one only surfaced at actual runtime load.

  Also surfaced a second real gap along the way: `wmctrl` (`Workspaces.qml`'s only real dependency, `wmctrl -d`/`wmctrl -s`) wasn't installed on this reference machine either, same story as the `maim` gap above - meaning the workspace pills would have silently rendered as an empty, invisible row even once properly wired in as a module. Both gaps found simply because building/verifying real features on this machine keeps surfacing real missing packages, not because either feature is new.

  Verified live end-to-end: `qmllint` clean on every touched file (post-fix), a real screenshot of the live bar confirms the Arch logo, the workspace pills (1/2/3/4, correct active-pill highlighting), and the shortened clock format all render correctly together in their expected left/center/right positions.

- **[Done]** Four more bar modules: RAM, Weather, Brightness, Taskbar - picked from a Noctalia/DMS widget-catalog gap analysis (researched both shells' actual bar widget lists against Zaris's own `ModulesConfig.moduleIds`), specifically the four that could reuse already-built services rather than needing new backend work (deferred: a privacy indicator, an audio visualizer, a VPN toggle, a color picker - each needs real new plumbing, not just wiring an existing service into the bar). All four go through the exact same `ModulesConfig`/`BarStatusModules` module system Launcher/Workspaces just joined - the "Bar Modules" Settings tab needed zero changes for any of them, confirming that generic system really does scale to new modules for free once `moduleIds`/`moduleNames` know about them.

  **RAM**: `MemUsage.qml` existed only as a bare value source for Control Center's gauge (no `Row`/icon/text of its own, unlike `CpuLoad.qml` which already did double duty). Gave it the same self-contained `Row` `CpuLoad.qml` has, so it now works standalone in the bar and with `visible: false` in Control Center exactly like `CpuLoad`/`HwmonSensor` already do. Enabled by default (`section` falls back to "right" like every other stat).

  **Weather**: `WeatherWidget.qml` (icon+condition+hi/lo+5-day forecast) is Control Center/calendar-flyout-sized, far too large for the bar. New `WeatherIndicator.qml` - icon+temperature only, reusing `WeatherService`'s already-fetched data, no new fetching logic. Off by default in the bar, same reasoning as `kernel` - Control Center already shows weather unconditionally, so it's still visible somewhere out of the box even disabled here.

  **Brightness**: new `BrightnessIndicator.qml` - icon+percentage for the primary monitor (`BrightnessService.getMonitorForScreen(Quickshell.screens[0])`, the same "primary" concept `OSD.qml`'s `showPrimaryBrightness()` already uses) plus scroll-to-adjust (`increaseBrightness()`/`decreaseBrightness()`, the same calls the keybinds already drive). Self-hides via `brightnessControlAvailable` when there's no controllable monitor - enabled by default like `battery`, gated the same graceful-degrade way. Not visually verified on this machine (no ddcutil/brightnessctl-controllable monitor here), only that the code path evaluates cleanly with no errors when `available` is false.

  **Taskbar**: `DockIcons.qml`/`DockItemsService.qml` (the running-apps icon strip) already existed, embedded directly in `BarConfig.layoutMode: "taskbar"`'s own fixed arrangement. Same component now also available as an opt-in module (off by default) for `"statusbar"` layout, for anyone who wants the icon strip without switching the whole bar's layout mode. Not visually verified against a real embedded strip in statusbar mode this pass (the module wiring matches the taskbar-layout usage verbatim, and it's off by default so nothing changed for anyone), worth a real check next time it's toggled on live.

  **Two real bugs caught getting there, neither caught by `qmllint`**: `BrightnessIndicator.qml` referenced `Quickshell.screens` without importing `Quickshell` (only `QtQuick`) - `qmllint` came back clean, the actual runtime scene log caught it (`ReferenceError: Quickshell is not defined`). Separately, its `MouseArea` for scroll-to-adjust sat directly inside the root `Row` - Quickshell's `Row` rejects anchored children entirely (`Cannot specify ... anchors for items inside Row. Row will not function`), the same class of layout mistake `StayAwake.qml`/`NightLight.qml`'s own established `Item` root + named inner `Row` + sibling `MouseArea` pattern exists specifically to avoid - restructured to match. Also hit a font-coverage gap: RAM's first icon choice (`nf-fa-memory`, Font Awesome 5's newer glyph, U+F538) rendered as a broken tofu box on this machine's actual Nerd Font build - swapped to `nf-md-memory` (Material Design Icons' much more completely-patched range), confirmed rendering correctly via a real screenshot.

  Verified live: `qmllint` clean on every file (post-fixes), zero errors/warnings in the real `qs` scene log after restart, real screenshots confirm RAM (chip icon + "12%") renders by default and Weather (icon + "73°F", real fetched data) renders correctly once toggled on and off again through the same live `modules.json` Settings itself writes to.

## Bugs — reported 2026-09-09

All 7 fixed/resolved same-day - see the matching entries folded into Done/working above (always-on-top, taskbar launcher focus, bar opacity's real root cause, Settings text fields, `NIconButton` icon centering, the notification panel's anchoring, and the Chromium/Flatpak media-art root cause).

## Backlog — reported 2026-09-09, not yet started

Large batch from the user, captured here verbatim-ish before any sequencing/priority decision. Reference screenshots (from a Noctalia/DankMaterialShell-style shell, not Zaris itself) were provided in a follow-up message for six of these items - described inline below where they apply. Screenshots aren't saved as files anywhere in this repo; if the visual description below ever needs re-checking against the originals, ask the user to resend them.

- **[Paused - explicit user decision] Settings Wallpaper tab: add a "Wallpaper Selector" button** that opens a bar-anchored picker showing every image file in the configured default wallpaper folder. Reference screenshot shows a fairly rich version of this: titled popup ("Wallpaper Selector", gear + close buttons), an "Apply to all monitors" toggle with description text, a filter/search field, a current-folder breadcrumb path with history/folder/grid-view/eye/refresh icon buttons, and a `GridView` of thumbnails (4 per row shown) - the selected one gets a highlighted border, a star badge, a checkmark badge, and a small strip of accent-color dots (likely a per-wallpaper extracted color palette preview) in its bottom corners. Asked the user how to scope the "Apply to all monitors" toggle specifically, since it implies real per-monitor wallpaper support that doesn't exist at all today (`WallpaperService.setWallpaper()` calls plain `xwallpaper --zoom <path>` with no `--output` targeting, so today's behavior is already "same image across the whole desktop" with no per-monitor code path to toggle away from) - a real new xrandr-output-aware capability, not just a UI toggle. User's answer: leave the whole feature as-is for now, revisit once Beta is reached, and move on to other backlog items in the meantime - not started.
- **First-run setup wizard** (explicitly post-Beta, pre-1.0 - not now). Walks a new user tab-by-tab through Settings to establish good starting defaults: layout choice (dock enabled/disabled, for statusbar mode), color scheme, profile display name, profile picture (a default set of ~10-20 to pick from, replaceable later with a custom one), weather location + hide-location choice, bar background opacity/height, and launcher icon choice (a handful of major distro logos - Arch/Debian/Fedora - or the ZarisWM logo as the non-distro default option). Control Center's icon always defaults to the ZarisWM logo regardless of what's picked for the launcher (changeable later, same as today). This expands on the existing "First-run setup wizard" entry further down this same backlog - the two should be merged/reconciled when this is actually picked up, not treated as two separate wizards.

## Backlog — discussed, not started

- **Per-module color overrides.** Once the custom color/palette system above exists, let individual bar modules override the shell's global theme colors with their own - e.g. a specific module keeping a fixed accent even after the user picks a different global palette. Explicitly deferred by the user until after the core theming system itself is built.

  A Debian 13 VM (`debian13-zaris`) was mid-unattended-install for this when the verification pass was deliberately paused to prioritize the Noctalia design-system port instead — not abandoned, just deprioritized. The VM is left running (a local libvirt VM, no ongoing cost) in case it's worth resuming from where it was rather than starting over; the install had gone through one restart already (an initial "desktop" profile attempt was pulling in an unwanted full GNOME install, aborted in favor of a slimmer profile) and was still short of a DHCP lease over an hour in when paused, a genuinely slower run than the equivalent Fedora verification.

  Worth being precise about scope once this resumes: both this item and the "done" Fedora entry above only ever verified that `zaris` *builds* cleanly against each distro's documented dependency list (`cmake` configure + build, confirmed via a real VM) - neither involved actually running a full live Zaris desktop session end-to-end (login, bar/dock/launcher up, real interaction) on that distro. A genuine live-session test is still open on every distro except this reference machine's own Artix/XLibre setup. Also flagged for later, per an explicit user question this session: Zaris has zero XLibre-specific code (confirmed via `grep` - it only links standard `xcb-*` protocol libraries, identical on X.Org and XLibre), so it's expected to run identically on plain X.Org, but that's an architectural expectation, not something actually verified live yet either. Once development reaches a stable point, the plan is to do real live-session testing on full X11 (plain X.Org, not XLibre) installs across Debian, Fedora, Ubuntu, etc., not just build checks.
- **[Beta blocker, in progress] Hone the default config, bar, and dock into a clean out-of-the-box look.** The goal for beta isn't "everything customized" (that's what the first-run wizard below is eventually for) — it's a sensible, presentable, mostly-empty-desktop-with-good-bones default that someone can install and immediately configure from, rather than something that visibly reads as "someone else's dotfiles." The `zaris.conf`/`modules.json` review pass is done (see Done above), and the dock itself now exists (`Dock.qml`/`DockConfig.qml`/`DockIcons.qml`/`DockLauncherIcon.qml`/`PinDialog.qml`) — pinned + running apps, a static launcher icon, right-click pin/unpin via a themed confirmation dialog, reserved-space or floating modes with a Settings toggle for both, all confirmed working live. Still open: further visual polish pass(es) on the bar itself.

  **First round, done**, against real Noctalia v4.7.7/v5 screenshots the user supplied (bar, Control Center, settings, launcher) — this corrected a wrong assumption: Noctalia's bar is *not* separated pill/island segments, it's one continuous bar with bare icons directly on it, already close to what Zaris had. Three concrete, screenshot-backed changes: (1) `Workspaces.qml`'s per-workspace pills — already existed, just had a hardcoded `radius: 4` instead of a design-system token; switched to `Math.min(Style.radiusS, height / 2)` for a true rounded-capsule shape matching the reference. (2) `NetworkStatus.qml` now shows the real interface name (e.g. "enp2s0") instead of a generic "Connected"/"Disconnected" label, matching the screenshot exactly — the interface name was already being parsed for the tooltip, just never exposed as the main label. (3) `MediaWidget.qml` gained a compact read-only progress scrubber (a slim `Colors.pill`-track/`activeColor`-fill bar) next to the track text, matching Noctalia's bar-mini media widget.

  Building the scrubber surfaced and fixed a real, independent bug in `MediaService.qml` (a Phase-1 port, close-to-verbatim from Noctalia): `trackLength` was a pure reactive binding off `currentPlayer.length`, but confirmed live (via a debug `Timer` + `qs log`) that this specific MPRIS source (a Firefox/YouTube browser-extension bridge — exactly the "generic browser tab" case this file's own header comment already calls out as the primary reason it was ported close to verbatim) doesn't reliably emit change-notification signals for `length`, so the binding froze at its first-seen value forever while `currentPosition` (which already polls via `positionTimer` rather than a plain binding, for what must be this exact same reliability reason) kept updating correctly. Fixed by converting `trackLength` to an imperatively-refreshed property (`_refreshTrackLength()`), called at every point `currentPosition` already refreshes from `currentPlayer` (`switchToPlayer`, `updateCurrentPlayer`, `positionTimer.onTriggered`, and all three `Connections` handlers) — confirmed live the value now updates every second instead of staying frozen.

  One caveat surfaced that's a real, documented limitation rather than something fixable here: this exact Firefox/YouTube bridge turned out to report no `mpris:length` at the D-Bus level *at all* (confirmed directly via `busctl ... Metadata` — no such key present for any track tried), yet Quickshell's own `MprisPlayer.length` still returns a nonzero, climbing value for it regardless — some internal fallback/synthesis with no exposed way to distinguish it from a real reported duration (`CanSeek` was checked as a possible tell and ruled out — reports `true` regardless). For that one class of source the scrubber renders as permanently near-full rather than hidden; real players (mpv, VLC, Spotify, etc.) report `mpris:length` properly per the MPRIS spec and aren't affected. Documented directly in `MediaWidget.qml`'s comment rather than chased further with a heuristic, since nothing in the exposed API reliably tells real data apart from the fallback.

  Verified in a Xephyr sandbox (config loads clean; workspace pills can't be tested there — no WM running to back `wmctrl -d`) and confirmed on the **live desktop**: workspace pills render with the correct rounder shape, `MediaWidget.qml`'s scrubber renders correctly (confirmed via a temporary forced-40%-fill debug override in the sandbox, since this player's own real length data is the unreliable one described above), and `MediaService.qml`'s fix confirmed via direct before/after debug-log comparison against real, live playback.

  **Second round, Control Center specifically**, three concrete live complaints: the 4 circular gauges needed to sit further right, the 5-day forecast had gone missing, and the settings/power/sound-output icons looked off-center. The gauges cluster turned out to just be packed immediately after the media card by a plain `Row`'s own left-to-right layout, leaving ~100px of dead space before the panel's real right edge unused - switched to explicit left/right anchoring instead. The forecast wasn't actually missing from the code at all - `WeatherWidget.qml`'s forecast row was there and correctly wired, just clipped down to its day-label line by the panel's own fixed 830px height (confirmed by testing in an artificially much taller Xephyr screen and seeing the *exact same* clipping point - not a screen-size issue). Reclaimed the needed ~40px by trimming the toggle grid's `tileHeight` (64→56 across 5 rows) rather than growing the panel's own fixed height, which its own comment already warns against re-litigating (Control Center must never require scrolling, a standing user decision). For the icon centering, built a dedicated Xephyr test harness rendering each candidate glyph at its *exact* real on-screen size (8x magnified with a crosshair marking true geometric center) rather than guessing from how a tiny 26px icon looks eyeballed or nearest-neighbor-zoomed (which turned out to be actively misleading for two of the three) - confirmed the sound-output icon (U+F028) has a real, substantial rightward ink offset baked into the glyph itself, while settings/power measured close to centered once examined properly. Added a reusable `iconOffsetX`/`iconOffsetY` nudge to `NIconButton.qml` (defaults to 0, a no-op for every other icon in the app) and applied it only to the one confirmed case, rather than nudging all three on the original report alone.

  Verified in a Xephyr sandbox against a real copy of the live shell config, then deployed to the real live desktop directly (`qs` restarted) since this is QML, not WM C++ - confirmed clean load with no errors, diffed the live files against the repo afterward and confirmed identical once synced.

  **Third round, small follow-ups from live use of the second round's changes**: the settings/power icon nudge got dialed back twice more (-3 → -1.5 → -2) chasing a "just a hair off" report each time - small enough that no further sandbox measurement was worth it, just trusting the direct live feedback each pass. The power-profile tile's own icon (always showing "Balanced" in practice, since `power-profiles-daemon` isn't installed on this machine and `currentProfile` never leaves its `""` default) got the same dedicated-test-harness treatment as the earlier icons - confirmed a real ~6px rightward offset on that specific glyph and fixed it with `anchors.horizontalCenterOffset`, scoped to the fallback case rather than a literal `=== "balanced"` check. Separately investigated a live report that the dock was "enabled and in reserved mode" despite being toggled off in Settings - `dock.json` and `Dock.qml`'s own reactive visibility bindings both checked out correctly, and a live screenshot taken mid-investigation showed no dock present at all; concluded this was most likely a stale rendering caught in a brief window between two of this session's own several live `qs` restarts, not a real bug - confirmed resolved by the user on a fresh check.
- **True live drag-and-drop retiling.** Other windows should visibly reshuffle in real time while a window is being dragged over them, not just snap into place on release. This is genuinely new work — even Hyprland doesn't fully do this today.
- **[Done - all 6 milestones shipped, see below] Bundled compositor.** Blur, shadows, and real anti-aliased rounded corners via XComposite/XDamage + GLX. (Animations and basic X-Shape-based rounded corners already worked before this without a compositor — this was about blur/shadows specifically, plus a visual upgrade to rounding.) This was the single biggest piece of work in the whole project, built as six independently-committable, independently-verified milestones (1 through 6 below) across several sessions - real, working `XComposite`/`XDamage` redirection, an `XRender` passthrough fallback, a full GLX/GLSL rendering path, anti-aliased rounded corners, drop shadows, dual-kawase background blur, and a damage-driven idle-skip performance pass, plus the Bar/Dock opacity sliders confirmed genuinely working end-to-end against the real Quickshell Bar (see milestone 6's own writeup for the real premultiplied-alpha bug that was silently defeating them even after real GL blending existed from milestone 3 onward). `enable_compositor` stays `0` in every real/shipped config - this is real, tested, working code, but still opt-in, not yet the default for real users.

  **Current architecture, confirmed by reading the source directly rather than assumed**: the WM is pure XCB core protocol today - zero XComposite/XDamage/XRender/GL/EGL anywhere (confirmed via `CMakeLists.txt`'s linked libraries: `xcb`, `xcb-shape`, `xcb-randr`, etc., nothing composite-related). Rounded corners are done via the X SHAPE extension (`applyShapeToWindow()` in `windowManager.cpp`) - a hard binary clip mask with no anti-aliasing, which is exactly why corners currently look slightly jagged up close. The main loop (`main.cpp`) calls a blocking `CWindowManager::handleEvent()` in a tight `while`, which itself blocks on `xcb_wait_for_event()` inside `recieveEvent()` - meaning new X events (including future `XDamage` notifications) arrive and get dispatched through the exact same `switch` in `recieveEvent()` every other event type already does, no separate plumbing needed for that part. Critically, there's already a **second thread** doing periodic work independent of that blocking wait: `Events::setThread()` spins up a GLib thread (`g_thread_new`, since `glib-2.0` is already linked) running `handle()` in `events.cpp`, ticking at `1000 / max_fps` ms (config-driven, default 60fps) forever, coordinating with the main thread via two plain busy-wait flags (`mainThreadBusy`/`animationUtilBusy`) rather than a real mutex - not textbook-clean, but a real, working, already-proven pattern. `ConfigManager::tick()` (hot-reloading `zaris.conf`) already runs from inside this exact thread. **This is where a compositor's own periodic redraw step belongs** - the timing infrastructure to drive continuous frame composition already exists; a redraw call just needs to join `ConfigManager::tick()` there.

  **Licensing note that changes the original "just adapt picom's code" framing**: picom is not simply "MIT-licensed" as earlier phrasing here assumed - checking the actual per-file SPDX headers shows it's dual-licensed, file by file: `src/backend/gl/glx.c` is MIT, but `src/backend/gl/gl_common.c` and `src/backend/gl/egl.c` are MPL-2.0 (a weak-copyleft license requiring modified versions of *that specific file* to stay MPL-2.0), and the shader files (`blur.c`, `shaders.c`) carry no SPDX header of their own to check either way. Zaris itself is BSD-3-Clause. Directly copying MPL-2.0-covered source into a BSD-3-Clause file would be a real licensing problem, not just a style concern - the plan is to **not copy any picom source directly**, and instead write an original implementation informed by the underlying *techniques* (which aren't copyrightable, only their specific expression is): dual-kawase blur is a published, widely-documented algorithm (originally a 2003 SIGGRAPH technique, popular in game engines and other compositors like KWin, not picom-specific), and anti-aliased rounded corners via a signed-distance-function test in a fragment shader is a standard, widely-taught graphics technique, not something original to picom either. Reviewing picom's actual shaders (done during this prep pass, MIT-clone in a scratch dir, not committed anywhere) confirmed both are compact (roughly 30-80 lines of GLSL each) and straightforward to reimplement from the general technique rather than needing to adapt picom's specific text - so the licensing wrinkle doesn't meaningfully change the amount of real work here, just means "write our own" instead of "port," consistent with how this project's own README already distinguishes real ports (credited, MIT-sourced) from original work.

  **Dependencies already installed on the reference machine, confirmed via `pkg-config --exists`**: `xcb-composite`, `xcb-damage`, `xcb-render`, `egl`, `gl`, `glesv2` all present already - no new system packages needed here specifically, though this should still be added to `DEPENDENCIES.md` once real code lands, since a fresh install on another distro isn't guaranteed to have mesa/libglvnd installed by default the way this dev machine already does.

  **Proposed milestones, each independently committable and verifiable (this can genuinely span multiple sessions - small verified steps, not one giant change)**:
  1. **[Done - see below] Inert `XComposite`/`XDamage` plumbing.** Add `xcb-composite`/`xcb-damage`/`xcb-render` to `CMakeLists.txt`. `xcb_composite_redirect_subwindows` on the root window in `XCB_COMPOSITE_REDIRECT_MANUAL` mode (manual = the WM controls painting, not the X server automatically - what every real compositor does). Create an `XCB_DAMAGE` object per window (`xcb_damage_create`, `XCB_DAMAGE_REPORT_LEVEL_NON_EMPTY`), add a new `XCB_DAMAGE_NOTIFY` case to `recieveEvent()`'s switch. Originally scoped to also include a trivial `XRender` passthrough draw so the desktop would look normal again immediately - split that back out into its own step instead (see the new milestone 1b below) once it became clear that's a real, separately-verifiable chunk of work on its own, not a "trivial" finishing touch to the same commit.
  1b. **[Done - see below] Trivial `XRender` passthrough draw.** The real next step - manual redirection alone (milestone 1, done) makes windows stop being painted to the screen at all, so this isn't optional polish, it's what makes the compositor safe to ever turn on outside a sandbox: an `XRender` composite of each window's redirected pixmap back to the root window, in the right position/stacking order, on every relevant `DamageNotify`/`ConfigureNotify` - no GL yet, just proving the desktop can look *exactly* like it does today while every frame is actually flowing through our own compositing step instead of the X server's automatic one.
  2. **[Done - see below] GLX context + trivial GL passthrough.** Replace milestone 1b's XRender passthrough with a plain textured-quad GL render of each window - still no shader effects, still must look pixel-identical to today at the end of this step.
  3. **[Done - see below] Rounded corners.** Swap the existing `XShape` hard clip for an anti-aliased signed-distance-field rounded-rect test in the fragment shader (an original implementation of the standard technique, per the licensing note above) - the first *visibly better* result, and likely the point the Bar/Dock opacity sliders start working too, since real alpha blending exists for the first time.
  4. **[Done - see below] Shadows.** A blurred dark quad rendered behind each window - simpler than blur-behind, good next increment.
  5. **[Done - see below] Background blur (dual-kawase).** The most visually complex piece - an original implementation of the down/up-sample dual-kawase technique, applied behind translucent surfaces (Settings/Control Center's own semi-transparent panels are the immediate beneficiaries).
  6. **[Done - see below] Polish.** Confirm the opacity sliders for real, and a performance pass so redraws only touch actually-damaged regions instead of the whole screen every tick.

  **Also turns out to gate the Bar tab's "Background opacity" slider** (reported 2026-09-09 as a bug: "Height works correctly, but Opacity does not change the Opacity of the Bar"). Investigated as a real bug first, not assumed - `BarConfig.qml`/`Bar.qml`'s opacity wiring is actually correct (`Qt.rgba(c.r, c.g, c.b, BarConfig.backgroundOpacity)`, the exact fix from an earlier pass this session), confirmed by testing at both extremes (0.75 and 0.1) against a solid-red root background in a Xephyr sandbox - the bar rendered fully opaque black at both settings, with zero visible difference. Root-caused via a small ctypes probe checking `_NET_WM_CM_S0`'s selection owner (the standard way an X11 client detects whether a compositing manager is present): **no owner on either the sandbox or the real live desktop** - there is no compositing manager running anywhere on this system yet. Without one, X11 has no mechanism to blend a window's alpha channel against whatever is behind it at all - a window's RGBA pixel buffer can carry real alpha data (confirmed the WM already sets up a 32-bit ARGB visual, `Depth = 32`, needed for the existing XShape-based corner rounding), but nothing ever performs the actual cross-window blend step without a compositor redirecting windows through one. This is genuinely the same missing piece as this entire backlog item, not a separate bug with its own fix - the opacity slider (and the equivalent one on Dock's `DockConfig.backgroundOpacity`) will start working correctly the moment a compositor exists, with no QML/WM changes needed on top. Not treated as a "bugs, not yet fixed" item any more since there's nothing to fix there in isolation; tracked here instead as one more concrete thing this backlog item unblocks once picked up.

  **Correction, once the compositor actually existed (milestone 6)**: "no QML/WM changes needed on top" turned out to be half right - a compositor existing was necessary but not sufficient. Milestone 6 found a real, separate WM-side bug once it actually tested this end-to-end against the live Bar for the first time: the compositor's own blend function assumed *straight* (non-premultiplied) alpha, while Quickshell/Qt's real ARGB32 rendering is premultiplied - so every translucent draw was silently double-darkening down to solid black, which is exactly why the slider still looked "dead" even after milestone 3 turned on real `GL_BLEND`. See milestone 6's own writeup below for the fix; confirmed working end-to-end against the real Bar afterward, with a visibly blended translucent background at both a low (0.15) and the default (0.75) `backgroundOpacity`.
- **[Reversed] Bundled compositor removed in favor of picom.** After living behind `enable_compositor` (default `0`, so this never shipped active to a real user) for several sessions, the whole XComposite/XDamage/GLX/GLSL implementation above was deleted rather than finished off and defaulted on. Reasoning: most of the bugs logged in this section since the compositor landed (the upside-down wallpaper, the per-window XCB round-trip perf pass, the premultiplied-alpha opacity-slider fix) were this code's own overhead, not anything the rest of the WM needed — and a mature, widely-deployed compositor like picom already handles the countless GPU/driver edge cases a from-scratch GLX renderer doesn't, for less ongoing maintenance than a second, WM-owned rendering path. Removed: `CWindowManager::compositorRepaint`/`compositorRepaintXRender`/`compositorRepaintGL`/`compositorSetupGL`/`compositorDrawBlurBehind`, every `GL*`/`Damage*`/`RootPicture` member and the `SGLTexFromPixmapConfig` struct in `windowManager.hpp`, the `enable_compositor` config key, `CWindow::DamageObject`, `Events::eventDamageNotify`, and the `xcb-composite`/`xcb-damage`/`xcb-render`/`xcb-renderutil`/`gl`/`x11` deps from `CMakeLists.txt`/`defines.hpp`. `applyShapeToWindow()`'s XShape-based rounding (the pre-compositor mechanism, independent of any of this) is untouched and is once again the only rounding path. Zaris now does nothing to claim or interfere with `_NET_WM_CM_S0`, so an external compositor is free to redirect subwindows itself; picom is the recommended one, with a starter config at `shell/zaris/picom.conf` and `exec-once=picom` wired into both `example/zaris.conf` and `shell/zaris/zaris.conf`. Verified: `cmake --build` clean after the removal, zero remaining `compositor`/`GLX`/`damage`/`xcb_render` references anywhere in `src/`.
- **Security features.** Not scoped yet — worth a dedicated look at what's worth adding. Some starting points: whether the idle-lock/manual-lock screen can be bypassed via a WM keybind firing while `i3lock` holds its keyboard grab (should already be safe since the grab should take priority, but unverified); hardening how much trust the WM places in client-supplied EWMH hints (the focus-stealing fix already moved to trusting no `_NET_ACTIVE_WINDOW` source value at all, for exactly this kind of reason); and being upfront in the docs about what's simply inherent to X11's security model (any client can inspect/control any other client's windows, keystrokes, etc. — a real architectural difference from Wayland, not something a WM-level fix can paper over).
- **GUI settings app, full WM config.** `zaris.conf` itself (keybinds, window rules, gaps/borders, etc.) is still hand-edit-only - no GUI category exists for any of it yet. Extending the settings app to cover that (as new categories in the sidebar shell - see Done above) is still deferred until just before release.
- **Screen recording.** Natural pair with `screenshot.sh` — same ffmpeg-family tooling, just recording instead of a single frame.
- **Keybind cheat-sheet overlay.** Press a key, see all current keybinds — useful once the binds list grows.
- **First-run setup wizard.** Now that `shell/` (see Done above) ships a working default, the end goal is a guided first-run flow that walks a new user through customizing it — which bar modules to show, what's tucked into the hidden tray, whether to enable the dock, etc. — rather than hand-editing `modules.json` and `zaris.conf`. Also where config export/import would live, for moving a tuned setup to another machine. Depends on the full WM-config GUI item above for the pieces that aren't already covered by the existing bar-module settings window.
- **Scratchpad / dropdown window.** A floating window (typically a terminal) bound to a key that instantly hides/shows rather than needing to be found/refocused normally - i3's scratchpad, or the dropdown-terminal patches common on dwm/bspwm. Popular enough to be worth having; scoped as a genuinely new WM-side mechanism (move-to-scratchpad + toggle-visibility on a designated window), not a windowrule tweak.
- **Sticky windows.** A window that stays visible on every workspace instead of belonging to just one (bspwm/i3) - useful for something like a music player or a notes window you always want at hand regardless of which workspace you're on.
- **Urgency hints.** A window demanding attention (e.g. an incoming chat ping) should flash its workspace indicator in the bar even while you're focused elsewhere (i3's urgency hint, sent via `_NET_WM_STATE_DEMANDS_ATTENTION`/`WM_HINTS` urgency bit) - the WM would need to pick up and expose that hint, and `Workspaces.qml` would need to react to it.
- **Workspace back-and-forth.** One keybind toggles between the current and the immediately-previous workspace (i3's `workspace back_and_forth`), rather than needing to remember and re-press the actual workspace number.
- **Smart gaps / smart borders.** Auto-hide gaps and/or the window border when only one window occupies the tiling area, restoring them the moment a second window is tiled alongside it (i3-gaps/bspwm staple) - purely a visual default that's easy to miss until you notice single-window layouts look unnecessarily inset.
- **Keyboard-driven resize mode.** A modal "resize" keybind state (press a key, then use arrow keys/hjkl to grow/shrink the focused window, escape to exit) as an alternative to mouse-drag-only resizing, matching i3's resize mode.
- **IPC/scripting interface.** i3 has `i3-ipc`/`i3-msg`, Sway has `swaymsg`, Hyprland has `hyprctl` - a socket-based protocol external tools and scripts can use to query window/workspace state and issue commands live, distinct from `zaris.conf`'s static config. Right now the only way anything (including Quickshell) talks to this WM is through config file rules - there's no live query-and-control path. Bigger, unscoped architectural work, not a quick add.
- **Portability of `shell/` to other X11 WMs.** A "super super long term" idea, explicitly not a design goal right now. Most of the shell's individual widgets/services (status modules, Bluetooth, MPRIS, the whole ported Noctalia ingredient set) only depend on generic X11/D-Bus/system services and would already work unmodified on any EWMH-compliant X11 WM. What wouldn't: every floating window's on-screen *position* (launcher, settings, OSD, dock's floating mode, etc.) relies entirely on Zaris's own custom windowrule system (`center`/`topright`/`bottomcenter`), and the bar/dock's rounded corners rely on Zaris's own X11 SHAPE-extension code applied to dock-type windows - neither exists on any other WM, so this exact config would render with square corners and misplaced floating panels elsewhere today. Making it genuinely portable would need either a fallback positioning/rounding layer built into the shell itself (not relying on the host WM at all), or equivalent hooks in whatever other WM it's asked to run under - neither attempted, both nontrivial.
- **Scrolling/column tiling layout (Niri-style).** A third tiling mode alongside the existing dwindle/master, offered as a user-selectable choice rather than a replacement - an infinite horizontal strip of columns with no wrapping, scrolled left/right to bring a column into focus, the way Niri does on Wayland. Explicitly requested as a "workflow choice" feature, not tied to Wayland specifically - it's a layout *policy* in the WM's own tiling code, so it's just as buildable on this X11 WM as dwindle/master were. Bigger in scope than a settings toggle: needs a real column-viewport model (horizontal scroll offset, per-column width) and new keybinds (scroll/focus-left/focus-right), closer in size to the original dwindle/master implementation than to a shell/QML feature. Explicitly deferred by the user until after the current Bar/Taskbar-mode work is complete - not started.
- **Monocle/tabbed layout.** A third tiling layout alongside the existing dwindle/master, where one window fills the whole tiling area and the rest queue behind it, cycled via keybind (bspwm's monocle, i3's tabbed layout) - useful for terminal-heavy or single-focus workflows where dwindle/master's always-visible splits aren't wanted.
- **Session save/restore.** Reopen the same apps in the same workspace layout after a WM restart or reboot (i3-resurrect-style) - not scoped yet; would need to decide how much (just workspace/position, or actual app relaunch commands) is realistic to restore given arbitrary GUI apps don't have a generic "reopen with this exact state" mechanism.
- **Fullscreen rule for non-Steam-launcher games.** Lutris/Heroic don't relabel `WM_CLASS` the way Steam does (no `lutris_*`/`heroic_*` equivalent), so the one-rule-covers-everything trick doesn't carry over. Need to test an actual Lutris/Heroic game to find out whether it already goes fullscreen on its own (via its own EWMH fullscreen request, a separate pre-existing code path) or needs a per-game rule. Also worth testing whether adding a Lutris/Heroic game to Steam as a non-Steam shortcut and launching it through Steam picks up the same `steam_app_*` class (likely, since that's Steam's own overlay/launch wrapper doing the relabeling — but unconfirmed).

## Open decisions

None currently open.
